---
quick_id: 260519-q34
description: Regenerate Figure 1 as a hero + anti-hero pair (DSP.md §1 motivator)
type: execute
autonomous: false
requirements:
  - Q34-01  # Add two new signal builders to research/utilities/dsp_helpers.py
  - Q34-02  # Add hero/anti-hero data-prep + render functions to figure_1.py
  - Q34-03  # Render two new PNGs via runnable entrypoint; do not auto-commit
files_modified:
  - research/utilities/dsp_helpers.py
  - research/utilities/__init__.py
  - research/dsplot/figures/figure_1.py
files_created:
  - assets/images/generated/figure_1_hero_click_plus_tone_v1.png
  - assets/images/generated/figure_1_antihero_low_vibrato_v1.png

must_haves:
  truths:
    - "Hero PNG exists at assets/images/generated/figure_1_hero_click_plus_tone_v1.png"
    - "Anti-hero PNG exists at assets/images/generated/figure_1_antihero_low_vibrato_v1.png"
    - "v8 chirp PNG (figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png) is untouched"
    - "figure_1.py::_prepare() (the locked chirp pipeline) is untouched"
    - "research/dsplot/figures/motivator.py is untouched"
    - "dsplot framework files under research/dsplot/{panels,plottables,style.py} are untouched"
    - "Both new PNGs share v8's 3-row layout, orange #e1641a inst-freq overlay, gray row-1 fill, log-spaced freq grid, and twin-axis Hz labels on row 1"
    - "Hero shows a horizontal 60 Hz band + vertical click streak in upper bins on the CWT row"
    - "Anti-hero shows visible CWT smear of the ±20 Hz wobble vs. clearer STFT wobble tracking with a long nperseg"
  artifacts:
    - path: "research/utilities/dsp_helpers.py"
      provides: "build_click_plus_tone() and build_low_vibrato() signal builders"
      contains: "def build_click_plus_tone, def build_low_vibrato"
    - path: "research/dsplot/figures/figure_1.py"
      provides: "_prepare_hero/_prepare_antihero data prep + render_hero/render_antihero render entrypoints + render_all() multi-figure dispatcher"
      contains: "def _prepare_hero, def _prepare_antihero, def render_hero, def render_antihero, def render_all"
    - path: "assets/images/generated/figure_1_hero_click_plus_tone_v1.png"
      provides: "Hero: 60 Hz tone + broadband click; CWT wins"
    - path: "assets/images/generated/figure_1_antihero_low_vibrato_v1.png"
      provides: "Anti-hero: 60 Hz ±20 Hz wobble at 8 Hz; STFT wins"
  key_links:
    - from: "figure_1.py::_prepare_hero / _prepare_antihero"
      to: "research/utilities/dsp_helpers.py::build_click_plus_tone / build_low_vibrato"
      via: "from utilities.dsp_helpers import ..."
      pattern: "build_click_plus_tone|build_low_vibrato"
    - from: "figure_1.py::render_hero / render_antihero"
      to: "compute_full_cwt() + _stft_on_log_bins() + dsplot panels/plottables"
      via: "shared utilities + dsplot reuse"
      pattern: "compute_full_cwt|_stft_on_log_bins"
    - from: "render_hero / render_antihero"
      to: "assets/images/generated/figure_1_{hero,antihero}_*_v1.png"
      via: "fig.savefig(...)"
      pattern: "figure_1_hero_click_plus_tone_v1|figure_1_antihero_low_vibrato_v1"
---

<objective>
Regenerate Figure 1 (DSP.md §1 motivator) as a hero + anti-hero pair that
complements the locked v8 chirp. Two NEW PNGs only. The v8 chirp render and
its `_prepare()` pipeline stay frozen.

- HERO: a click + sustained tone signal where the CWT visibly beats the STFT
  (CWT shows a clean horizontal 60 Hz band PLUS a vertical click streak;
  STFT must smear one axis or the other).
- ANTI-HERO: a 60 Hz ±20 Hz vibrato at 8 Hz where the CWT visibly smears
  (Morlet time support ~100 ms at 60 Hz can't keep up with a 125 ms
  modulation period) while STFT with a long `nperseg` tracks the wobble more
  clearly.

Purpose: surface both halves of the time-frequency tradeoff so the user can
make the CWT case AND honestly disclose CWT failure modes in DSP.md prose.

Output: two new PNGs under `assets/images/generated/`, plus signal builders
and render functions wired through a `render_all()` entrypoint.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@/home/eddie-water/dev/python/sub-shader/.planning/STATE.md
@/home/eddie-water/dev/python/sub-shader/research/dsplot/figures/figure_1.py
@/home/eddie-water/dev/python/sub-shader/research/utilities/dsp_helpers.py
@/home/eddie-water/dev/python/sub-shader/research/utilities/plotting.py
@/home/eddie-water/.claude/projects/-home-eddie-water-dev-python-sub-shader/memory/project_figure_1.md
@/home/eddie-water/.claude/projects/-home-eddie-water-dev-python-sub-shader/memory/project_dsplot_framework.md
@/home/eddie-water/.claude/projects/-home-eddie-water-dev-python-sub-shader/memory/feedback_png_naming.md
@/home/eddie-water/.claude/projects/-home-eddie-water-dev-python-sub-shader/memory/feedback_no_unverified_merges.md
@/home/eddie-water/.claude/projects/-home-eddie-water-dev-python-sub-shader/memory/feedback_collaborative_authoring_patterns.md

<interfaces>
<!-- Extracted from current code so the executor doesn't re-explore. -->

From research/utilities/dsp_helpers.py (existing pattern for build_waypoint_chirp at line 274):
```
def build_waypoint_chirp(
    sr: int,
    duration_s: float,
    waypoints: tuple[tuple[float, float], ...],
    *,
    bc_type: str = "natural",
    clip_to_waypoints: bool = True,
) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
    # returns (signal, inst_freq, t) — all 1-D arrays of length int(sr * duration_s)
```
All builder functions in this module already follow:
  - Return `(signal: float64, inst_freq: float64, t: float64)`
  - `signal` shape == `inst_freq` shape == `t` shape == `int(sr * duration_s)`
  - phase computed as `2*pi*np.cumsum(inst_freq)/sr` for FM-style signals.

From research/utilities/__init__.py lines 85-94 (where the builders are re-exported):
```
from .dsp_helpers import (
    BouncingChirpConfig,
    WaypointChirpConfig,
    build_chirp_chunks,
    build_wandering_chirp_chunks,
    build_fm_chirp_chunks,
    build_bouncing_chirp,
    build_bouncing_chirp_chunks,
    build_waypoint_chirp,
)
```
The two new builders must be added to this re-export tuple (and __all__ if present) so `from utilities.dsp_helpers import build_click_plus_tone` works the same way `build_waypoint_chirp` does in figure_1.py.

From research/utilities/plotting.py:
```
def compute_full_cwt(signal, sr, *, root_note_hz=..., num_octaves=...) -> (cwt_data, cwt_freqs, start_sample, end_sample)
```

From research/dsplot/style.py (constants the figure already uses — do NOT redefine):
```
PRIMARY_COLOR     = "#e1641a"   # orange
NEUTRAL_COLOR     = "#EEEEEE"
TICK_LABEL_COLOR  = "#888888"
INST_FREQ_COLOR     = PRIMARY_COLOR
INST_FREQ_LINEWIDTH = 2.4
INST_FREQ_ALPHA     = 0.9
```

From research/dsplot/figures/figure_1.py (current module-level — KEEP):
```
SR = 44100
DURATION_S = 2.36
WAYPOINTS = (...)   # locked v8 chirp — DO NOT TOUCH
DISPLAY_FREQ_LIM_HZ = (20.0, 21500.0)
DISPLAY_FREQ_TICKS = (200, 2000, 20000)
XTICKS = [0.0, 0.5, 1.0, 1.5, 2.0]

def _prepare() -> dict:                # locked — DO NOT TOUCH
def _stft_on_log_bins(signal, sr, log_freqs) -> np.ndarray:   # reuse as-is
def build_figure() -> Figure:          # locked v8 — DO NOT TOUCH
def render(output_dir=..., output_filename="figure_1_fourier_vs_wavelet.png") -> str:  # locked
```

Twin-axis pattern that the new figures must mirror (from build_figure lines 159-183):
```
row1 = TimeSeriesPanel(
    title=..., units=(3, 1),
    x_label=None, xticks=XTICKS,
    twin_y=True, twin_y_label="f (Hz)", twin_y_side="left",
    twin_yticks=twin_ytick_positions,
    twin_ytick_labels=twin_ytick_labels,
    twin_ylim=twin_ylim,
)
row1.add(TimeSeries(signal, SR, color=style.TICK_LABEL_COLOR))
row1.add_twin(Line(t_axis, inst_freq_bins, color=style.INST_FREQ_COLOR,
                   linewidth=style.INST_FREQ_LINEWIDTH, alpha=style.INST_FREQ_ALPHA))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build_click_plus_tone and build_low_vibrato signal builders</name>
  <files>research/utilities/dsp_helpers.py, research/utilities/__init__.py</files>
  <action>
Append two new builder functions to `research/utilities/dsp_helpers.py`, AFTER `build_waypoint_chirp` (around current line 329) and BEFORE `build_bouncing_chirp_chunks`. Follow the existing module conventions exactly: PEP 8, descriptive snake_case names, type hints with `np.ndarray`, return `(signal, inst_freq, t)` triple as `np.float64` arrays of length `int(sr * duration_s)`, docstring explaining intent (no inline comment litter — structure over docs per CLAUDE.md), and use `numpy as np` (already imported at module top).

Builder 1 — `build_click_plus_tone(sr, duration_s, tone_hz=60.0, click_t=0.7, click_center_hz=5000.0, click_duration_s=0.008, click_amp=0.7, tone_amp=1.0, click_seed=0)`:
  - Sustained sine tone at `tone_hz` for the full `duration_s` (use phase = `2*pi*tone_hz*t` for a clean constant-freq tone; do NOT use cumsum here since freq is constant).
  - One Gaussian-windowed band-limited noise burst centered at `click_t` seconds, with FWHM ≈ `click_duration_s`. Generate the burst by: (a) draw white Gaussian noise with `np.random.default_rng(click_seed)`, (b) bandpass to a narrow band around `click_center_hz` (use a simple FFT-domain rectangular bandpass of width ~2000 Hz, i.e. zero out FFT bins outside `[click_center_hz - 1000, click_center_hz + 1000]` then irfft), (c) multiply by a Gaussian window `exp(-((t - click_t)**2) / (2*sigma**2))` where `sigma = click_duration_s / 2.355` (FWHM→sigma), (d) normalize so peak abs amplitude == `click_amp`.
  - Combined signal = `tone_amp * sin(2*pi*tone_hz*t) + click_burst`.
  - `inst_freq`: return the analytical inst-freq curve as a `tone_hz`-constant array (length n). The click is broadband — there is no single inst freq for it, and the consumer needs a per-sample array. Per the deliverable spec, the inst-freq overlay should be a horizontal line at 60 Hz during the tone span and "no curve over the click (or a brief vertical mark)". A constant `tone_hz` array is the cleanest analytical truth here; the brief vertical mark vs no curve is a render-time decision (see Task 3, not this builder).
  - `t = np.arange(n, dtype=np.float64) / sr`.
  - Returns `(signal.astype(np.float64), inst_freq, t)`.

Builder 2 — `build_low_vibrato(sr, duration_s, carrier_hz=60.0, depth_hz=20.0, mod_hz=8.0, amp=1.0)`:
  - 60 Hz carrier with ±20 Hz sinusoidal vibrato at 8 Hz modulation rate.
  - `inst_freq(t) = carrier_hz + depth_hz * sin(2*pi*mod_hz*t)`.
  - `phase = 2*pi*np.cumsum(inst_freq) / sr` (mirrors `build_waypoint_chirp` line 326).
  - `signal = amp * np.sin(phase).astype(np.float64)`.
  - Returns `(signal, inst_freq, t)` with `t = np.arange(n, dtype=np.float64) / sr`.

Then update `research/utilities/__init__.py`:
  - Add `build_click_plus_tone` and `build_low_vibrato` to the `from .dsp_helpers import (...)` tuple (currently lines 85-94).
  - If a `__all__` list extends past those imports (starting line 99), append both new names there too.

Do NOT touch any other function in dsp_helpers.py. Do NOT modify imports beyond what's needed.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader &amp;&amp; source venv/bin/activate &amp;&amp; python -c "
from utilities.dsp_helpers import build_click_plus_tone, build_low_vibrato
import numpy as np
SR = 44100
s, f, t = build_click_plus_tone(SR, 2.0)
assert s.shape == (SR * 2,), f'hero signal shape {s.shape}'
assert f.shape == (SR * 2,), f'hero inst_freq shape {f.shape}'
assert t.shape == (SR * 2,), f'hero t shape {t.shape}'
assert s.dtype == np.float64
# click should make a localized burst near t=0.7s, not at t=0
click_idx = int(SR * 0.7)
local_peak = np.max(np.abs(s[click_idx - SR // 200 : click_idx + SR // 200]))
early_peak = np.max(np.abs(s[: SR // 10]))
assert local_peak &gt; early_peak * 1.1, f'no click localized at t=0.7s: local={local_peak} early={early_peak}'

s2, f2, t2 = build_low_vibrato(SR, 1.5)
assert s2.shape == (int(SR * 1.5),)
# inst_freq should swing between 40 and 80 Hz
assert 39 &lt; f2.min() &lt; 41, f'min {f2.min()}'
assert 79 &lt; f2.max() &lt; 81, f'max {f2.max()}'
print('OK')
"</automated>
  </verify>
  <done>Both builders return the expected (signal, inst_freq, t) triple with correct shapes/dtypes; click is localized at t≈0.7 s; anti-hero inst_freq sweeps 40-80 Hz. Both names importable via `from utilities.dsp_helpers import ...`.</done>
</task>

<task type="auto">
  <name>Task 2: Add _prepare_hero/_prepare_antihero data prep + render_hero/render_antihero render functions to figure_1.py</name>
  <files>research/dsplot/figures/figure_1.py</files>
  <action>
Extend `research/dsplot/figures/figure_1.py` WITHOUT modifying `_prepare`, `build_figure`, `render`, `show`, `_stft_on_log_bins`, or any module-level constant. Add new code below the existing `show()` function (end of file) or alongside `_prepare()` — pick whichever keeps the locked v8 pipeline visually grouped at the top.

Add the following module-level signal-spec constants (mirrors the style of the existing `SR`, `DURATION_S`, `WAYPOINTS` block — keep them grouped near each other):

```
# Hero — 60 Hz sustained tone + ~8 ms broadband click at t≈0.7 s.
HERO_DURATION_S = 2.0
HERO_TONE_HZ = 60.0
HERO_CLICK_T = 0.7
HERO_CLICK_CENTER_HZ = 5000.0
HERO_CLICK_DURATION_S = 0.008
HERO_CLICK_AMP = 0.7
HERO_TONE_AMP = 1.0

# Anti-hero — 60 Hz carrier with ±20 Hz wobble at 8 Hz.
ANTIHERO_DURATION_S = 1.5
ANTIHERO_CARRIER_HZ = 60.0
ANTIHERO_DEPTH_HZ = 20.0
ANTIHERO_MOD_HZ = 8.0
```

Update the top-of-file import line to add the two new builders:
`from utilities.dsp_helpers import build_waypoint_chirp, build_click_plus_tone, build_low_vibrato`

Then add `_prepare_hero()` and `_prepare_antihero()`, both modeled on `_prepare()` (lines 61-105). Each must:
  1. Call its signal builder (`build_click_plus_tone` / `build_low_vibrato`) with the module-level constants.
  2. Call `compute_full_cwt(signal, SR, root_note_hz=..., num_octaves=...)`. Use `root_note_hz=20.0` and `num_octaves` computed the same way as `_prepare()` (so cwt_freqs cover 20-21500 Hz). EXCEPTION: for `_prepare_antihero`, the anti-hero signal lives at 40-80 Hz; a 20-21500 Hz CWT bank dilutes the wobble visually. So also expose this knob: `_prepare_antihero` should additionally compute `root_note_hz=20.0` (matches v8 default) but the RENDER step (Task 3) can override the visible display range. Keep the CWT bank at the default 20-21500 Hz here — display narrowing happens via `tick_freqs` + dsplot's heatmap rendering, not by truncating the CWT data.
  3. Trim with `start_sample/end_sample` exactly like `_prepare()` lines 76-82.
  4. Apply the zero-crossing snap (lines 86-95) — same logic. For the hero signal: the click is impulsive and the resulting signal may not have clean zero crossings near boundaries; the snap pattern (find first/last sign change) should still work, but if `len(sign_changes) < 2` the snap is bypassed (already handled by the existing `if` guard — preserve it).
  5. Return the bundle dict shape: `{"signal", "inst_freq", "t", "duration_s", "cwt_data", "cwt_freqs"}` matching `_prepare()`.

Hero-specific detail in `_prepare_hero`:
  - After `build_click_plus_tone` returns, the `inst_freq` array is constant at `HERO_TONE_HZ` (per Task 1 builder design). Keep it as-is — the overlay rendering in Task 3 will decide whether to draw a single horizontal line or mask out the click region. Carry the constant array through the trim+snap pipeline like `_prepare()` does.

Anti-hero `_prepare_antihero` is the most direct mirror — wobbling inst_freq, no extra masking.

Then add `render_hero()` and `render_antihero()` functions. Each mirrors `build_figure()` + `render()` from lines 134-251, sharing the same 3-row structure, same `style.*` constants, same `spec_extent` pattern, same twin-axis setup on row 1. Differences:

For both:
  - Replace the data source with `_prepare_hero()` / `_prepare_antihero()`.
  - Recompute `stft_mag_log` via `_stft_on_log_bins(data["signal"], SR, data["cwt_freqs"])` — reuse the helper.
  - Recompute `t_axis`, `inst_freq_bins`, `twin_ytick_positions`, `twin_ytick_labels`, `twin_ylim`, `spec_extent` exactly the same way as `build_figure()`.

Hero-specific render choices:
  - Suptitle: `"Click + Tone — CWT Wins"` (descriptive — final wording is the user's call but plan it as a starting suggestion).
  - STFT `nperseg` selection: the deliverable wants a "compromise" window where one axis smears. The current `_stft_on_log_bins` hardcodes `nperseg = min(1024, max(64, 1 << int(math.log2(max(64, n // 4)))))`. For a 2.0 s, 44100 Hz signal, `n // 4 = 22050`, `log2(22050) ≈ 14.4`, so `nperseg = min(1024, 1 << 14) = 1024`. With `nperseg=1024` (~23 ms window) the click smears HORIZONTALLY in the STFT and the 60 Hz tone is also marginal (need ~735 samples per 60 Hz cycle; 1024 samples ≈ 1.4 cycles → poor freq localization too). This is exactly the "compromise window" the deliverable describes. Reuse `_stft_on_log_bins` as-is for the hero. If after rendering the smear story is unclear, leave a TODO comment for v2 iteration but do not refactor the helper now.
  - Row-1 inst-freq overlay: draw the horizontal Line at the constant 60 Hz position on the twin axis — same `Line(t_axis, inst_freq_bins, ...)` call as the locked figure. The constant inst_freq array → flat horizontal line at the 60 Hz bin position. This satisfies "horizontal line at 60 Hz for tone span; no curve over the click" by virtue of the analytical truth being constant. Do NOT add a separate vertical click mark in v1 — user can request that in v2 if desired.

Anti-hero-specific render choices:
  - Suptitle: `"Low Vibrato — STFT Wins"`.
  - STFT `nperseg`: long enough to resolve the 8 Hz modulation and ±20 Hz swing around 60 Hz. A 4096-sample window (≈ 93 ms, ~7-8 vibrato periods) tracks the wobble cleanly. Since `_stft_on_log_bins` hardcodes nperseg via the `n // 4` rule, for a 1.5 s signal (`n=66150`), `n // 4 = 16537`, `log2(16537) ≈ 14.01`, so it picks `nperseg = min(1024, 1 << 14) = 1024`. That's TOO SHORT to resolve a 20 Hz swing (~735 samples/cycle; 1024 samples ≈ 1.4 cycles). To fix this for anti-hero ONLY, do NOT modify `_stft_on_log_bins`. Instead, add an OPTIONAL `nperseg` kwarg to `_stft_on_log_bins` (default `None`, preserves current behavior). When `nperseg` is None, use the existing formula. When a value is passed, use it directly (after clamping to `min(nperseg, n)`). This is a minimal additive change that keeps the locked v8 render bit-exact. Call from anti-hero: `_stft_on_log_bins(data["signal"], SR, data["cwt_freqs"], nperseg=4096)`.
  - Row-1 inst-freq overlay: the wobbling `inst_freq_bins` curve naturally renders as a sinusoid in bin-space on the twin axis — same `Line(...)` call, no extra work.
  - Display range: deliverable allows narrowing to 20-500 Hz for anti-hero if it makes the wobble visible. The figure already uses `tick_freqs=DISPLAY_FREQ_TICKS = (200, 2000, 20000)` and a 20-21500 Hz `cwt_freqs` bank. Narrowing the display means changing `tick_freqs` AND visually cropping the heatmap rows to a lower bin range. Cropping requires reshaping `extent` and `cwt_data`/`stft_mag_log` slices, which is risky for v1. PROPOSAL FOR v1: keep the full 20-21500 Hz range and the (200, 2000, 20000) ticks identical to the hero/v8. The 40-80 Hz wobble will look small but the smear-vs-track story remains visible at the bottom of the panel. If user wants a tighter range in v2, we can add a `display_range` kwarg cleanly. Add a `# TODO(v2): tighter display range for anti-hero` comment near the row-3 setup so the iteration path is obvious.

Both `render_hero()` and `render_antihero()` should follow the exact same shape as the existing `render(output_dir=..., output_filename=...)` (lines 243-251) — build the figure, call `fig.render()`, ensure `output_dir` exists, `fig.savefig(...)`, return absolute path. Default filenames:
  - `render_hero(output_dir="assets/images/generated", output_filename="figure_1_hero_click_plus_tone_v1.png")`
  - `render_antihero(output_dir="assets/images/generated", output_filename="figure_1_antihero_low_vibrato_v1.png")`

Filenames must EXACTLY match the deliverable spec (per [[feedback-png-naming]] — never overwrite, use descriptive `_v1` suffix).

Finally, add `render_all()` at the bottom:
```
def render_all() -> tuple[str, str, str]:
    """Render the v8 chirp + hero + anti-hero into assets/images/generated/."""
    chirp_path = render()
    hero_path = render_hero()
    antihero_path = render_antihero()
    return chirp_path, hero_path, antihero_path
```

Do NOT add a `if __name__ == "__main__"` guard unless one already exists (it doesn't — the user runs via `python -m research.dsplot.figures.figure_1` style or inline). The runnable entrypoint is the function name itself.

CRITICAL DO-NOT-TOUCH list:
  - `_prepare()` (lines 61-105) — locked
  - `build_figure()` (lines 134-240) — locked
  - `render()` default kwargs (must still default to `figure_1_fourier_vs_wavelet.png` so any existing caller stays bit-exact)
  - `show()` (lines 254-260)
  - Module-level `WAYPOINTS`, `DURATION_S`, `DISPLAY_FREQ_LIM_HZ`, `DISPLAY_FREQ_TICKS`, `XTICKS`, `SR` constants
  - `research/dsplot/figures/motivator.py` (legacy reference)
  - Any file under `research/dsplot/panels/`, `research/dsplot/plottables/`, or `research/dsplot/style.py` — if a kwarg seems missing, surface it as an open question in the summary, do NOT modify framework code.

One framework-touching exception is explicitly allowed by this plan: adding the optional `nperseg` kwarg to `_stft_on_log_bins` in `figure_1.py` (NOT a dsplot framework file — it's a module-local helper in figure_1.py itself). Confirmed by re-reading lines 108-131 of figure_1.py.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader &amp;&amp; source venv/bin/activate &amp;&amp; python -c "
from research.dsplot.figures.figure_1 import (
    _prepare_hero, _prepare_antihero, render_hero, render_antihero, render_all,
)
hero = _prepare_hero()
ah = _prepare_antihero()
for name, d in [('hero', hero), ('antihero', ah)]:
    assert set(d.keys()) == {'signal','inst_freq','t','duration_s','cwt_data','cwt_freqs'}, (name, d.keys())
    assert d['signal'].ndim == 1
    assert d['cwt_data'].ndim == 2
    assert d['cwt_data'].shape[1] == d['signal'].shape[0], (name, d['cwt_data'].shape, d['signal'].shape)
print('OK')
"</automated>
  </verify>
  <done>`_prepare_hero` and `_prepare_antihero` return the same dict shape as `_prepare()`; `render_hero`, `render_antihero`, and `render_all` are importable. `_prepare()`, `build_figure()`, and `render()` default filename remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Render both PNGs via render_all() — produces v1 hero + anti-hero artifacts</name>
  <files>assets/images/generated/figure_1_hero_click_plus_tone_v1.png, assets/images/generated/figure_1_antihero_low_vibrato_v1.png</files>
  <action>
Execute the render entrypoint from a venv-activated shell to produce both new PNGs without re-rendering the locked v8 (the user does not want to regenerate the v8 chirp — the v8 PNG must remain byte-identical):

```
cd /home/eddie-water/dev/python/sub-shader
source venv/bin/activate
python -c "
from research.dsplot.figures.figure_1 import render_hero, render_antihero
hero = render_hero()
ah = render_antihero()
print('HERO:', hero)
print('ANTIHERO:', ah)
"
```

Note: do NOT call `render_all()` — that would re-render the v8 chirp into `figure_1_fourier_vs_wavelet.png` which is undesirable (the locked v8 PNG is `figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png`; the unversioned `figure_1_fourier_vs_wavelet.png` is an old "current" alias that may or may not match v8 — leave it untouched to avoid confusion). `render_all()` exists for future convenience; this task uses only the two new render functions.

Confirm both PNGs land at:
  - `assets/images/generated/figure_1_hero_click_plus_tone_v1.png`
  - `assets/images/generated/figure_1_antihero_low_vibrato_v1.png`

Confirm v8 PNG `assets/images/generated/figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png` is byte-identical to its pre-task state (use `git status` — file should not appear as modified).

Surface both PNG paths in the SUMMARY.md (absolute paths) for the user to view and approve. DO NOT `git add`, DO NOT `git commit`, DO NOT merge anything. Per [[feedback-no-unverified-merges]] and the explicit "DO NOT auto-commit" constraint, the user will visually verify the PNGs and decide next steps (iterate to v2, accept, or revert).
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader &amp;&amp; \
      test -f assets/images/generated/figure_1_hero_click_plus_tone_v1.png &amp;&amp; \
      test -f assets/images/generated/figure_1_antihero_low_vibrato_v1.png &amp;&amp; \
      [ -z "$(git status --porcelain assets/images/generated/figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png)" ] &amp;&amp; \
      echo "OK: both v1 PNGs exist, v8 untouched"</automated>
  </verify>
  <done>Both v1 PNGs exist on disk at the spec'd paths. The v8 PNG appears clean (no modifications) in `git status`. No `git add`/`git commit` ran. SUMMARY.md surfaces both absolute PNG paths for user review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User visually verifies hero + anti-hero PNGs</name>
  <what-built>
Two new figures rendered via the dsplot framework, reusing the locked v8 visual chrome (orange #e1641a inst-freq overlay, gray row-1 fill, log-spaced freq grid, twin-axis Hz labels on row 1) on new signal designs:
  - HERO: 60 Hz sustained tone + ~8 ms broadband click at t≈0.7 s → CWT should show a clean horizontal 60 Hz band + a vertical click streak in upper bins; STFT smears.
  - ANTI-HERO: 60 Hz carrier with ±20 Hz wobble at 8 Hz (1.5 s) using a longer STFT nperseg=4096 → STFT should track the wobble; CWT should smear visibly.
  </what-built>
  <how-to-verify>
1. Open both PNGs:
   - `/home/eddie-water/dev/python/sub-shader/assets/images/generated/figure_1_hero_click_plus_tone_v1.png`
   - `/home/eddie-water/dev/python/sub-shader/assets/images/generated/figure_1_antihero_low_vibrato_v1.png`
2. Confirm the visual chrome matches v8 (compare to `figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png`):
   - Same 3-row layout, same panel sizes/margins/gutters.
   - Same orange #e1641a inst-freq curve color/weight on all 3 rows.
   - Same gray row-1 fill.
   - Same 200/2k/20k twin-axis labels on row 1.
3. Confirm the SIGNAL STORY for each:
   - Hero CWT: horizontal 60 Hz band + vertical click streak at t≈0.7 s, upper bins.
   - Hero STFT: visibly smeared (either click smears horizontally OR 60 Hz tone smears vertically — ideally both at nperseg=1024).
   - Anti-hero CWT: wobble in the 40-80 Hz region looks smeared (Morlet time support exceeds modulation period).
   - Anti-hero STFT: wobble visible as a sinusoidal trace in the 40-80 Hz band.
   - Orange inst-freq overlay: hero shows a flat horizontal line at 60 Hz; anti-hero shows a sinusoidal curve oscillating between 40 and 80 Hz.
4. Confirm the v8 chirp PNG is untouched: `git status` should show no modification to `figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png`.
5. Decide:
   - Approve → user owns the commit ([[feedback-no-unverified-merges]]).
   - Iterate → user requests a v2 with specific changes (tighter freq range for anti-hero, vertical click mark for hero, different nperseg, etc.).
   - Reject → flag what's broken and we replan.
  </how-to-verify>
  <resume-signal>Type "approved" / "iterate" / "reject" with notes.</resume-signal>
</task>

</tasks>

<verification>
Plan-level checks beyond per-task verify:

1. The locked v8 chirp pipeline is untouched:
   `git diff research/dsplot/figures/figure_1.py | grep -E '^[-+].*_prepare\(|build_figure\(|^def render\(|WAYPOINTS|DURATION_S = 2.36'`
   should return zero matches that delete or modify those tokens (only additions of NEW symbols are allowed).

2. dsplot framework files are untouched:
   `git status research/dsplot/panels research/dsplot/plottables research/dsplot/style.py`
   should report no modifications.

3. Legacy motivator is untouched:
   `git status research/dsplot/figures/motivator.py` → no modifications.

4. v8 PNG is untouched:
   `git status assets/images/generated/figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png` → no modifications.

5. Both new PNGs exist at the spec'd paths.

6. No commits were made by Claude — `git log --oneline -5` shows the same HEAD as plan start.
</verification>

<success_criteria>
- `build_click_plus_tone` and `build_low_vibrato` exist in `research/utilities/dsp_helpers.py` and are re-exported through `research/utilities/__init__.py`.
- `_prepare_hero`, `_prepare_antihero`, `render_hero`, `render_antihero`, `render_all` exist in `research/dsplot/figures/figure_1.py`.
- `_prepare`, `build_figure`, `render`, `show`, and all module-level v8 constants in `figure_1.py` are bit-identical to their pre-task state (only ADDITIONS).
- `research/dsplot/figures/motivator.py` and all dsplot framework files are unchanged.
- Both new PNGs exist at:
  - `assets/images/generated/figure_1_hero_click_plus_tone_v1.png`
  - `assets/images/generated/figure_1_antihero_low_vibrato_v1.png`
- v8 PNG `assets/images/generated/figure_1_fourier_vs_wavelet_v8_inst_freq_on_all_rows.png` is untouched.
- Checkpoint completed: user has visually verified both PNGs and signaled approval / iteration / rejection.
- No `git add` / `git commit` invoked by Claude during the task.
</success_criteria>

<open_questions>
Surface in SUMMARY.md, do NOT block on these — they are v2 iteration candidates:

1. Hero: should the inst-freq overlay add a brief vertical mark at t≈0.7 s to indicate "click is here, no inst-freq defined"? v1 omits this (constant 60 Hz line through the click region); easy v2 addition.
2. Anti-hero: should the display range narrow to 20-500 Hz to make the 40-80 Hz wobble more prominent? v1 keeps the full 20-21500 Hz range; tightening requires a `display_range` kwarg on the heatmap or a manual `extent` + data slice — straightforward v2 task.
3. Hero STFT: does nperseg=1024 produce the desired "compromise smear"? If the 60 Hz tone resolves too cleanly (or too poorly), a different nperseg (e.g. 512 or 2048) can be passed via the new `_stft_on_log_bins(..., nperseg=...)` knob.
4. Anti-hero suptitle vs hero suptitle: planner suggested `"Click + Tone — CWT Wins"` and `"Low Vibrato — STFT Wins"` as starting placeholders. User authors final prose per CLAUDE.md voice rule.
</open_questions>

<output>
Write SUMMARY at `.planning/quick/260519-q34-regenerate-figure-1-as-a-hero-click-sust/260519-q34-SUMMARY.md` listing:
- Both new PNG absolute paths.
- Confirmation that v8 PNG and locked code are untouched.
- The four open questions above as v2 iteration candidates.
- NO commit, NO push — user owns the next step.
</output>
