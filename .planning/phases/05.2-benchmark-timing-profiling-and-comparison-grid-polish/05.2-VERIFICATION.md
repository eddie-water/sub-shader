---
phase: 05.2-benchmark-timing-profiling-and-comparison-grid-polish
verified: 2026-03-25T00:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 05.2: Benchmark Timing Profiling and Comparison Grid Polish — Verification Report

**Phase Goal:** Add cwt_timed() sub-stage profiling to --timing output, add NumPy timing to --comparison table, and commit existing grid visual improvements
**Verified:** 2026-03-25
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | cwt_timed() returns identical CWT output to cwt() plus a dict of 6 stage timings | VERIFIED | `test_cwt_timed_output_matches_cwt` passes (rtol=1e-10); method at wavelet.py:179; keys confirmed in test at test_wavelet.py:436 |
| 2 | --timing shows per-stage breakdown (6 CWT sub-stages + get_chunk + push_frame) instead of a single cwt() row | VERIFIED | timing.py:90-131 shows 8-method accumulator, cwt_stages list defined at line 90, push_frame timed at line 110, cwt() total row at line 125 |
| 3 | PyWavelet normalize_by_scale divides by sqrt(scale), making energy comparable to SubShader | VERIFIED | wavelet.py:419 — `return cwt_coefs / np.sqrt(self.scales)[:, np.newaxis]` with docstring explaining L1/sqrt(scale) equivalence |
| 4 | Comparison grid uses GRID_BG_COLOR #1A1A1A and gridspec label column layout | VERIFIED | figures.py:624 defines `GRID_BG_COLOR = "#1A1A1A"`; gridspec used at figures.py:837 |
| 5 | test_normalization_comparability passes, verifying PyWavelet/SubShader energy parity | VERIFIED | test runs and passes (3 tests selected by `-k "timed or normalization_comparability"` — all 3 PASSED in 1.24s) |
| 6 | --comparison timing table shows SubShader (CPU) row using NumPyWavelet | VERIFIED | figures.py:721 sets `"SubShader (CPU)"` in methods list; figures.py:755 times `npwt.cwt` into `acc["SubShader (CPU)"]` |
| 7 | --comparison timing table shows SubShader (GPU) row using CuPyWavelet when GPU available | VERIFIED | figures.py:724-725 conditionally appends `"SubShader (GPU)"`; figures.py:685-687 instantiates CuWavelet; figures.py:760-761 times `cpwt.cwt` |
| 8 | --stub-pywt skips PyWavelet but still times both SubShader (CPU) and SubShader (GPU) | VERIFIED | figures.py:721-725 — `"SubShader (CPU)"` is the base methods list entry; `"PyWavelet"` is only inserted when `not stub_pywt`; GPU path is independent |
| 9 | No new figure row is added for NumPy — timing table only | VERIFIED | figures.py:760-761 — GPU result is discarded (`_`); no corresponding `column_data` entry added for either NumPy or GPU timing |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/subshader/dsp/wavelet.py` | cwt_timed() method on Wavelet base class | VERIFIED | `def cwt_timed` at line 179; `import time` at line 21; no imports from research/ |
| `research/timing.py` | TimedSubShader with sub-stage breakdown and render timing | VERIFIED | 8-method accumulator (lines 90-92); CircularFrameBuffer at lines 94-98; cwt_timed at line 107; push_frame at line 110 |
| `src/subshader/dsp/test_wavelet.py` | Tests for cwt_timed and normalization comparability | VERIFIED | `test_cwt_timed_returns_six_stages` at line 428; `test_cwt_timed_output_matches_cwt` at line 443; `test_normalization_comparability` at line 318 |
| `research/figures.py` | 4-method comparison timing (STFT, PyWavelet, SubShader CPU, SubShader GPU) | VERIFIED | `"SubShader (CPU)"` at lines 721/755; `"SubShader (GPU)"` at lines 725/761; `GPU_COMP_AVAILABLE` at line 612; `CuWavelet` at lines 686-687 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/subshader/dsp/wavelet.py` | `research/timing.py` | TimedSubShader calls wavelet.cwt_timed() | WIRED | timing.py:107 — `result, stage_times = wavelet.cwt_timed(audio_data)` |
| `research/timing.py` | `src/subshader/viz/plotter.py` | CircularFrameBuffer for render timing | WIRED | timing.py:4 imports CircularFrameBuffer; lines 94-98 instantiate it; line 110 calls `buf.push_frame(result)` |
| `research/figures.py` | `src/subshader/dsp/wavelet.py` | Instantiates both NumPyWavelet and CuPyWavelet for comparison timing | WIRED | figures.py:686 — `from subshader.dsp.wavelet import CuWavelet`; figures.py:755/761 both timing paths active |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `research/timing.py` | `stage_times` | `wavelet.cwt_timed(audio_data)` | Yes — real per-stage perf_counter measurements | FLOWING |
| `research/timing.py` | `acc["push_frame()"][i]` | `time_call(buf.push_frame, result)` | Yes — real CircularFrameBuffer push timing | FLOWING |
| `research/figures.py` | `acc["SubShader (CPU)"][acc.current_idx]` | `time_call(npwt.cwt, chunk)` | Yes — real NumPyWavelet CWT timing | FLOWING |
| `research/figures.py` | `acc["SubShader (GPU)"][acc.current_idx]` | `time_call(cpwt.cwt, chunk)` when GPU available | Yes — real CuWavelet CWT timing (guarded by `cpwt is not None`) | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| cwt_timed 6-stage keys test passes | `pytest src/subshader/dsp/test_wavelet.py -k "timed" -v` | 2 passed | PASS |
| normalization comparability test passes | `pytest src/subshader/dsp/test_wavelet.py -k "normalization_comparability" -v` | 1 passed | PASS |
| Full test suite clean | `python -m pytest src/ -q` | 15 passed in 6.63s | PASS |
| TimedSubShader import succeeds | `python -c "from timing import TimedSubShader; print('Import OK')"` (from research/) | Import OK | PASS |
| Old 2-method accumulator removed | `grep 'TimingAccumulator.*"cwt()"' research/timing.py` | no match | PASS |
| Old bare "SubShader" label removed | `grep 'acc\["SubShader"\]' research/figures.py` | no match | PASS |

---

### Requirements Coverage

The plans for this phase declare requirements `TBD-01`, `TBD-02`, `TBD-03`, `TBD-04`. These IDs do not appear anywhere in `.planning/REQUIREMENTS.md` — the file contains no TBD-prefixed entries and has no traceability row mapping any requirement to Phase 5.2.

This is a coverage gap in the requirements document, not a gap in implementation. The phase delivered all planned functionality. The requirement IDs were placeholders assigned during planning and were never promoted to formal entries in REQUIREMENTS.md.

| Requirement | Source Plan | Description (from VALIDATION.md) | Status | Evidence |
|-------------|-------------|-----------------------------------|--------|---------|
| TBD-01 | 05.2-01 | cwt_timed() on Wavelet base class with 6 stage keys | SATISFIED | wavelet.py:179; test passes |
| TBD-02 | 05.2-01 | TimedSubShader 8-row timing breakdown | SATISFIED | timing.py:90-131; import succeeds |
| TBD-03 | 05.2-02 | --comparison table SubShader (CPU) row | SATISFIED | figures.py:721/755 |
| TBD-04 | 05.2-01 | test_normalization_comparability passes | SATISFIED | test_wavelet.py:318; test passes |

**Orphaned requirements:** None — no additional requirement IDs are mapped to Phase 5.2 in REQUIREMENTS.md. The TBD-0X IDs are local to the phase planning artifacts.

**Note for future cleanup:** TBD-01 through TBD-04 should be promoted to named requirements in REQUIREMENTS.md (e.g., under a new "Benchmarking" or "Research Toolkit" section) if the project's coverage accounting is to remain accurate.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/subshader/dsp/wavelet.py` | 422 | `# stub` comment on `discard_unreliable_coefs` in PyWavelet | Info | Pre-existing; PyWavelet stub was in place before this phase and is noted in the test suite |

No new stubs introduced by this phase. The `# stub` at line 422 is pre-existing (PyWavelet's edge-trim is intentionally a no-op because pywt.cwt handles edge effects differently). It does not affect the timing or comparison logic added in this phase.

---

### Human Verification Required

#### 1. --comparison output format with 4-method table

**Test:** Run `python benchmark.py --comparison-grid --comparison` on a machine with GPU available
**Expected:** Timing table prints 4 rows: STFT, PyWavelet, SubShader (CPU), SubShader (GPU) — in that order
**Why human:** The printing section iterates `acc.methods` dynamically. The code paths are verified correct, but the actual terminal output format (column alignment, separator placement, method label width) cannot be confirmed without running the GPU-enabled pipeline end-to-end.

#### 2. --timing 8-row sub-stage output format

**Test:** Run `python benchmark.py --timing`
**Expected:** Output shows get_chunk(), separator, 6 indented sub-stage rows (raw_cwt, normalize, magnitude, edge_trim, hop_center, downsample), cwt() total row, separator, push_frame() row, then loop summary
**Why human:** Functional wiring is verified. Visual formatting (indentation, separator placement, column widths) requires a live run to confirm readability.

---

### Gaps Summary

No gaps. All 9 observable truths are verified. All 4 artifacts pass all three substantive levels (exists, substantive, wired) and Level 4 data-flow trace. Key links are wired in both directions. Tests pass at 15/15. The TBD requirement IDs are not tracked in REQUIREMENTS.md, but this is a documentation bookkeeping issue — not a functional gap in what was built.

---

_Verified: 2026-03-25_
_Verifier: Claude (gsd-verifier)_
