# Phase 5.2: Benchmark Timing Profiling and Comparison Grid Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-25
**Phase:** 05.2-benchmark-timing-profiling-and-comparison-grid-polish
**Areas discussed:** --timing sub-stage profiling, --comparison NumPy timing, Grid visual polish scope

---

## --timing Sub-Stage Profiling

| Option | Description | Selected |
|--------|-------------|----------|
| cwt_timed() on Wavelet | Method on base class, returns (result, timings_dict). Works across all backends. | ✓ |
| Inline in TimedSubShader | Timing logic in research code, production untouched. Same drift risk, harder to spot. | |
| Decorator/context manager | Zero drift but adds complexity to Wavelet class. Overkill for 6 calls. | |

**User's choice:** cwt_timed() on Wavelet
**Notes:** User asked for implications of each choice before selecting. Key factor: drift visibility — having cwt_timed() next to cwt() in the same file makes pipeline changes visible.

---

## --comparison NumPy Timing

| Option | Description | Selected |
|--------|-------------|----------|
| Extra row in timing table | 4th timed method alongside STFT, PyWavelet, SubShader. No figure changes. | ✓ |
| Separate timing summary | NumPy timing in its own section after comparison table. | |
| Add NumPy row to figure | Both timing AND 4th spectrogram row. Makes grid 6x3 and slower. | |

**User's choice:** Extra row in timing table
**Notes:** User corrected terminology (rows not columns in table) and specified labels: "SubShader (CPU)" for NumPyWavelet, "SubShader (GPU)" for CuPyWavelet.

---

## Grid Visual Polish Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Lock what's done | Commit existing changes, no further grid work. | ✓ |
| Include minor tweaks | Allow small adjustments discovered during timing work. | |
| Full grid polish pass | Dedicated visual polish with axis styling, fonts, etc. | |

**User's choice:** Lock what's done
**Notes:** Existing work (normalization, labels, bg color, DAW ticks, Freq label placement, comparability test) is committed as-is. Axis styling, DPI, decorators deferred.

---

## Claude's Discretion

- Timing table formatting for sub-stage breakdown
- Total row in sub-stage timing
- Render timing graceful skip when no GPU/display

## Deferred Ideas

- Axis label/title styling — future phase
- Full grid polish pass — future phase
- Decorator utilities — future phase
