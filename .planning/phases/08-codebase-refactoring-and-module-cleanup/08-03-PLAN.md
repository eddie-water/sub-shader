---
phase: 08-codebase-refactoring-and-module-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/subshader/renderer/__init__.py
  - src/subshader/renderer/renderer.py
  - src/subshader/renderer/frame_buffer.py
  - src/subshader/renderer/intensity.py
  - src/subshader/renderer/shaders/vertex.glsl
  - src/subshader/renderer/shaders/fragment.glsl
autonomous: true
requirements:
  - D-15
  - D-16
  - D-17

must_haves:
  truths:
    - "renderer/ directory exists where viz/ was"
    - "Renderer class (renamed from ShaderPlot) handles OpenGL context + shader rendering"
    - "CircularFrameBuffer and AudioFrameBuffer live in frame_buffer.py"
    - "IntensityTracker lives in intensity.py"
    - "Shader files use short names: vertex.glsl, fragment.glsl"
    - "viz/ directory and its files still exist (not deleted until Plan 05 switchover)"
  artifacts:
    - path: "src/subshader/renderer/renderer.py"
      provides: "Renderer class (was ShaderPlot) + GLContext"
      contains: "class Renderer"
    - path: "src/subshader/renderer/frame_buffer.py"
      provides: "CircularFrameBuffer + AudioFrameBuffer"
      contains: "class CircularFrameBuffer"
    - path: "src/subshader/renderer/intensity.py"
      provides: "IntensityTracker"
      contains: "class IntensityTracker"
    - path: "src/subshader/renderer/shaders/vertex.glsl"
      provides: "Vertex shader source"
    - path: "src/subshader/renderer/shaders/fragment.glsl"
      provides: "Fragment shader source"
  key_links:
    - from: "src/subshader/renderer/renderer.py"
      to: "src/subshader/renderer/frame_buffer.py"
      via: "import CircularFrameBuffer"
      pattern: "from .frame_buffer import"
    - from: "src/subshader/renderer/renderer.py"
      to: "src/subshader/renderer/intensity.py"
      via: "import IntensityTracker"
      pattern: "from .intensity import"
---

<objective>
Split the 800-line plotter.py into three focused files under a new renderer/ directory, rename ShaderPlot to Renderer, and rename shader files to shorter names.

Purpose: plotter.py has 7 classes spanning GL context management, frame buffering, intensity tracking, and shader rendering. One concern per file makes each independently readable.
Output: renderer/ directory with renderer.py, frame_buffer.py, intensity.py, and renamed shader files.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-CONTEXT.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/orchestrator-design.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create renderer/ directory and split plotter.py into three files</name>
  <files>
    src/subshader/renderer/__init__.py
    src/subshader/renderer/renderer.py
    src/subshader/renderer/frame_buffer.py
    src/subshader/renderer/intensity.py
    src/subshader/renderer/shaders/vertex.glsl
    src/subshader/renderer/shaders/fragment.glsl
  </files>
  <read_first>
    - src/subshader/viz/plotter.py (full 799 lines — primary source)
    - src/subshader/viz/plot_normalizer.py (IntensityTracker — moves to intensity.py)
    - src/subshader/viz/__init__.py (current exports — will NOT be modified here)
    - src/subshader/viz/shaders/__init__.py (shader loading pattern)
    - src/subshader/viz/shaders/vertex_shader.glsl
    - src/subshader/viz/shaders/fragment_shader.glsl
    - src/subshader/config.py (for new RendererConfig import)
  </read_first>
  <action>
    Per D-15, D-16, D-17:

    1. Create `src/subshader/renderer/` directory structure:
       ```
       mkdir -p src/subshader/renderer/shaders
       ```

    2. Create `src/subshader/renderer/frame_buffer.py`:
       - Extract `CircularFrameBuffer` class from plotter.py (the class that manages the circular array of CWT frames)
       - Extract `AudioFrameBuffer` class from plotter.py (if it exists as separate class; it may be part of CircularFrameBuffer)
       - Keep the same logic, just move to own file
       - Imports: numpy, logging

    3. Create `src/subshader/renderer/intensity.py`:
       - Copy `IntensityTracker` class from `viz/plot_normalizer.py`
       - This is 61 lines — take the whole class verbatim
       - Update import: `from ..config import ColorNormalizationConfig` (or PipelineConfig if that's what it uses)

    4. Create `src/subshader/renderer/renderer.py`:
       - Extract `GLContext` class from plotter.py (the OpenGL context manager)
       - Extract `ShaderPlot` class from plotter.py and RENAME it to `Renderer`
       - The Renderer class should:
         - Take `config` (RendererConfig or PipelineConfig) and `frame_shape` in __init__
         - Create GLContext internally
         - Create CircularFrameBuffer internally (import from .frame_buffer)
         - Create IntensityTracker internally (import from .intensity)
         - Expose: `update(coefs)`, `should_close() -> bool`, `cleanup()`
         - Apply `@timed` decorator on `update()` method (per D-21)
       - Load shaders using `Path(__file__).parent / "shaders"` pattern
       - Do NOT include the deprecated PyQtGraph plotter class — leave it in viz/plotter.py
       - Do NOT include the abstract `Plotter` base class — Renderer stands alone

    5. Copy shader files with new names:
       - Copy `src/subshader/viz/shaders/vertex_shader.glsl` to `src/subshader/renderer/shaders/vertex.glsl`
       - Copy `src/subshader/viz/shaders/fragment_shader.glsl` to `src/subshader/renderer/shaders/fragment.glsl`
       - No `__init__.py` in shaders/ — load via Path (per D-17)

    6. Create `src/subshader/renderer/__init__.py`:
       ```python
       """Renderer module — OpenGL-based real-time visualization."""

       from .renderer import Renderer

       # DEPRECATED aliases — remove after all callers migrated
       ShaderPlot = Renderer

       __all__ = ["Renderer", "ShaderPlot"]
       ```

    IMPORTANT: Do NOT delete or modify viz/ directory. Leave it in place — Plan 05 handles the switchover.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -c "
from subshader.renderer import Renderer, ShaderPlot
from subshader.renderer.frame_buffer import CircularFrameBuffer
from subshader.renderer.intensity import IntensityTracker
assert ShaderPlot is Renderer
print('Renderer module OK')
" && test -f src/subshader/renderer/shaders/vertex.glsl && test -f src/subshader/renderer/shaders/fragment.glsl && echo "Shaders OK"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/subshader/renderer/` exists
    - `src/subshader/renderer/renderer.py` contains `class Renderer` (not `class ShaderPlot`)
    - `src/subshader/renderer/renderer.py` contains `class GLContext`
    - `src/subshader/renderer/renderer.py` contains `from .frame_buffer import`
    - `src/subshader/renderer/renderer.py` contains `from .intensity import`
    - `src/subshader/renderer/frame_buffer.py` contains `class CircularFrameBuffer`
    - `src/subshader/renderer/intensity.py` contains `class IntensityTracker`
    - `src/subshader/renderer/shaders/vertex.glsl` exists (not `vertex_shader.glsl`)
    - `src/subshader/renderer/shaders/fragment.glsl` exists (not `fragment_shader.glsl`)
    - No `src/subshader/renderer/shaders/__init__.py` file
    - `src/subshader/renderer/__init__.py` contains `ShaderPlot = Renderer`
    - `python -c "from subshader.renderer import Renderer"` exits 0
    - `src/subshader/viz/plotter.py` still exists (not deleted)
  </acceptance_criteria>
  <done>Renderer module created with three focused files. ShaderPlot renamed to Renderer. Shader files use short names. GLContext, CircularFrameBuffer, IntensityTracker each in own file. viz/ preserved for backward compat.</done>
</task>

</tasks>

<verification>
- `python -c "from subshader.renderer import Renderer"` exits 0
- `python -c "from subshader.renderer.frame_buffer import CircularFrameBuffer"` exits 0
- `python -c "from subshader.renderer.intensity import IntensityTracker"` exits 0
- `python -c "from subshader.viz.plotter import ShaderPlot"` still exits 0 (old file preserved)
- `test -f src/subshader/renderer/shaders/vertex.glsl`
</verification>

<success_criteria>
Renderer module exists as three files (renderer.py, frame_buffer.py, intensity.py) with clean imports between them. Shader files renamed. Old viz/ directory preserved. Both old and new import paths work.
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-refactoring-and-module-cleanup/08-03-SUMMARY.md`
</output>
