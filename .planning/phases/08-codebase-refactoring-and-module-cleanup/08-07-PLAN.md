---
phase: 08-codebase-refactoring-and-module-cleanup
plan: 07
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - research/archive/comparison_navigator.py
  - research/archive/WaveletDesign.md
  - research/archive/frame_counter_pyqt5.py
  - research/archive/gl_diagnostics.py
  - research/archive/quick_plot.py
  - research/archive/pipeline_timing_profile.py
  - research/archive/benchmark_timing_template.txt
  - research/archive/benchmark_results.csv
  - research/archive/branch_goals.md
  - research/archive/GPU_BUFFER_IDEA.md
  - assets/archive/audio/
  - assets/archive/images/
  - assets/archive/claude/
  - assets/archive/diagnostics/
  - README.md
  - docs/DSP.md
  - docs/AUDIO.md
autonomous: true
requirements:
  - D-36
  - D-37
  - D-38

must_haves:
  truths:
    - "All unused source files archived to research/archive/"
    - "All unused asset files archived to assets/archive/"
    - "README.md image/audio paths updated to new asset locations"
    - "DSP.md and AUDIO.md paths updated to new locations"
    - "No broken asset references in documentation"
    - "viz/ directory fully removed (no stray files, no __pycache__)"
  artifacts:
    - path: "research/archive/"
      provides: "Archived research files"
    - path: "assets/archive/"
      provides: "Archived asset files"
  key_links:
    - from: "README.md"
      to: "assets/images/generated/"
      via: "image references"
      pattern: "assets/images/generated/"
---

<objective>
Archive all unused source and asset files (nothing deleted), clean up the old viz/ directory, and update documentation paths to reflect the new asset structure.

Purpose: Clean up the repository without losing any historical files. Documentation must reference the new asset locations.
Output: research/archive/ and assets/archive/ populated, viz/ removed, documentation paths updated.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-CONTEXT.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive unused source files to research/archive/</name>
  <files>
    research/archive/WaveletDesign.md
    research/archive/frame_counter_pyqt5.py
    research/archive/gl_diagnostics.py
    research/archive/quick_plot.py
    research/archive/pipeline_timing_profile.py
    research/archive/benchmark_timing_template.txt
    research/archive/benchmark_results.csv
    research/archive/branch_goals.md
    research/archive/font_showcase_20.py
  </files>
  <read_first>
    - src/subshader/utils/ (list directory to identify files to archive: frame_counter_pyqt5.py, gl_diagnostics.py, quick_plot.py, signal_generator.py)
    - src/subshader/dsp/ (check for WaveletDesign.md)
    - research/ (check for pipeline_timing_profile.py, benchmark_results.csv, branch_goals.md)
  </read_first>
  <action>
    Per D-37:

    1. Create archive directory:
       ```bash
       mkdir -p research/archive
       ```

    2. Archive unused source files using `git mv`:
       - `git mv src/subshader/dsp/WaveletDesign.md research/archive/WaveletDesign.md`
       - `git mv src/subshader/utils/frame_counter_pyqt5.py research/archive/frame_counter_pyqt5.py`
       - `git mv src/subshader/utils/gl_diagnostics.py research/archive/gl_diagnostics.py`
       - `git mv src/subshader/utils/quick_plot.py research/archive/quick_plot.py`
       - `git mv research/pipeline_timing_profile.py research/archive/pipeline_timing_profile.py`
       - `git mv research/utilities/font_showcase_20.py research/archive/font_showcase_20.py` (Claude's discretion: archive, not delete)
       - Check for benchmark_timing_template.txt, benchmark_results.csv, branch_goals.md in research/ and archive them if they exist

    3. Clean up the old `src/subshader/viz/` directory (Plan 05 already deleted the main files and archived comparison_navigator.py + GPU_BUFFER_IDEA.md):
       - First verify nothing tracked remains: `git status src/subshader/viz/`
       - If any tracked files remain, `git rm` them
       - Remove only __pycache__ and .pyc files: `find src/subshader/viz/ -name "__pycache__" -type d -exec rm -rf {} + 2>/dev/null; find src/subshader/viz/ -name "*.pyc" -delete 2>/dev/null`
       - Then remove empty directory tree: `rmdir src/subshader/viz/shaders/ 2>/dev/null; rmdir src/subshader/viz/ 2>/dev/null`
       - If rmdir fails (something still there), run `ls -la src/subshader/viz/` to diagnose and handle remaining files
       - Final verification: `! test -d src/subshader/viz` must be true

    IMPORTANT: Use `git mv` for all moves so git tracks the history. Only use `rm -rf` on __pycache__ directories and .pyc files.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test -d research/archive && test -f research/archive/pipeline_timing_profile.py && ! test -d src/subshader/viz && echo "Source archive and viz cleanup OK"</automated>
  </verify>
  <acceptance_criteria>
    - `research/archive/` directory exists with at least: WaveletDesign.md, frame_counter_pyqt5.py, gl_diagnostics.py, quick_plot.py, pipeline_timing_profile.py
    - `src/subshader/viz/` directory does not exist (confirmed by `! test -d src/subshader/viz`)
    - `git status src/subshader/viz/` shows no remaining entries
    - No files were deleted (only moved via git mv, except __pycache__/.pyc which are not tracked)
  </acceptance_criteria>
  <done>All unused source files archived to research/archive/. viz/ directory fully removed including __pycache__.</done>
</task>

<task type="auto">
  <name>Task 2: Archive unused asset files to assets/archive/</name>
  <files>
    assets/archive/audio/
    assets/archive/images/
    assets/archive/claude/
    assets/archive/diagnostics/
  </files>
  <read_first>
    - assets/images/claude/ (check for archived images)
    - assets/images/diagnostics/ (check for archived images)
    - assets/audio/daw/ (check for any remaining files not moved in Plan 01)
    - assets/audio/songs/ (check for any remaining files not moved in Plan 01)
  </read_first>
  <action>
    Per D-38, D-36:

    1. Create archive directories:
       ```bash
       mkdir -p assets/archive/audio assets/archive/images assets/archive/claude assets/archive/diagnostics
       ```

    2. Archive unused asset files using `git mv`:
       - `git mv assets/images/claude/ assets/archive/claude/` (entire directory if it has tracked files)
       - `git mv assets/images/diagnostics/ assets/archive/diagnostics/` (entire directory if it has tracked files)
       - Check for any remaining files in `assets/audio/daw/` and `assets/audio/songs/` — if they weren't moved in Plan 01, move unused ones to `assets/archive/audio/`
       - Move old/unused image files in `assets/images/` (not in reference/ or generated/) to `assets/archive/images/`

    3. Clean up empty directories:
       ```bash
       rmdir assets/audio/daw/ 2>/dev/null || true
       rmdir assets/audio/songs/ 2>/dev/null || true
       rmdir assets/images/benchmarks/ 2>/dev/null || true
       rmdir assets/images/claude/ 2>/dev/null || true
       rmdir assets/images/diagnostics/ 2>/dev/null || true
       ```

    IMPORTANT: Use `git mv` for all moves so git tracks the history.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test -d assets/archive && ! test -d assets/audio/daw && ! test -d assets/audio/songs && echo "Asset archive OK"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/archive/` directory exists
    - `assets/audio/daw/` directory does not exist (empty dirs removed)
    - `assets/audio/songs/` directory does not exist (empty dirs removed)
    - No files were deleted (only moved via git mv)
  </acceptance_criteria>
  <done>All unused asset files archived to assets/archive/. Empty directories cleaned up.</done>
</task>

<task type="auto">
  <name>Task 3: Update documentation asset paths</name>
  <files>
    README.md
    docs/DSP.md
    docs/AUDIO.md
  </files>
  <read_first>
    - README.md (search for assets/ references)
    - docs/DSP.md (if exists — search for assets/ references)
    - docs/AUDIO.md (if exists — search for assets/ references)
    - assets/audio/README.md (if exists — search for old path references)
  </read_first>
  <action>
    Update all asset path references in documentation to match the new structure:

    **Path mapping:**
    | Old Path | New Path |
    |----------|----------|
    | `assets/images/benchmarks/comparison_grid.png` | `assets/images/generated/comparison_grid.png` |
    | `assets/images/benchmarks/chirp_signal_comparison.png` | `assets/images/generated/chirp_signal_comparison.png` |
    | `assets/images/benchmarks/polyphonic_signal_comparison.png` | `assets/images/generated/polyphonic_signal_comparison.png` |
    | `assets/images/benchmarks/musical_signal_comparison.png` | `assets/images/generated/musical_signal_comparison.png` |
    | `assets/images/benchmarks/timing_bar_chart.png` | `assets/images/generated/timing_bar_chart.png` |
    | `assets/images/benchmarks/numpy_vs_cupy_diff.png` | `assets/images/reference/numpy_vs_cupy_diff.png` |
    | `assets/audio/daw/a2a3_a4_minor_scale.wav` | `assets/audio/reference/a2a3_a4_minor_scale.wav` |
    | `assets/audio/songs/beltran_sc_rip_16_bar.wav` | `assets/audio/reference/beltran_sc_rip_16_bar.wav` |
    | `assets/images/diagnostics/*` | `assets/archive/diagnostics/*` |

    Search each doc file for any `assets/images/benchmarks/`, `assets/audio/daw/`, `assets/audio/songs/`, `assets/images/diagnostics/` references and update them.

    If DSP.md or AUDIO.md don't exist yet, skip them (they may be scaffolds from Phase 5 that haven't been finalized).

    Also update `assets/audio/README.md` if it contains old path references.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && ! grep -r "assets/images/benchmarks/" README.md docs/ 2>/dev/null && ! grep -r "assets/audio/daw/" README.md docs/ 2>/dev/null && ! grep -r "assets/audio/songs/" README.md docs/ 2>/dev/null && echo "No old paths in docs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "assets/images/benchmarks/" README.md` returns no matches
    - `grep "assets/audio/daw/" README.md` returns no matches
    - `grep "assets/audio/songs/" README.md` returns no matches
    - If DSP.md exists: `grep "assets/images/benchmarks/" docs/DSP.md` returns no matches
    - README.md contains `assets/images/generated/` for benchmark figures
  </acceptance_criteria>
  <done>All documentation references updated to new asset paths. No broken image/audio references remain.</done>
</task>

</tasks>

<verification>
- `test -d research/archive && test -d assets/archive`
- `! test -d src/subshader/viz`
- `! grep -r "assets/images/benchmarks/" README.md docs/ 2>/dev/null`
- `! grep -r "assets/audio/daw/" README.md docs/ 2>/dev/null`
- `python -c "from subshader.pipeline import SubShader; print('All imports still work')"` exits 0
</verification>

<success_criteria>
Repository cleaned up: unused files archived (not deleted), viz/ directory fully removed, empty directories removed, documentation paths updated. All imports still work. Zero references to old asset paths in docs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-refactoring-and-module-cleanup/08-07-SUMMARY.md`
</output>
