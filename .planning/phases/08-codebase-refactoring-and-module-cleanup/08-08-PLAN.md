---
phase: 08-codebase-refactoring-and-module-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/subshader/renderer/renderer.py
autonomous: true
gap_closure: true
requirements:
  - D-21
  - D-24

must_haves:
  truths:
    - "Renderer.update() has the @timed decorator"
    - "@timed is imported from subshader.utils.timing in renderer.py"
    - "All three pipeline stages (AudioStream, DSP, Renderer) now have @timed coverage"
  artifacts:
    - path: "src/subshader/renderer/renderer.py"
      provides: "Renderer with @timed on update()"
      contains: "@timed"
  key_links:
    - from: "src/subshader/renderer/renderer.py"
      to: "src/subshader/utils/timing.py"
      via: "from subshader.utils.timing import timed"
      pattern: "from subshader\\.utils\\.timing import timed"
---

<objective>
Add @timed decorator to Renderer.update() — closing the only verification gap from Phase 08.

Purpose: D-21 requires @timed on ALL pipeline stages and D-24 requires --timing to include the renderer. AudioStream and DSP stages already have @timed; Renderer.update() is the sole missing piece. Once decorated, the TimedSubShader timing harness in research/timing.py can collect renderer stage timing automatically via the _timing_update_ms instance attribute.
Output: Updated renderer.py with @timed import and decorator on update().
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @timed decorator to Renderer.update()</name>
  <files>src/subshader/renderer/renderer.py</files>
  <read_first>
    - src/subshader/renderer/renderer.py (file being modified — see current imports at lines 10-21, update() at line 438)
    - src/subshader/audio/audio_stream.py (reference for how @timed is imported and applied — line 17: `from subshader.utils.timing import timed`, line 68: `@timed` on get_chunk, line 80: `@timed` on next_chunk)
    - src/subshader/utils/timing.py (source of @timed decorator)
  </read_first>
  <action>
Two changes to src/subshader/renderer/renderer.py:

1. Add import at line 18 (after the existing `from subshader.config import RendererConfig` line):
   ```python
   from subshader.utils.timing import timed
   ```

2. Add `@timed` decorator directly above `def update(self, coefs: np.ndarray) -> None:` at line 438:
   ```python
   @timed
   def update(self, coefs: np.ndarray) -> None:
   ```

This matches the exact pattern used in audio_stream.py (line 17 for import, lines 68/80 for decorator placement) and cwt.py (line 31 for import). The @timed decorator stores elapsed time in `self._timing_update_ms` after each call, which the TimedSubShader harness in research/timing.py reads to report renderer timing in --timing output.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && grep -n "@timed" src/subshader/renderer/renderer.py && grep -n "from subshader.utils.timing import timed" src/subshader/renderer/renderer.py && python -c "from subshader.renderer import Renderer; print('import OK')" && pytest research/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@timed" src/subshader/renderer/renderer.py` returns at least one line showing `@timed` above `def update`
    - `grep -n "from subshader.utils.timing import timed" src/subshader/renderer/renderer.py` returns exactly one line
    - `python -c "from subshader.renderer import Renderer"` exits 0 (no import errors)
    - `pytest research/tests/ -x -q` shows 15 passed (no regressions)
  </acceptance_criteria>
  <done>Renderer.update() has @timed decorator, import is present, all 15 tests pass, D-21 and D-24 are satisfied.</done>
</task>

</tasks>

<verification>
1. `grep -c "@timed" src/subshader/renderer/renderer.py` returns >= 1
2. `grep "from subshader.utils.timing import timed" src/subshader/renderer/renderer.py` returns match
3. `python -c "from subshader.renderer import Renderer; print('OK')"` exits 0
4. `pytest research/tests/ -x -q` — 15 tests pass
5. All three pipeline modules now have @timed:
   - `grep -l "@timed" src/subshader/audio/audio_stream.py src/subshader/dsp/cwt.py src/subshader/renderer/renderer.py` returns all 3 files
</verification>

<success_criteria>
- @timed decorator appears on Renderer.update() in renderer.py
- `from subshader.utils.timing import timed` import present in renderer.py
- All 15 existing tests pass without regression
- D-21 (all pipeline stages timed) and D-24 (renderer included in --timing) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-refactoring-and-module-cleanup/08-08-SUMMARY.md`
</output>
