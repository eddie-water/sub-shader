---
phase: 08-codebase-refactoring-and-module-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/subshader/config.py
  - assets/audio/reference/beltran_sc_rip.wav
  - assets/audio/reference/beltran_sc_rip_4_bar.wav
  - assets/audio/reference/beltran_sc_rip_8_bar.wav
  - assets/audio/reference/beltran_sc_rip_16_bar.wav
  - assets/audio/reference/a2a3_a4_minor_scale.wav
  - assets/audio/reference/midi_sine_waves.wav
  - assets/audio/generated/bouncing_chirp.wav
  - assets/images/reference/numpy_vs_cupy_diff.png
  - assets/images/generated/chirp_comparison.png
  - assets/images/generated/polyphonic_comparison.png
  - assets/images/generated/musical_comparison.png
  - assets/images/generated/comparison_grid.png
  - assets/images/generated/timing_bar_chart.png
  - assets/timing/.gitkeep
  - research/utilities/constants.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-33
  - D-34
  - D-35

must_haves:
  truths:
    - "PipelineConfig is a flat dataclass with file_path, chunk_size, overlap_factor, sample_rate, total_samples"
    - "CWTConfig inherits PipelineConfig and adds wavelet-specific params"
    - "RendererConfig inherits PipelineConfig and adds rendering params"
    - "hop_size and nyquist_freq are @property on PipelineConfig"
    - "Assets are organized into reference/ and generated/ subdirectories"
    - "Default audio path points to assets/audio/reference/beltran_sc_rip.wav"
    - "assets/plots/ directory is left untouched for architecture diagrams (per D-35)"
  artifacts:
    - path: "src/subshader/config.py"
      provides: "PipelineConfig, CWTConfig, RendererConfig, ColorNormalizationConfig"
      contains: "class PipelineConfig"
    - path: "assets/audio/reference/"
      provides: "Committed audio input files"
    - path: "assets/audio/generated/"
      provides: "Synthesized audio files"
    - path: "assets/images/reference/"
      provides: "Committed reference images"
    - path: "assets/images/generated/"
      provides: "Test-suite generated figures"
    - path: "assets/timing/"
      provides: "Timestamped timing output directory"
  key_links:
    - from: "src/subshader/config.py"
      to: "assets/audio/reference/beltran_sc_rip.wav"
      via: "PipelineConfig.file_path default"
      pattern: 'file_path.*=.*"assets/audio/reference/beltran_sc_rip.wav"'
    - from: "research/utilities/constants.py"
      to: "assets/"
      via: "path constants"
      pattern: "assets/audio/reference/"
---

<objective>
Redesign the config system from nested composition (ProcessingConfig with sub-configs) to flat inheritance (PipelineConfig base with CWTConfig and RendererConfig subclasses), and reorganize assets into reference/generated lifecycle directories.

Purpose: Config is the foundation every other plan depends on. Asset paths must be settled before modules reference them.
Output: New config.py with PipelineConfig/CWTConfig/RendererConfig, reorganized assets/, updated constants.py paths.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/08-CONTEXT.md
@.planning/phases/08-codebase-refactoring-and-module-cleanup/config-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite config.py with PipelineConfig inheritance hierarchy</name>
  <files>src/subshader/config.py</files>
  <read_first>
    - src/subshader/config.py
    - .planning/phases/08-codebase-refactoring-and-module-cleanup/config-design.md
    - src/subshader/__main__.py (to understand current get_default_config usage)
  </read_first>
  <action>
    Rewrite config.py per D-01, D-02, D-03, D-04:

    1. Create `PipelineConfig` dataclass with these exact fields:
       - `file_path: str = "assets/audio/reference/beltran_sc_rip.wav"` (per D-04)
       - `chunk_size: int = 1 << 14`
       - `overlap_factor: float = 0.5`
       - `sample_rate: float = 44100.0`  (discovered at runtime by AudioStream, per D-06)
       - `total_samples: int = 0`  (discovered at runtime by AudioStream, per D-06)
       - `@property hop_size(self) -> int: return int(self.chunk_size * (1.0 - self.overlap_factor))`  (per D-03)
       - `@property nyquist_freq(self) -> float: return self.sample_rate / 2.0`  (per D-03)
       - `validate(self) -> list[str]` method checking file_path exists, chunk_size > 0, 0 < overlap_factor < 1

    2. Create `ColorNormalizationConfig` dataclass (preserve current fields from existing VisualizationConfig):
       - `gamma: float = 0.5`
       - `global_intensity_smoothing_weight: float = 0.1`
       - `global_intensity_percentile: float = 99.0`
       - `frame_intensity_percentile: float = 95.0`
       - `decay_rate: float = 0.95`
       - `initial_intensity: float = 0.1`
       - `frame_brightness_percentile: float = 99.0`

    3. Create `CWTConfig(PipelineConfig)` dataclass per D-02:
       - `notes_per_octave: int = 12`
       - `num_octaves: int = 10`
       - `root_note_hz: float = 27.5`
       - `target_width: int = 64`
       - `num_cycles: int = 6`
       - `num_fwhm_cycles: int = 3`
       - `validate(self)` extends super().validate() with wavelet-specific checks

    4. Create `RendererConfig(PipelineConfig)` dataclass per D-02:
       - `num_frames: int = 256`
       - `color_norm: ColorNormalizationConfig = field(default_factory=ColorNormalizationConfig)`
       - `validate(self)` extends super().validate() with rendering checks (num_frames > 0)

    5. Keep `_get_system_display_size()` utility function.

    6. Create `get_default_config()` function that returns a `CWTConfig` instance (the most param-rich subclass, per research recommendation). This maintains backward compat — callers that currently call `get_default_config()` still get a working config. Add a comment explaining the choice.

    7. Keep `ProcessingConfig` as a DEPRECATED alias: `ProcessingConfig = CWTConfig` with a comment "# DEPRECATED: Use CWTConfig or PipelineConfig directly. Will be removed in next phase." This prevents breaking all existing imports while we migrate callers in subsequent plans.

    IMPORTANT: Do NOT remove the window_width/window_height fields yet — they are used by the renderer. Add them to RendererConfig:
       - `window_width: int` and `window_height: int` with defaults from `_get_system_display_size()`

    Preserve the existing section comment style (`# ===== SECTION =====`).
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -c "from subshader.config import PipelineConfig, CWTConfig, RendererConfig, ColorNormalizationConfig, get_default_config; c = get_default_config(); print(f'hop_size={c.hop_size}, nyquist={c.nyquist_freq}, path={c.file_path}'); assert c.hop_size == 8192; assert c.nyquist_freq == 22050.0; assert 'beltran_sc_rip' in c.file_path; print('Config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config.py contains `class PipelineConfig` with `file_path`, `chunk_size`, `overlap_factor`, `sample_rate`, `total_samples` fields
    - config.py contains `def hop_size` and `def nyquist_freq` as @property methods on PipelineConfig
    - config.py contains `class CWTConfig(PipelineConfig)` with `notes_per_octave`, `num_octaves`, `root_note_hz`
    - config.py contains `class RendererConfig(PipelineConfig)` with `num_frames`, `color_norm`
    - config.py contains `ProcessingConfig = CWTConfig` compatibility alias
    - PipelineConfig.file_path default contains `assets/audio/reference/beltran_sc_rip.wav`
    - `python -c "from subshader.config import PipelineConfig"` exits 0
  </acceptance_criteria>
  <done>PipelineConfig, CWTConfig, RendererConfig exist with all fields, @property derivations work, get_default_config() returns CWTConfig, ProcessingConfig alias preserved for backward compat</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize assets into reference/generated lifecycle dirs and update constants.py</name>
  <files>
    assets/audio/reference/
    assets/audio/generated/
    assets/images/reference/
    assets/images/generated/
    assets/timing/.gitkeep
    research/utilities/constants.py
  </files>
  <read_first>
    - research/utilities/constants.py
    - assets/audio/README.md
  </read_first>
  <action>
    Reorganize assets per D-33, D-34, D-35:

    1. Create directory structure:
       ```
       mkdir -p assets/audio/reference assets/audio/generated
       mkdir -p assets/images/reference assets/images/generated
       mkdir -p assets/images/generated/dpi assets/images/generated/stubs
       mkdir -p assets/timing
       touch assets/timing/.gitkeep
       ```

    2. Move audio files using `git mv`:
       - `git mv assets/audio/songs/beltran_sc_rip_16_bar.wav assets/audio/reference/beltran_sc_rip_16_bar.wav`
       - `git mv assets/audio/songs/beltran_sc_rip_8_bar.wav assets/audio/reference/beltran_sc_rip_8_bar.wav`
       - `git mv assets/audio/daw/a2a3_a4_minor_scale.wav assets/audio/reference/a2a3_a4_minor_scale.wav`
       - `git mv assets/audio/daw/chirp_comparison_grid.wav assets/audio/generated/chirp_comparison_grid.wav`
       - `git mv assets/audio/daw/chirp_random_walk.wav assets/audio/generated/chirp_random_walk.wav`
       - Check for overlapping_A3_A4_A5.wav or similar polyphonic file and move to reference/

    3. Move image files using `git mv`:
       - `git mv assets/images/benchmarks/comparison_grid.png assets/images/generated/comparison_grid.png`
       - `git mv assets/images/benchmarks/chirp_signal_comparison.png assets/images/generated/chirp_signal_comparison.png`
       - `git mv assets/images/benchmarks/polyphonic_signal_comparison.png assets/images/generated/polyphonic_signal_comparison.png`
       - `git mv assets/images/benchmarks/musical_signal_comparison.png assets/images/generated/musical_signal_comparison.png`
       - `git mv assets/images/benchmarks/numpy_vs_cupy_diff.png assets/images/reference/numpy_vs_cupy_diff.png`
       - Move timing_bar_chart.png if it exists to generated/
       - Move dpi/ contents to generated/dpi/
       - Move stubs/ contents to generated/stubs/

    4. Update `research/utilities/constants.py`:
       - Change all `assets/audio/daw/` paths to `assets/audio/reference/`
       - Change all `assets/audio/songs/` paths to `assets/audio/reference/`
       - Change all `assets/images/benchmarks/` paths to `assets/images/generated/`
       - Add `TIMING_DIR = "assets/timing"` constant
       - Add `AUDIO_REFERENCE_DIR = "assets/audio/reference"` constant
       - Add `AUDIO_GENERATED_DIR = "assets/audio/generated"` constant
       - Add `IMAGES_REFERENCE_DIR = "assets/images/reference"` constant
       - Add `IMAGES_GENERATED_DIR = "assets/images/generated"` constant

    5. Handle any files in `assets/audio/figures/` — if they are synthesized outputs, move to `assets/audio/generated/`. If references, move to `assets/audio/reference/`.

    6. Per D-35: Do NOT touch `assets/plots/` — it stays as-is for architecture diagrams (drawio/mermaid).

    Note: Do NOT move archive-bound files yet (D-36, D-38 handled in Plan 07). Only reorganize active files into the new lifecycle structure.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test -d assets/audio/reference && test -d assets/audio/generated && test -d assets/images/reference && test -d assets/images/generated && test -d assets/timing && python -c "from research.utilities.constants import *; print('Constants import OK')" && echo "Assets OK"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `assets/audio/reference/` exists and contains at least `a2a3_a4_minor_scale.wav`
    - Directory `assets/audio/generated/` exists
    - Directory `assets/images/reference/` exists and contains `numpy_vs_cupy_diff.png`
    - Directory `assets/images/generated/` exists and contains `comparison_grid.png`
    - Directory `assets/timing/` exists
    - `assets/plots/` directory is untouched (per D-35) — if it exists, it remains as-is
    - `research/utilities/constants.py` contains string `assets/audio/reference/` (not `assets/audio/daw/`)
    - `research/utilities/constants.py` contains `TIMING_DIR`
    - `python -c "from research.utilities.constants import *"` exits 0
  </acceptance_criteria>
  <done>Assets reorganized into reference/generated lifecycle directories, constants.py paths updated, assets/plots/ untouched per D-35, all imports still work</done>
</task>

</tasks>

<verification>
- `python -c "from subshader.config import PipelineConfig, CWTConfig, RendererConfig"` exits 0
- `python -c "from subshader.config import ProcessingConfig, get_default_config"` exits 0 (backward compat)
- `ls assets/audio/reference/ assets/images/generated/` shows expected files
- `python -c "from research.utilities.constants import *"` exits 0
</verification>

<success_criteria>
Config system redesigned with inheritance hierarchy. Assets organized by lifecycle. All existing imports still work via ProcessingConfig alias. constants.py paths point to new locations. assets/plots/ untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-refactoring-and-module-cleanup/08-01-SUMMARY.md`
</output>
