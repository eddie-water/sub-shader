---
status: testing
phase: 05.1-research-toolkit-restructure
source: [05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md]
started: 2026-03-24T21:00:00Z
updated: 2026-03-24T21:00:00Z
---

## Current Test

number: 1
name: Comparison grid generation still works
expected: |
  Run `python research/benchmark.py --comparison-grid --stub-pywt` from repo root.
  Produces a comparison grid PNG in assets/images/benchmarks/ without errors.
awaiting: user response

## Tests

### 1. Comparison grid generation still works
expected: Run `python research/benchmark.py --comparison-grid --stub-pywt` from repo root. Produces a comparison grid PNG in assets/images/benchmarks/ without errors.
result: [pending]

### 2. Timing benchmark still works
expected: Run `python research/benchmark.py --timing` from repo root. Prints STFT vs PyWavelet vs CWT timing comparison output to terminal without errors.
result: [pending]

### 3. Pytest suite runs and passes
expected: Run `pytest src/ -v` from repo root. Collects 12 tests across 4 files (test_wavelet.py, test_wavelet_kernel.py, test_plotter.py, test_audio_overlap.py). All 12 pass.
result: [pending]

### 4. Unit tests via benchmark flag
expected: Run `python research/benchmark.py --unit-tests` from repo root. Dispatches to pytest, runs all 12 tests, exits cleanly (does NOT launch the main subshader app).
result: [pending]

### 5. Old test files deleted
expected: `research/utilities/unit_tests.py` and `research/overlap_diagnostic.py` no longer exist. `research/font_showcase_20.py` no longer exists at root (moved to `research/utilities/font_showcase_20.py`).
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

[none yet]
