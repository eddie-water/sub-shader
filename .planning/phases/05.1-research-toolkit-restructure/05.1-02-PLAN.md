---
phase: 05.1-research-toolkit-restructure
plan: 02
type: execute
wave: 2
depends_on: [05.1-01]
files_modified:
  - src/subshader/dsp/test_wavelet.py
  - src/subshader/dsp/test_wavelet_kernel.py
  - src/subshader/viz/test_plotter.py
  - src/subshader/audio/test_audio_overlap.py
  - src/subshader/conftest.py
  - research/benchmark.py
  - research/utilities/font_showcase_20.py
autonomous: true
requirements: [RTK-04, RTK-05, RTK-06]

must_haves:
  truths:
    - "pytest src/ discovers and runs all migrated tests"
    - "All 10 original test categories pass under pytest"
    - "research/utilities/unit_tests.py is deleted"
    - "research/overlap_diagnostic.py is deleted"
    - "font_showcase_20.py lives in research/utilities/"
    - "benchmark.py --unit-tests dispatches to pytest"
  artifacts:
    - path: "src/subshader/dsp/test_wavelet.py"
      provides: "Tests 0, 1, 2, 3, 6, 7, 8 migrated to pytest assertions"
      contains: "def test_pure_tone_peak_accuracy"
    - path: "src/subshader/dsp/test_wavelet_kernel.py"
      provides: "Tests 4, 5, 9 migrated to pytest assertions"
      contains: "def test_kernel_fft_bank_integrity"
    - path: "src/subshader/viz/test_plotter.py"
      provides: "Test 10 migrated to pytest assertions"
      contains: "def test_buffer_normalization"
    - path: "src/subshader/audio/test_audio_overlap.py"
      provides: "Overlap diagnostic test logic as pytest test"
      contains: "def test_"
    - path: "src/subshader/conftest.py"
      provides: "Shared test helpers"
      contains: "def generate_tone"
    - path: "research/utilities/font_showcase_20.py"
      provides: "Relocated font showcase script"
      contains: "matplotlib"
  key_links:
    - from: "src/subshader/dsp/test_wavelet.py"
      to: "src/subshader/conftest.py"
      via: "pytest auto-discovery of conftest.py helpers"
      pattern: "generate_tone|find_peak_bin|_make_wavelet"
    - from: "research/benchmark.py"
      to: "pytest"
      via: "--unit-tests flag dispatches to pytest runner"
      pattern: "pytest"
---

<objective>
Migrate unit_tests.py into colocated pytest test files alongside source modules, relocate standalone files, and update the benchmark.py --unit-tests dispatch.

Purpose: Per D-04/D-05/D-06/D-07, tests should live next to the code they test using flat colocated pattern. The old handrolled test runner (_pass_fail + run_all) is replaced by standard pytest assertions.
Output: 4 colocated test files in src/subshader/, shared conftest.py, deleted old files, updated CLI dispatch
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-research-toolkit-restructure/05.1-CONTEXT.md
@.planning/phases/05.1-research-toolkit-restructure/05.1-RESEARCH.md
@.planning/phases/05.1-research-toolkit-restructure/05.1-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from the existing codebase -->

From src/subshader/config.py:
  get_default_config() -> ProcessingConfig
  ProcessingConfig has: .audio (AudioConfig), .wavelet (WaveletConfig)
  WaveletConfig has: .typical_sampling_freq, .target_width
  ColorNormalizationConfig()

From src/subshader/dsp/wavelet.py:
  class PyWavelet(Wavelet)   - .cwt(data) -> np.ndarray, .freqs, .num_freqs, .get_output_shape()
  class NumPyWavelet(Wavelet) - same interface
  class CuPyWavelet(Wavelet) - same interface (GPU-only)

From src/subshader/dsp/wavelet_kernel.py:
  class WaveletKernel - .kernel_t, .kernel_fft, .freq

From src/subshader/audio/audio_input.py:
  class AudioInput(path, chunk_size, overlap_factor) - .get_chunk(), .get_sample_rate()

From src/subshader/viz/plotter.py:
  class CircularFrameBuffer(frame_shape, num_frames, color_norm_config)
    - .push_frame(frame), .get_intensity_max(), .get_flattened_buffer()

From src/subshader/utils:
  gpu_available() -> bool  (already exported from subshader.utils)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create colocated test files and conftest.py</name>
  <files>src/subshader/conftest.py, src/subshader/dsp/test_wavelet.py, src/subshader/dsp/test_wavelet_kernel.py, src/subshader/viz/test_plotter.py, src/subshader/audio/test_audio_overlap.py</files>
  <read_first>
    - research/utilities/unit_tests.py (the source file being migrated — read the ENTIRE 664 lines)
    - research/overlap_diagnostic.py (test logic to fold into test_audio_overlap.py)
    - src/subshader/dsp/wavelet.py (verify class names and interfaces)
    - src/subshader/dsp/wavelet_kernel.py (verify WaveletKernel interface — .kernel_t, .kernel_fft, .freq)
    - src/subshader/viz/plotter.py (verify CircularFrameBuffer interface)
    - src/subshader/audio/audio_input.py (verify AudioInput interface)
    - src/subshader/utils/gpu.py (verify gpu_available exists here)
  </read_first>
  <action>
    Create 5 files. CRITICAL: Convert ALL `_pass_fail(condition, label)` calls to `assert condition, label` statements. Remove all `all_passed &= ...` tracking — pytest handles pass/fail via exceptions. Remove all `print(f"\n--- Test N: ...")` headers — pytest provides its own output. Keep any print statements that show diagnostic values (useful in `-v` mode).

    **src/subshader/conftest.py** — Shared test helpers:
    ```python
    """Shared test helpers for SubShader pytest suite."""
    import numpy as np
    from subshader.config import get_default_config

    def generate_tone(freq_hz, sample_rate, num_samples):
        """Generate a pure sine tone at a given frequency."""
        t = np.linspace(0, num_samples / sample_rate, num_samples, endpoint=False)
        return np.sin(2 * np.pi * freq_hz * t).astype(np.float64)

    def find_peak_bin(cwt_output, freqs):
        """Find the frequency bin with the highest mean energy."""
        mean_energy = np.mean(cwt_output, axis=1)
        peak_bin = np.argmax(mean_energy)
        return peak_bin, freqs[peak_bin]

    def _make_wavelet(cls, config=None):
        """Construct a wavelet instance with default config."""
        if config is None:
            config = get_default_config()
        sr = int(config.wavelet.typical_sampling_freq)
        chunk = config.audio.chunk_size
        return cls(sample_rate=sr, input_n=chunk, config=config.wavelet)
    ```
    NOTE: These are plain functions, NOT pytest fixtures. Test files import them directly: `from conftest import generate_tone, find_peak_bin, _make_wavelet`. (pytest auto-discovers conftest.py but the functions are used as regular imports.)

    **src/subshader/dsp/test_wavelet.py** — Tests 0, 1, 2, 3, 6, 7, 8:
    - Imports: `import numpy as np`, `import pytest`, `from subshader.config import get_default_config, WaveletConfig, ColorNormalizationConfig`, `from subshader.dsp.wavelet import PyWavelet, NumPyWavelet`, `from conftest import generate_tone, find_peak_bin, _make_wavelet`
    - `TEST_FREQS = [27.5, 130.81, 440.0, 1000.0, 4186.01, 10000.0]` (module-level constant)
    - Test 0: `test_numpy_vs_cupy()` — decorated with `@pytest.mark.skipif(not _gpu_available(), reason="No CUDA GPU available")`. Inline `_gpu_available()` as a 5-line helper at module level:
      ```python
      def _gpu_available():
          try:
              import cupy as cp
              cp.cuda.runtime.getDevice()
              return True
          except Exception:
              return False
      ```
      Inside the test, lazy-import CuPyWavelet: `from subshader.dsp.wavelet import CuPyWavelet`. Convert the body from unit_tests.py `verify_numpy_vs_cupy` function. Replace `_pass_fail(passed, label)` with `assert passed, label`. Replace `_pass_fail(rel_error < 1e-3 and len(low_corr) == 0, ...)` with `assert rel_error < 1e-3, f"..."` and `assert len(low_corr) == 0, f"..."`. Drop save_figure parameter and `_save_diff_figure` call (diagnostic output is not needed in pytest; the figure generation was for the old handrolled runner).
    - Test 1: `test_pure_tone_peak_accuracy()` — from unit_tests.py lines 174-205. For each wavelet class and freq, assert `abs(peak_bin - expected_bin) <= 1`.
    - Test 2: `test_cross_implementation_agreement()` — from unit_tests.py lines 212-240. Assert Pearson r > 0.99 between NumPy and PyWavelet outputs.
    - Test 3: `test_chirp_monotonicity()` — from unit_tests.py lines 247-291. Assert peak bin increases monotonically as chirp frequency rises.
    - Test 6: `test_normalization_behavior()` — from unit_tests.py lines 368-453. Convert all `_pass_fail` to `assert`.
    - Test 7: `test_reliable_region_consistency()` — from unit_tests.py lines 460-487. Convert all `_pass_fail` to `assert`.
    - Test 8: `test_output_shape_regression()` — from unit_tests.py lines 494-523. Assert `declared_shape == expected_shape`, `output.shape == declared_shape`, `len(wt.freqs) == wt.num_freqs`.

    **src/subshader/dsp/test_wavelet_kernel.py** — Tests 4, 5, 9:
    - Imports: `import numpy as np`, `from numpy.fft import fft`, `from subshader.dsp.wavelet import NumPyWavelet`, `from conftest import _make_wavelet`
    - Test 4: `test_kernel_fft_bank_integrity()` — from unit_tests.py lines 298-319. For each wavelet kernel, assert `np.allclose(fft(w.kernel_t, n=N), w.kernel_fft, atol=1e-10)`.
    - Test 5: `test_impulse_response_center_freq()` — from unit_tests.py lines 326-361. Assert peak FFT bin is within 2 bins of expected center frequency.
    - Test 9: `test_kernel_energy_per_scale()` — from unit_tests.py lines 530-573. Assert coefficient of variation < 0.15 and log-log slope within 0.15 of -0.5.

    **src/subshader/viz/test_plotter.py** — Test 10:
    - Imports: `import numpy as np`, `from subshader.config import ColorNormalizationConfig`, `from subshader.viz.plotter import CircularFrameBuffer`
    - `test_buffer_normalization()` — from unit_tests.py lines 580-619. Assert `imax > 0`, `flat.shape[0] == n_freqs`, `flat.shape[1] == n_time * n_frames`, `flat.max() > 1.0`.

    **src/subshader/audio/test_audio_overlap.py** — Overlap diagnostic:
    - Imports: `import numpy as np`, `from subshader.config import get_default_config`, `from subshader.dsp.wavelet import NumPyWavelet`
    - `test_overlap_hop_center_extraction()` — Extract the core assertion from overlap_diagnostic.py: given overlapping chunks from a synthetic chirp, the hop-center extraction (taking only the center `hop_size` columns from each CWT frame's reliable region) should produce a continuous spectrogram without redundant overlap. Assert that tiling hop-center slices produces correct total width: `n_chunks * hop_cols == expected_total_width`. Also assert no NaN values in the tiled output.
    - Keep the test self-contained: generate a short chirp signal, split into overlapping chunks, compute CWT on each, extract hop-center columns, tile, and verify dimensions and continuity. Do NOT import from overlap_diagnostic.py — rewrite the assertion logic as a clean pytest test (~30-40 lines).

    CRITICAL: Do NOT add `__init__.py` files to any directory that doesn't already have one. pytest discovers test files without them.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -m pytest src/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/subshader/conftest.py exists and contains `def generate_tone(`
    - src/subshader/conftest.py contains `def find_peak_bin(`
    - src/subshader/conftest.py contains `def _make_wavelet(`
    - src/subshader/dsp/test_wavelet.py contains `def test_pure_tone_peak_accuracy(`
    - src/subshader/dsp/test_wavelet.py contains `def test_cross_implementation_agreement(`
    - src/subshader/dsp/test_wavelet.py contains `def test_chirp_monotonicity(`
    - src/subshader/dsp/test_wavelet.py contains `def test_normalization_behavior(`
    - src/subshader/dsp/test_wavelet.py contains `def test_reliable_region_consistency(`
    - src/subshader/dsp/test_wavelet.py contains `def test_output_shape_regression(`
    - src/subshader/dsp/test_wavelet.py contains `@pytest.mark.skipif`
    - src/subshader/dsp/test_wavelet_kernel.py contains `def test_kernel_fft_bank_integrity(`
    - src/subshader/dsp/test_wavelet_kernel.py contains `def test_impulse_response_center_freq(`
    - src/subshader/dsp/test_wavelet_kernel.py contains `def test_kernel_energy_per_scale(`
    - src/subshader/viz/test_plotter.py contains `def test_buffer_normalization(`
    - src/subshader/audio/test_audio_overlap.py contains `def test_`
    - No file contains `_pass_fail(` (old pattern fully replaced)
    - No file contains `all_passed &=` (old pattern fully replaced)
    - `pytest src/ --collect-only` shows >= 12 test items collected
    - `pytest src/ -x` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>All 10 test categories plus overlap diagnostic are migrated to colocated pytest test files, all passing with `pytest src/`</done>
</task>

<task type="auto">
  <name>Task 2: Delete old files, relocate font_showcase, update CLI dispatch</name>
  <files>research/utilities/unit_tests.py, research/overlap_diagnostic.py, research/font_showcase_20.py, research/utilities/font_showcase_20.py, research/benchmark.py</files>
  <read_first>
    - research/benchmark.py (verify current --unit-tests dispatch logic at line ~1184-1186)
    - research/font_showcase_20.py (verify it has no dependencies on research/ or utilities/)
    - research/utilities/unit_tests.py (confirm this is the file to delete)
    - research/overlap_diagnostic.py (confirm this is the file to delete)
  </read_first>
  <action>
    Three operations:

    1. **Delete old files** (per D-10, clean break):
       - Delete `research/utilities/unit_tests.py`
       - Delete `research/overlap_diagnostic.py`

    2. **Relocate font_showcase_20.py** (per D-08):
       - Move `research/font_showcase_20.py` to `research/utilities/font_showcase_20.py`
       - The file is a standalone matplotlib script with zero imports from `subshader.*` or `utilities`. No import changes needed.

    3. **Update benchmark.py --unit-tests dispatch** (per D-07):
       - In research/benchmark.py, find the `--unit-tests` dispatch block (currently: `from utilities.unit_tests import run_all as run_unit_tests; modes.append(("Unit Tests", run_unit_tests))`)
       - Replace with a dispatch that runs pytest:
         ```python
         if args.unit_tests:
             import subprocess
             result = subprocess.run(
                 [sys.executable, "-m", "pytest", "src/", "-v"],
                 cwd=os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
             )
             if result.returncode != 0:
                 sys.exit(result.returncode)
         ```
       - This runs `pytest src/ -v` from the project root. The `--unit-tests` flag continues to work from `python research/benchmark.py --unit-tests`.
       - Add `import subprocess` and `import sys` and `import os` to the imports at the top of benchmark.py (if not already present). `sys` and `os` should already be there from the dispatcher; add `subprocess` if missing.
       - IMPORTANT: The unit-tests dispatch should NOT be added to the `modes` list (since it's not a function to call via `run_modes`). Instead, handle it separately before the `modes` block, and skip adding to modes. If `--unit-tests` is the only flag and it passes, exit normally. If combined with other flags, run pytest first then continue with other modes.

  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python research/benchmark.py --unit-tests 2>&1 | tail -5 && test ! -f research/utilities/unit_tests.py && echo "unit_tests.py deleted OK" && test ! -f research/overlap_diagnostic.py && echo "overlap_diagnostic.py deleted OK" && test -f research/utilities/font_showcase_20.py && echo "font_showcase relocated OK" && test ! -f research/font_showcase_20.py && echo "old font_showcase removed OK"</automated>
  </verify>
  <acceptance_criteria>
    - research/utilities/unit_tests.py does NOT exist
    - research/overlap_diagnostic.py does NOT exist
    - research/font_showcase_20.py does NOT exist (moved)
    - research/utilities/font_showcase_20.py exists and contains `matplotlib`
    - `python research/benchmark.py --unit-tests` runs pytest and shows test results
    - `python research/benchmark.py --help` still shows --unit-tests flag
    - benchmark.py contains `subprocess` (for pytest dispatch)
    - benchmark.py does NOT contain `from utilities.unit_tests import`
  </acceptance_criteria>
  <done>Old test files deleted, font_showcase relocated to research/utilities/, --unit-tests flag dispatches to pytest src/ -v</done>
</task>

</tasks>

<verification>
1. `pytest src/ -v` — all tests pass (>= 12 items collected)
2. `python research/benchmark.py --unit-tests` — runs pytest, all tests pass
3. `python research/benchmark.py --help` — shows all flags including --unit-tests
4. `test ! -f research/utilities/unit_tests.py` — old file deleted
5. `test ! -f research/overlap_diagnostic.py` — old file deleted
6. `test -f research/utilities/font_showcase_20.py` — font showcase relocated
7. `python research/benchmark.py --comparison-grid --stub-pywt --dpi 100` — figures still generate (Plan 01 backward compat)
</verification>

<success_criteria>
- All 10 test categories migrated to 4 colocated pytest test files
- _pass_fail pattern fully replaced with assert statements
- Old unit_tests.py and overlap_diagnostic.py deleted
- font_showcase_20.py relocated to research/utilities/
- --unit-tests CLI flag dispatches to pytest
- pytest src/ -v runs clean with all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-research-toolkit-restructure/05.1-02-SUMMARY.md`
</output>
