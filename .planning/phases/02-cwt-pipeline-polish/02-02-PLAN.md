---
phase: 02-cwt-pipeline-polish
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - assets/images/benchmarks/chirp_signal_comparison.png
  - assets/images/benchmarks/polyphonic_signal_comparison.png
autonomous: false
requirements: [PIPE-01, QUAL-02]

must_haves:
  truths:
    - "Chirp signal comparison figure shows uniform brightness across the frequency sweep (no high-frequency decay)"
    - "Polyphonic signal comparison figure shows comparable intensity across tones"
    - "Benchmark figure generation completes without errors"
  artifacts:
    - path: "assets/images/benchmarks/chirp_signal_comparison.png"
      provides: "Updated chirp comparison with normalized kernels"
    - path: "assets/images/benchmarks/polyphonic_signal_comparison.png"
      provides: "Updated polyphonic comparison with normalized kernels"
  key_links:
    - from: "research/benchmark.py"
      to: "src/subshader/dsp/wavelet.py"
      via: "imports NumPyWavelet for figure generation"
      pattern: "from subshader\\.dsp\\.wavelet import.*NumPyWavelet"
---

<objective>
Regenerate benchmark figures to visually confirm the normalization fix and get user sign-off.

Purpose: The automated tests prove magnitude ratios are within bounds, but the user needs to see the chirp and polyphonic comparison figures to confirm the fix looks correct visually. Per D-06, if SubShader's output looks better than PyWavelet's, that is a feature worth showing. Per D-09, observe whether intensity tracker parameters need tuning after the fix.

Output: Updated benchmark PNGs, user visual confirmation.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cwt-pipeline-polish/2-CONTEXT.md
@.planning/phases/02-cwt-pipeline-polish/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate benchmark comparison figures</name>
  <files>assets/images/benchmarks/chirp_signal_comparison.png, assets/images/benchmarks/polyphonic_signal_comparison.png</files>
  <read_first>
    - research/benchmark.py
    - src/subshader/dsp/wavelet_kernel.py
    - .planning/phases/02-cwt-pipeline-polish/02-01-SUMMARY.md
  </read_first>
  <action>
    Run the benchmark figure generation to produce updated PNGs with the L1-normalized kernels:

    ```bash
    cd /home/eddie-water/dev/python/sub-shader && python research/benchmark.py --figures
    ```

    If the command fails, read the error and fix. Common issues:
    - Import errors: check that wavelet_kernel.py changes from Plan 01 are on disk
    - Missing audio file: verify assets/audio/ files exist
    - matplotlib backend: benchmark.py already sets Agg backend (line 26), should work headless

    After generation, verify the output PNGs exist in assets/images/benchmarks/.

    Do NOT modify research/benchmark.py unless it errors due to the cwt_out_type removal or normalize_by_scale change. If it references cwt_out_type, remove that reference. If it calls normalize_by_scale expecting different behavior, that is fine -- it goes through the standard cwt() pipeline which now uses the no-op.
  </action>
  <verify>
    <automated>ls -la /home/eddie-water/dev/python/sub-shader/assets/images/benchmarks/chirp_signal_comparison.png /home/eddie-water/dev/python/sub-shader/assets/images/benchmarks/polyphonic_signal_comparison.png 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - assets/images/benchmarks/chirp_signal_comparison.png exists and was modified within the last 5 minutes
    - assets/images/benchmarks/polyphonic_signal_comparison.png exists and was modified within the last 5 minutes
    - `python research/benchmark.py --figures` exits with code 0
  </acceptance_criteria>
  <done>Benchmark figures regenerated with normalized kernels. PNGs updated on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of normalized CWT output</name>
  <files>assets/images/benchmarks/chirp_signal_comparison.png, assets/images/benchmarks/polyphonic_signal_comparison.png</files>
  <action>
    Present the regenerated benchmark figures to the user for visual verification.

    What was built: CWT kernel normalization fix (Plan 01) + regenerated benchmark figures (Task 1).
    The fix normalizes wavelet kernels to unit L1 area at construction time, eliminating the frequency-dependent brightness bias where low frequencies appeared disproportionately brighter.

    Verification steps for user:
    1. Open assets/images/benchmarks/chirp_signal_comparison.png
       - The SubShader CWT panel should show approximately uniform brightness across the full frequency sweep (low to high)
       - Compare with the STFT panel -- brightness distribution should be similar
       - The old behavior showed brightness decaying at high frequencies; this should be gone

    2. Open assets/images/benchmarks/polyphonic_signal_comparison.png
       - Equal-amplitude tones should appear at comparable brightness levels across frequency bands
       - Compare with the DAW spectrogram reference -- intensity across tones should be similar

    3. Compare SubShader output vs PyWavelet output in the same figures
       - Per D-06: if SubShader looks better than PyWavelet, that is expected and a positive result

    4. Per D-09: Observe whether the overall color range looks reasonable
       - If the image looks washed out or too dark, intensity tracker parameters (decay_rate=0.001, percentile=99, warmup=10) may need tuning in a follow-up
       - If the image looks good, no action needed

    Resume signal: Type "approved" if figures look correct, or describe what looks wrong.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting user visual verification of benchmark figures"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms chirp_signal_comparison.png shows uniform brightness across the frequency sweep
    - User confirms polyphonic_signal_comparison.png shows comparable intensity across tones
    - User types "approved" or provides feedback
  </acceptance_criteria>
  <done>User has visually confirmed that CWT output brightness is balanced across frequency bands in both benchmark figures.</done>
</task>

</tasks>

<verification>
1. Benchmark figures exist and are recently generated
2. User confirms visual correctness of chirp and polyphonic comparison figures
3. `pytest tests/ -q` still passes (regression check after any benchmark.py changes)
</verification>

<success_criteria>
- Chirp comparison figure shows uniform brightness across frequency sweep (user confirmed)
- Polyphonic comparison figure shows comparable intensity across tones (user confirmed)
- No intensity tracker tuning needed (or tuning identified as follow-up)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cwt-pipeline-polish/02-02-SUMMARY.md`
</output>
