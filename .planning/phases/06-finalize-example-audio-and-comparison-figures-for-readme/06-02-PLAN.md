---
phase: 06-finalize-example-audio-and-comparison-figures-for-readme
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - research/benchmark.py
  - README.md
  - DSP.md
autonomous: false
requirements:
  - FIG-04
  - FIG-05
  - FIG-06

must_haves:
  truths:
    - "README.md shows the comparison grid as a centered hero image in the Performance section"
    - "README.md per-signal sections (Chirp, Polyphonic, Musical) have scaffold captions replacing REWRITE markers"
    - "Timing bar chart reference is removed from README.md and placed in DSP.md"
    - "Final comparison grid PNG is generated at user-selected DPI with full PyWavelet (not stub)"
  artifacts:
    - path: "README.md"
      provides: "Updated Performance section with comparison grid figure"
      contains: "comparison_grid.png"
    - path: "DSP.md"
      provides: "Timing bar chart reference"
      contains: "timing_bar_chart"
    - path: "assets/images/benchmarks/comparison_grid.png"
      provides: "Final comparison grid at user-selected DPI"
  key_links:
    - from: "README.md"
      to: "assets/images/benchmarks/comparison_grid.png"
      via: "img src reference"
      pattern: "comparison_grid.png"
    - from: "DSP.md"
      to: "assets/images/benchmarks/timing_bar_chart.png"
      via: "img src reference"
      pattern: "timing_bar_chart.png"
---

<objective>
Generate the final comparison grid at user-selected DPI (with real PyWavelet), update README.md with the grid figure and scaffold captions, and move the timing bar chart to DSP.md.

Purpose: The README becomes a landing page with one hero comparison figure. Detailed timing analysis moves to DSP.md where it belongs.
Output: Final comparison_grid.png, updated README.md, updated DSP.md
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-finalize-example-audio-and-comparison-figures-for-readme/06-CONTEXT.md
@.planning/phases/06-finalize-example-audio-and-comparison-figures-for-readme/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From research/benchmark.py:
```python
def generate_comparison_grid(stub_pywt: bool = False, dpi: int = 200):
    # --dpi flag controls output DPI
    # Saves to comparison_grid.png (or comparison_grid_STUB.png / comparison_grid_{dpi}dpi.png)
```

Current README.md structure (Performance section):
```markdown
## Performance
[REWRITE markers for intro paragraphs]
[PLACEHOLDER: figure -- comparison grid]
### Chirp Signal (Frequency Sweep)
[REWRITE: intent="brief caption..."]
### Polyphonic Signal (MIDI Audio)
[REWRITE: intent="brief caption..."]
### Musical Signal (percussion + sustained bass)
[REWRITE: intent="brief caption..."]

## Benchmark
<p align="center"><img src="assets/images/benchmarks/timing_bar_chart.png" width="50%"></p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate final comparison grid and update README.md + DSP.md</name>
  <files>research/benchmark.py, README.md, DSP.md</files>
  <read_first>
    - README.md (full file — need to see all REWRITE/PLACEHOLDER markers and current structure)
    - DSP.md (full file — find correct insertion point for timing bar chart)
    - .planning/phases/06-finalize-example-audio-and-comparison-figures-for-readme/06-01-SUMMARY.md (user's DPI choice and any chirp adjustments)
    - research/benchmark.py (generate_comparison_grid function — confirm dpi parameter is wired)
  </read_first>
  <action>
    **1. Generate final comparison grid at user-selected DPI (from Plan 01 checkpoint):**

    Run with real PyWavelet (no --stub-pywt):
    ```bash
    cd research && python benchmark.py --comparison-grid --dpi {USER_SELECTED_DPI}
    ```
    This produces `assets/images/benchmarks/comparison_grid.png` (the canonical filename when using default or matching DPI).

    If user selected a DPI other than 200, also ensure the file is saved as `comparison_grid.png` (the canonical name README will reference). Update the save logic if needed so the non-suffixed filename always uses the user-selected DPI.

    **2. Update README.md Performance section:**

    Replace the `[PLACEHOLDER: figure -- "3x3 comparison grid..."]` block (around line 62) with:
    ```markdown
    <p align="center"><img src="assets/images/benchmarks/comparison_grid.png" width="80%"></p>
    ```
    Per D-08: centered at ~80% width.

    Replace the three per-signal `[REWRITE: intent="..."]` markers (per D-09) with scaffold captions:

    For **Chirp Signal** section (rename to "Bouncing Chirp" to match grid column label):
    ```markdown
    ### Bouncing Chirp

    [WRITE: "Non-stationary test signal — frequency sweeps upward across three decades (20Hz to 20kHz) with periodic parabolic dips. Designed to stress-test time-frequency resolution: methods with fixed resolution blur the rapid frequency transitions, while adaptive resolution tracks the contour precisely."]
    ```

    For **Polyphonic Signal** section:
    ```markdown
    ### Polyphonic Signal

    [WRITE: "MIDI composition with overlapping notes at varying pitches and durations. Tests each method's ability to resolve simultaneous frequencies and distinguish sustained tones from transient attacks."]
    ```

    For **Musical Signal** section:
    ```markdown
    ### Musical Signal

    [WRITE: "Eight bars of electronic music with four-on-the-floor percussion and sustained bass. Real-world audio that combines broadband transients (kicks, hats) with tonal content — the most demanding test for any time-frequency method."]
    ```

    Remove the structured comparison block (lines ~76-100, the "STFT / PYWT CWT / Sub Shader CWT" bullet lists with TODO placeholders). Replace with:
    ```markdown
    [WRITE: "Structured comparison of STFT vs PyWavelet vs SubShader — accuracy and computational cost for each method"]
    ```

    Remove the analogy REWRITE block (lines ~102-105). Replace with:
    ```markdown
    [WRITE: "Summary of time-frequency resolution tradeoffs — keep the analogy tone from the original draft"]
    ```

    **3. Move timing bar chart from README.md to DSP.md:**

    In README.md, remove the Benchmark section content (lines ~108-114) and replace:
    ```markdown
    ## Benchmark

    [WRITE: "Brief summary of SubShader pipeline timing — link to DSP.md for detailed breakdown"]

    For detailed timing analysis, see [DSP: Computational Cost](DSP.md#6-computational-cost).
    ```

    In DSP.md, find section 6 (Computational Cost) or the closest appropriate section. Add the timing bar chart reference:
    ```markdown
    <p align="center"><img src="assets/images/benchmarks/timing_bar_chart.png" width="60%"></p>

    [WRITE: "SubShader pipeline timing breakdown — per-stage costs for CWT computation, normalization, and rendering"]
    ```
  </action>
  <verify>
    <automated>grep -c "comparison_grid.png" README.md && grep -c "timing_bar_chart.png" DSP.md && grep "Bouncing Chirp" README.md && ls -la assets/images/benchmarks/comparison_grid.png</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `comparison_grid.png` in an img tag with `width="80%"`
    - README.md contains `### Bouncing Chirp` (renamed from "Chirp Signal")
    - README.md per-signal sections contain `[WRITE:` scaffold markers (not `[REWRITE:`)
    - README.md does NOT contain `[PLACEHOLDER: figure` for comparison grid
    - README.md does NOT contain `timing_bar_chart.png` in an img tag
    - README.md Benchmark section contains link to `DSP.md#`
    - DSP.md contains `timing_bar_chart.png` in an img tag
    - assets/images/benchmarks/comparison_grid.png exists (final DPI version)
  </acceptance_criteria>
  <done>Final comparison grid generated with real PyWavelet at user-selected DPI, README.md updated with hero figure and scaffold captions, timing bar chart moved to DSP.md</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies README layout and final comparison grid</name>
  <files>README.md, DSP.md, assets/images/benchmarks/comparison_grid.png</files>
  <action>
    Present updated README.md, DSP.md, and final comparison grid for user review. User evaluates:
    1. Comparison grid renders with real PyWavelet (not stub) at selected DPI
    2. README Performance section layout with centered hero image
    3. Scaffold captions are reasonable starting points for user authoring
    4. Timing bar chart appears in DSP.md, not README.md
  </action>
  <verify>User provides approval or specific issues to address</verify>
  <done>User has approved the final README layout, comparison grid quality, and DSP.md timing placement</done>
  <what-built>
    Final comparison grid with real PyWavelet rendering, README.md with hero figure and scaffold captions, timing bar chart relocated to DSP.md.
  </what-built>
  <how-to-verify>
    1. Open `assets/images/benchmarks/comparison_grid.png` — verify all 3 signal columns render correctly with real PyWavelet (not stub)
    2. Preview README.md in a markdown viewer — verify:
       - Comparison grid displays centered at ~80% width in Performance section
       - Per-signal sections have scaffold captions (not REWRITE markers)
       - Timing bar chart is gone from README
       - Benchmark section links to DSP.md
    3. Preview DSP.md — verify timing bar chart appears in the computational cost section
    4. Note any scaffold caption wording you want to adjust
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- README.md has one hero comparison grid image, no per-signal individual figures
- Timing bar chart appears in DSP.md, not README.md
- All PLACEHOLDER markers for comparison grid are resolved
- REWRITE markers in per-signal sections replaced with WRITE scaffold markers
- Final comparison_grid.png rendered with real PyWavelet at user-selected DPI
</verification>

<success_criteria>
- README Performance section is a clean landing page with one comparison grid figure
- Per-signal sections have scaffold captions ready for user authoring
- Timing analysis content lives in DSP.md
- User has approved the final layout
</success_criteria>

<output>
After completion, create `.planning/phases/06-finalize-example-audio-and-comparison-figures-for-readme/06-02-SUMMARY.md`
</output>
