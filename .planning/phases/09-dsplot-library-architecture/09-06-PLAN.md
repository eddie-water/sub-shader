---
phase: 09-dsplot-library-architecture
plan: 06
type: execute
wave: 3
depends_on: [09-04, 09-05]
files_modified:
  - src/subshader/dsp/dsp.ipynb
  - research/tests/dsplot/test_dsplot_example.py
  - research/dsplot/figures/style_override_demo.py
  - research/dsp_figures.py
  - .planning/phases/09-dsplot-library-architecture/style-override-snippet.md
autonomous: false
requirements:
  - PLOT-26  # dsp.ipynb cell 01 renders §2.4 Figure 1 as a 1×3 mixed Figure per CONTEXT.md D-08 amendment: [StaticPanel(xy projection of A), DynamicPanel(reconstruction order, ~6 frames), DynamicPanel(orthogonality, 5 a' x-sweep frames)]
  - PLOT-27  # Example unit test demonstrates testing-consumer pattern (saves a result PNG)
  - PLOT-28  # Style override pattern documented + working demo for BOTH override modes per D-05: (a) global reassignment affects every figure, (b) local figure override leaves global default untouched
  - PLOT-29  # research/dsp_figures.py retired (either deleted or reduced to a deprecation shim re-exporting from dsplot)

must_haves:
  truths:
    - "src/subshader/dsp/dsp.ipynb cell 01 (currently empty) renders §2.4 Figure 1 (`components_recombine_either_order`) as a 1×3 mixed Figure per the D-08 amendment in CONTEXT.md. Panel 1 (StaticPanel): xy projection of vector a at A=(2.0, 3.0) — the static decomposition into x and y components. Panel 2 (DynamicPanel): reconstruction-order animation, ~6 frames showing that the destination is the same regardless of whether x-component is added before or after the y-component (e.g. origin → +x → +x+y = a; then origin → +y → +y+x = a; same arrival both orders). Panel 3 (DynamicPanel): orthogonality animation, 5 frames where vector a stays anchored at A=(2.0, 3.0) across every frame and sibling vector a' sweeps its x-component through [+2.0, +1.0, 0.0, -1.0, -2.0] while keeping a'.y = 3.0 fixed. The orthogonality beat: a's reference position never changes, and a' demonstrates that y is invariant under x — its y-component stays anchored at 3.0 across every frame even as x crosses zero."
    - "Both DynamicPanels loop with auto-reset (FuncAnimation repeat=True); the StaticPanel renders once and stays put"
    - "Figure.add_panel composes Static + Dynamic + Dynamic side-by-side without panel-type branching (validated post-09-04 user check; smoke notebook Smoke 5 also pinned Dynamic+Interactive on one figure)"
    - "research/tests/dsplot/test_dsplot_example.py imports dsplot, runs a Plottable+Panel composition, and savefig()'s a PNG that the test verifies exists and is non-empty"
    - "A style-override demonstration snippet exists at .planning/phases/09-dsplot-library-architecture/style-override-snippet.md AND a runnable variant at research/dsplot/figures/style_override_demo.py renders THREE side-by-side PNGs proving BOTH override modes per D-05: (1) default_palette.png — baseline; (2) global_override.png — palette globally reassigned, affects all figures; (3) local_override.png — a single figure module overrides its own derived constant without touching the global default"
    - "research/dsp_figures.py is either deleted OR reduced to a small deprecation shim that imports + re-exports the dsplot figures module's render() functions for backwards compat"
  artifacts:
    - path: "src/subshader/dsp/dsp.ipynb"
      provides: "Notebook cell 01 populated with the §2.4 Figure 1 mixed-Panel composition per D-08 (Static + Dynamic reconstruction + Dynamic orthogonality)"
      contains: "DynamicPanel"
    - path: "research/tests/dsplot/test_dsplot_example.py"
      provides: "Example unit test demonstrating dsplot integration"
      contains: "def test_"
    - path: "research/dsplot/figures/style_override_demo.py"
      provides: "Runnable demo of BOTH style override modes per D-05 (global reassignment + local figure override)"
      contains: "PRIMARY_COLOR"
    - path: ".planning/phases/09-dsplot-library-architecture/style-override-snippet.md"
      provides: "Docs-ready snippet showing BOTH override modes per D-05 for downstream consumers"
      contains: "dsplot.style.PRIMARY_COLOR"
    - path: "research/dsp_figures.py"
      provides: "Either deleted or reduced to a deprecation shim importing from research.dsplot.figures"
      contains: "from research.dsplot"
  key_links:
    - from: "src/subshader/dsp/dsp.ipynb"
      to: "research/dsplot/"
      via: "import dsplot in cell 01"
      pattern: "from dsplot"
    - from: "research/dsp_figures.py"
      to: "research/dsplot/figures/"
      via: "re-export render functions for backwards compat (if shim chosen over deletion)"
      pattern: "from research.dsplot.figures"
---

<objective>
Land the last three verification targets from CONTEXT.md: notebook demo (#3), example unit test (#4), style-override demonstration (#5 — both global and local modes per D-05), and retire research/dsp_figures.py (#6). After this plan, all 6 verification targets are satisfied and Phase 9 is complete.

Purpose: The notebook cell 01 is the highest-visibility deliverable — live demo of the dynamic-panel architecture inside a Jupyter consumer. The example test is the "this is how downstream code uses dsplot" stub. Style override demo proves the locked D-05 override pattern in BOTH modes:
  (a) **global reassignment** — `dsplot.style.PRIMARY_COLOR = "#new"` affects every figure rendered after.
  (b) **local figure override** — a figure module reassigns its OWN derived constant (e.g. `LABEL_RATIO = some_other_value`) without touching the global default, so other figures keep the inherited template.
Retiring dsp_figures.py closes the migration loop.
Output: Notebook demo working, example test passing, style override demo producing THREE visually distinct PNGs (baseline + global-mode + local-mode), old module retired to shim form.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-dsplot-library-architecture/09-CONTEXT.md
@.planning/phases/09-dsplot-library-architecture/09-01-PLAN.md
@.planning/phases/09-dsplot-library-architecture/09-02-PLAN.md
@.planning/phases/09-dsplot-library-architecture/09-03-PLAN.md
@.planning/phases/09-dsplot-library-architecture/09-04-PLAN.md
@.planning/phases/09-dsplot-library-architecture/09-05-PLAN.md
@src/subshader/dsp/dsp.ipynb
@src/subshader/dsp/DSP.md
@research/dsp_figures.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement notebook cell 01 — figure 1 as DynamicPanel with orthogonality morphing</name>
  <files>
    src/subshader/dsp/dsp.ipynb
  </files>
  <read_first>
    - src/subshader/dsp/dsp.ipynb (current state: 1 markdown cell + 1 empty code cell with id "7ab635e8")
    - src/subshader/dsp/DSP.md lines 120-145 (the markdown section the notebook mirrors)
    - research/dsplot/figures/components_recombine.py (static figure 1 reference — panel 3 specifically morphs)
    - research/dsplot/figures/foundation_constants.py (A, A_PRIME, FOUND_LIM)
    - research/dsplot/panels/dynamic_panel.py (DynamicPanel API)
  </read_first>
  <action>
    Per CONTEXT.md verification target #3 + the morphing-orthogonality example from <specifics>: vector a is the FIXED REFERENCE (anchored across all frames); sibling a' SWEEPS its x-component through 5 values [+2.0, +1.0, 0.0, -1.0, -2.0] while keeping y = 3.0 invariant. The visual story is "y measurement is unaffected by changing x" — a' moves left-right while its height stays put, and the static a anchors the reference frame so the viewer's eye has a fixed point to compare against.

    1. Read the existing notebook structure using nbformat: 1 markdown cell (id "4e7d6776") + 1 empty code cell (id "7ab635e8").

    2. Use the nbformat library to populate the empty code cell:
       - Sys.path plumbing so `dsplot` is importable from the notebook's launch context (cwd is typically project root or src/subshader/dsp/; resolve relative to `os.getcwd()` or check both).
       - `%matplotlib widget` magic for interactive matplotlib; comment that `%matplotlib inline` is the fallback (animation will not loop live but the figure will render frame 0).
       - Imports: `numpy as np`, `matplotlib.pyplot as plt`, and from dsplot: `Figure`, `StaticPanel`, `DynamicPanel`, `Vector`, `VectorComponents`, `Annotation`, `style`.
       - Frame-builder function: `a` is a FIXED reference vector at `A = (2.0, 3.0)` rendered identically on EVERY frame. For each of 5 frame x-values `A_PRIME_X_SWEEP = [2.0, 1.0, 0.0, -1.0, -2.0]` (positive, smaller positive, zero, smaller negative, negative), build a list of Plottables containing both the static `a = A` and the morphing `a_prime = (a_prime_x, 3.0)`. Each rendered with VectorComponents (neutral, dashed) plus a bold orange Vector with labels "a" and "a′". Y stays fixed at 3.0 across all frames for BOTH vectors. (All 2-tuples — per D-02 polymorphic Vector renders 2D automatically.)
       - Build the figure:
         - `Figure(n_rows=1, n_cols=1, figsize=(7, 7), suptitle="Orthogonality: y is invariant under changes in x")`
         - `DynamicPanel(frames=frames, interval_ms=600, repeat=True, lim=FOUND_LIM, axis_style="arrow", axis_labels=True, show_border=False, title="a stays anchored; a' sweeps x", subtitle="a'.y stays at 3 across every frame — y is independent of x")`
         - `fig.add_panel(panel, row=0, col=0); fig.render(); plt.show()`
       - Optional GIF export (separate cell or commented in same cell): `panel.save_gif("../../../assets/images/dsp/dsp_figure1_orthogonality_animation.gif", fps=2)`. Comment-out by default; user uncomments to export.

    3. Write the notebook back via nbformat (preserves cell structure better than hand-editing JSON):
       - `nbformat.read(path, as_version=4)`
       - Find the cell where `cell.get("id") == "7ab635e8"`
       - Set `cell.source = "..."` (the multi-line source string above)
       - Set `cell.metadata = {}` (drop the vscode `languageId: plaintext` flag that marked the cell as non-python)
       - Set `cell.execution_count = None`, `cell.outputs = []`
       - `nbformat.write(nb, path)`

    4. DO NOT execute the cell as part of this task. Kernel state pollution is fragile and the human-verification checkpoint (task 4) confirms live animation behavior.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -c "import nbformat; nb = nbformat.read('src/subshader/dsp/dsp.ipynb', as_version=4); cells = [c for c in nb.cells if c.get('id') == '7ab635e8']; assert len(cells) == 1; src = cells[0].source; assert 'DynamicPanel' in src; assert 'Vector' in src; assert 'dsplot' in src; assert cells[0].cell_type == 'code'; print('notebook cell 01 populated correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/subshader/dsp/dsp.ipynb` cell with id "7ab635e8" populated with dsplot import, frame definitions, DynamicPanel construction, Figure render
    - Cell source references: `DynamicPanel`, `Vector`, `VectorComponents`, `dsplot`
    - Cell metadata cleaned up (vscode languageId flag removed)
    - Cell type is "code", execution_count=None, outputs=[]
    - The previous markdown cell (id "4e7d6776") is left UNTOUCHED
    - Notebook still parses as valid nbformat v4
  </acceptance_criteria>
  <done>Notebook cell 01 populated with the figure-1 morphing animation code; ready for human live-animation verification in checkpoint task 4</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write example unit test + style-override demo (BOTH modes per D-05) + docs snippet</name>
  <files>
    research/tests/dsplot/test_dsplot_example.py
    research/dsplot/figures/style_override_demo.py
    .planning/phases/09-dsplot-library-architecture/style-override-snippet.md
  </files>
  <read_first>
    - research/tests/dsplot/test_figure_orchestrator.py (existing pattern for tmp_path + Figure + savefig)
    - research/dsplot/__init__.py (current exports)
    - research/dsplot/figures/vector_basics.py (simple figure to model the demo after)
    - research/dsplot/figures/motivator.py (the local-override pattern in action — LABEL_RATIO / HSPACE derived from style.DEFAULT_* per D-05)
    - CONTEXT.md <verification_target> items #4 and #5 (updated per D-05 to require BOTH override modes)
  </read_first>
  <behavior>
    - Test in test_dsplot_example.py: A single test function. Imports `dsplot`, builds a small Figure + StaticPanel + Vector composition, calls `fig.savefig(tmp_path / ...)`, asserts the file exists and size > 0. The test name and docstring make the teaching intent explicit.
    - style_override_demo: When run as `python -m research.dsplot.figures.style_override_demo`, produces THREE PNGs in `assets/images/dsp/style_override_demo/`:
      - `default_palette.png` — baseline with the inherited template (D-05).
      - `global_override.png` — `dsplot.style.PRIMARY_COLOR` reassigned (mode 1: global reassignment affects this figure AND would affect every other figure rendered after if we hadn't restored in try/finally).
      - `local_override.png` — a figure-local constant (e.g. `MY_PRIMARY = "#some_other_color"`) overrides the figure's color WITHOUT touching `dsplot.style.PRIMARY_COLOR` (mode 2: local override leaves global default intact).
      Plus an inline assertion in main() that after the demo runs, `dsplot.style.PRIMARY_COLOR` equals the ORIGINAL value (proves the global reassignment was reversed AND the local override never touched it).
    - style-override-snippet.md: A 25-40 line markdown file showing BOTH override modes with copy-pasteable snippets + explanation of when to use each (per D-05).
  </behavior>
  <action>
    1. Create `research/tests/dsplot/test_dsplot_example.py` — a single small test:
       - Module docstring states explicitly: "teaching artifact, not coverage; downstream test-suite authors should follow this shape."
       - `matplotlib.use("Agg")` at top.
       - One test function `test_dsplot_example_composition(tmp_path)` that creates a `Figure`, adds a `StaticPanel` with `axis_style="arrow"` and a `Vector((1.5, 1.0), label="a")`, renders, savefigs to `tmp_path / "example_vector_figure.png"`, closes, asserts file exists and `stat().st_size > 0`.
       - Total length: ~30 lines including imports + docstring. No fixtures, no parametrization.

    2. Create `research/dsplot/figures/style_override_demo.py` — demonstrates BOTH D-05 override modes:
       - Module docstring: how to run, what it produces, why it proves BOTH override modes from D-05.
       - `OUT_DIR` resolves to `<repo>/assets/images/dsp/style_override_demo/` via `os.path.dirname(__file__)` + relative ascent.
       - `_render_one(filename, primary_color, secondary_color, tertiary_color)` is a pure renderer: builds a 1-row 3-col Figure where each StaticPanel renders a `Vector(color=primary/secondary/tertiary)`. The colors come from the function args, NOT from `dsplot.style` directly — this is how a figure module would normally take its colors from `style.PRIMARY_COLOR` etc., but we parameterize to make the override comparison explicit.
       - `_render_global_default(filename)`: calls `_render_one(filename, dsplot.style.PRIMARY_COLOR, dsplot.style.SECONDARY_COLOR, dsplot.style.TERTIARY_COLOR)` — uses the CURRENT global values. This is what figure modules normally do when they take colors from the inheritable template.
       - `main()`:
         ```python
         # ------ Baseline ------
         _render_global_default("default_palette.png")
         original_primary = dsplot.style.PRIMARY_COLOR
         original_secondary = dsplot.style.SECONDARY_COLOR
         original_tertiary = dsplot.style.TERTIARY_COLOR

         try:
             # ------ Mode 1: GLOBAL reassignment (per D-05) ------
             # Reassign the dsplot.style constants. Every figure rendered after this
             # picks up the new values — this is the "affects all figures" mode.
             dsplot.style.PRIMARY_COLOR   = "#22c55e"  # green
             dsplot.style.SECONDARY_COLOR = "#ec4899"  # pink
             dsplot.style.TERTIARY_COLOR  = "#06b6d4"  # cyan
             _render_global_default("global_override.png")  # uses NEW values via dsplot.style.*

             # Restore originals before the local-override demo
             dsplot.style.PRIMARY_COLOR   = original_primary
             dsplot.style.SECONDARY_COLOR = original_secondary
             dsplot.style.TERTIARY_COLOR  = original_tertiary

             # ------ Mode 2: LOCAL figure override (per D-05) ------
             # Define module-local constants for THIS figure only. Other figures
             # still inherit dsplot.style.* untouched. This is how motivator.py
             # overrides LABEL_RATIO without affecting other figures.
             MY_PRIMARY   = "#facc15"  # yellow — local choice for this figure
             MY_SECONDARY = "#a855f7"  # purple
             MY_TERTIARY  = "#f97316"  # orange
             _render_one("local_override.png", MY_PRIMARY, MY_SECONDARY, MY_TERTIARY)

             # Sanity assertion: global defaults UNCHANGED by the local override
             assert dsplot.style.PRIMARY_COLOR == original_primary, \
                 "Local override should NOT mutate dsplot.style.PRIMARY_COLOR (D-05 violation)"
         finally:
             # Belt-and-braces: ensure originals restored even if local mode raised
             dsplot.style.PRIMARY_COLOR   = original_primary
             dsplot.style.SECONDARY_COLOR = original_secondary
             dsplot.style.TERTIARY_COLOR  = original_tertiary

         print(f"  Saved -> {os.path.join(OUT_DIR, 'default_palette.png')}")
         print(f"  Saved -> {os.path.join(OUT_DIR, 'global_override.png')}  (mode 1: global reassignment, affects all)")
         print(f"  Saved -> {os.path.join(OUT_DIR, 'local_override.png')}    (mode 2: local-only, global untouched)")
         ```
       - `if __name__ == "__main__": main()`.

    3. Create `.planning/phases/09-dsplot-library-architecture/style-override-snippet.md`:
       - Title: "# dsplot Style Override — Two Modes (D-05)"
       - One-paragraph context: "Every visual default in dsplot lives on `dsplot.style` as an inheritable template. Override either GLOBALLY (affects every figure) or LOCALLY (single figure only) — pick whichever matches your intent."
       - "## Mode 1: Global reassignment" section: code block reassigning `dsplot.style.PRIMARY_COLOR` and building a figure. Explanation: "Affects every figure rendered after the reassignment. Use this for theming the entire library output (e.g. light-mode vs dark-mode swap, brand palette substitution)."
       - "## Mode 2: Local figure override" section: code block showing a figure module defining its own constants derived from `style.DEFAULT_*`. Explanation: "The figure module reassigns its OWN module-level constant. Other figures still inherit the global default. Use this when one figure needs to differ from the template (e.g. motivator.py's LABEL_RATIO)."
       - "## Why this works" section: ~3 sentences on lazy draw-time lookup; mention that Plottables constructed BEFORE a global reassignment still pick up the new color at render time (lazy lookup), while local overrides are static at the figure-module scope.
       - Pointer to `research/dsplot/figures/style_override_demo.py` as the runnable side-by-side example producing 3 PNGs.
       - Total length: 30-50 lines markdown.

    4. Do NOT add style_override_demo to `research/dsplot/figures/__main__.py` — keep the main dispatcher focused on canonical DSP.md figures. Document in 09-06-SUMMARY that the demo lives at its own module entry point.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -m pytest research/tests/dsplot/test_dsplot_example.py -x -q && python -m research.dsplot.figures.style_override_demo && test -f assets/images/dsp/style_override_demo/default_palette.png && test -f assets/images/dsp/style_override_demo/global_override.png && test -f assets/images/dsp/style_override_demo/local_override.png && test -f .planning/phases/09-dsplot-library-architecture/style-override-snippet.md && python -c "import dsplot; assert dsplot.style.PRIMARY_COLOR == '#e1641a', 'style override demo failed to restore global default'" && echo "all 3 PNGs + snippet present, global defaults restored"</automated>
  </verify>
  <acceptance_criteria>
    - `research/tests/dsplot/test_dsplot_example.py` exists, passes pytest, demonstrates integration in ~30 lines
    - `research/dsplot/figures/style_override_demo.py` exists, runs as a module, produces THREE PNGs in `assets/images/dsp/style_override_demo/`:
      - `default_palette.png` — baseline (inherited template)
      - `global_override.png` — mode 1: global reassignment of `dsplot.style.PRIMARY_COLOR` etc.
      - `local_override.png` — mode 2: local module-level constants (no `dsplot.style.*` mutation)
    - The demo's `main()` asserts `dsplot.style.PRIMARY_COLOR == original` after the local-mode block, proving local override never touched the global
    - Final smoke check: importing dsplot after running the demo shows `dsplot.style.PRIMARY_COLOR == "#e1641a"` (default) — global state restored via try/finally
    - `.planning/phases/09-dsplot-library-architecture/style-override-snippet.md` exists with copy-pasteable code for BOTH override modes + explanation of when to use each
    - Style override demo restores original palette in a try/finally (no test pollution if imported)
    - Zero subshader imports in any of the new files (test_dsplot_example.py is in research/tests/ — same boundary as figures/ per D-01; the demo is in research/dsplot/figures/ — also consumer code)
  </acceptance_criteria>
  <done>Verification targets #4 (example test) and #5 (style override demo for BOTH D-05 modes) satisfied; docs-ready snippet shows BOTH override patterns for downstream consumer use</done>
</task>

<task type="auto">
  <name>Task 3: Retire research/dsp_figures.py — deprecation shim with backwards-compat re-exports</name>
  <files>
    research/dsp_figures.py
  </files>
  <read_first>
    - research/dsp_figures.py (current state — 1546 lines, the module being retired)
    - research/dsplot/figures/__main__.py (knows the canonical render function names)
    - research/dsplot/figures/foundation_constants.py
  </read_first>
  <action>
    Per CONTEXT.md verification target #6: "research/dsp_figures.py is either deleted or reduced to a deprecation shim that re-exports from dsplot for backwards compat during the transition."

    Pick the SHIM approach over deletion. Rationale: a ~30-line shim is safer than deletion — if any in-tree caller, notebook reference, or stale doc link exists, the shim keeps them working while emitting a one-line DeprecationWarning. Also leaves a breadcrumb for the user about the migration.

    1. Scan for callers in the repo BEFORE rewriting:
       Run: `grep -rn "from research.dsp_figures\|from dsp_figures\|import dsp_figures\|generate_all_dsp_figures\|generate_motivator_versions\|generate_foundations_figures\|generate_alignment_diagnostic" . --include="*.py" --include="*.ipynb" --include="*.md" --exclude-dir=.planning --exclude-dir=.git --exclude-dir=venv --exclude-dir=__pycache__`

       Record the hits. The shim's re-export list MUST cover every top-level name any caller uses. If a caller imports a private helper (`_plot_sharp_vector`, etc.), that contract is already broken — don't honor it; let those callers update.

    2. Replace the entire contents of `research/dsp_figures.py` with a deprecation shim. The shim:
       - Module docstring: "DEPRECATED — use research/dsplot/figures/ instead" + a migration map (old name → new path).
       - Single `warnings.warn(..., DeprecationWarning, stacklevel=2)` call at module top so importing the module triggers the warning once.
       - Re-export the canonical constants: `from research.dsplot.figures.foundation_constants import A, A_PRIME, B, A_Z, FOUND_LIM`
       - Re-export the canonical render functions under their OLD names via `as` aliases:
         - `from research.dsplot.figures.motivator import render_all as generate_motivator_versions, MOTIVATOR_VERSIONS, MotivatorVersion as ChirpFigureConfig`
         - `from research.dsplot.figures.alignment_diagnostic import render as generate_alignment_diagnostic`
       - Define a `generate_all_dsp_figures()` function that imports and calls `research.dsplot.figures.__main__.main()`.
       - `if __name__ == "__main__": generate_all_dsp_figures()` so legacy `python research/dsp_figures.py` invocation still works.
       - Target shim size: under 60 lines total.

    3. Re-run the caller grep to confirm every caller resolves to one of the re-exports. Add any missing aliases.

    4. Run the dsplot test suite: `python -m pytest research/tests/dsplot/ -x -q` — should still pass (no existing test imports from dsp_figures).

    5. Run the legacy command to confirm the shim works: `python research/dsp_figures.py`. Expected: DeprecationWarning prints, then all 10 figures regenerate via the dsplot dispatcher.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test $(wc -l < research/dsp_figures.py) -lt 70 && python -W error::DeprecationWarning -c "from research.dsp_figures import A" 2>&1 | grep -q "DeprecationWarning\|deprecated" && python -m pytest research/tests/dsplot/ -x -q && echo "shim retired OK"</automated>
  </verify>
  <acceptance_criteria>
    - `research/dsp_figures.py` is under 70 lines
    - Importing from `research.dsp_figures` triggers a DeprecationWarning
    - All canonical top-level names from the old module are still importable (A, A_PRIME, B, A_Z, FOUND_LIM, generate_all_dsp_figures, generate_motivator_versions, generate_alignment_diagnostic, MOTIVATOR_VERSIONS, ChirpFigureConfig)
    - `python research/dsp_figures.py` still regenerates all DSP.md figures (via delegation to the new dispatcher)
    - All existing pytest cases still pass
    - In-tree caller grep documented in 09-06-SUMMARY (caller list + whether shim covers each)
  </acceptance_criteria>
  <done>research/dsp_figures.py retired to deprecation shim form per verification target #6; legacy callers and the legacy command still work; migration map documented in the shim's docstring</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end verification of all 6 CONTEXT.md verification targets</name>
  <what-built>
    Phase 9 is functionally complete. This checkpoint runs every CONTEXT.md verification target and confirms each one passes from the user's perspective. After this checkpoint, the phase ships.
  </what-built>
  <how-to-verify>
    Run each step in order. Each maps to one of the 6 verification targets in CONTEXT.md <verification_target>.

    ## Target #1: Module isolation (per D-01)
    ```bash
    cd /home/eddie-water/dev/python/sub-shader
    grep -rIn "from subshader\|import subshader" research/dsplot/ --exclude-dir=figures
    ```
    Expected: zero output lines. The figures/ subdir is excluded per the documented library/consumer boundary (D-01 in CONTEXT.md).

    ## Target #2: Figure parity
    ```bash
    python -m research.dsplot.figures
    ```
    Expected: 10 "Saved -> ..." lines, no errors. Then visually inspect each PNG in `assets/images/dsp/`:
    - vector_basics.png, dot_product_geometry.png, components_recombine_either_order_v18.png, projection_reconstruction_either_order_v9.png, vector_projection_3d_v2_combo5_palette.png, vector_xy_reconstruction.png — all foundation figures match or improve on the prior renders (already inspected in 09-05 checkpoint). vector_xy_reconstruction.png is now reproducible (no longer orphaned) per D-03.
    - dsp_motivator_{v4,v5,vw1_gentle,vw2_moderate,vw3_aggressive,vw4_aggressive}*.png — all motivator versions render correctly with the 3-row layout (TimeSeries + STFT + CWT) and row labels. Layout constants derive from style.DEFAULT_* per D-05.
    - dsp_alignment_diagnostic.png — burst markers visible at correct frequencies and time.

    Confirm: every PNG referenced in DSP.md still exists and renders correctly when DSP.md is opened in a markdown viewer.

    ## Target #3: Notebook demo
    Open `src/subshader/dsp/dsp.ipynb` in Jupyter. Run cell 01 (the previously empty cell, now populated).

    Confirm: A figure appears with the title "Orthogonality: y is invariant under changes in x". Vector `a` stays ANCHORED at (2, 3) across every frame — does not move at all. Sibling vector `a'` sweeps its x-component through 5 values: +2, +1, 0, -1, -2 (one per frame). `a'`'s y-component stays at 3 throughout — the tip moves horizontally at the same height across every frame. The animation loops with auto-reset (after frame 4, returns to frame 0 and starts over).

    If `%matplotlib widget` fails to import (no jupyterlab-widgets or no widget backend), switch to `%matplotlib inline` — the animation may not loop live, but frame 0 should still render correctly. If you want to verify the looping behavior, uncomment the `panel.save_gif(...)` line and open the resulting GIF.

    ## Target #4: Test integration
    ```bash
    python -m pytest research/tests/dsplot/ -v
    ```
    Expected: all tests pass, including `test_dsplot_example_composition`. Open `research/tests/dsplot/test_dsplot_example.py` and confirm the docstring + structure read as a teaching artifact (clear, minimal, shows the integration shape).

    ## Target #5: Style override works — BOTH modes per D-05
    ```bash
    python -m research.dsplot.figures.style_override_demo
    ```
    Expected: prints THREE output paths. Open each PNG:
    - `assets/images/dsp/style_override_demo/default_palette.png` — vectors in orange / purple / gold (baseline inherited template).
    - `assets/images/dsp/style_override_demo/global_override.png` — vectors in green / pink / cyan (mode 1: GLOBAL reassignment of `dsplot.style.PRIMARY_COLOR` etc.).
    - `assets/images/dsp/style_override_demo/local_override.png` — vectors in yellow / purple / orange (mode 2: LOCAL module-level constants, `dsplot.style.*` untouched).

    Confirm:
    1. Three visually distinct palettes across the three PNGs.
    2. The terminal output mentions "mode 1: global reassignment, affects all" and "mode 2: local-only, global untouched".
    3. After running the demo: `python -c "import dsplot; print(dsplot.style.PRIMARY_COLOR)"` prints `#e1641a` (the ORIGINAL default — proves the global reassignment was correctly reversed AND the local mode never touched it).

    Cross-reference `.planning/phases/09-dsplot-library-architecture/style-override-snippet.md` and confirm BOTH modes are documented with snippets and the "when to use each" guidance.

    ## Target #6: Old module retired
    ```bash
    wc -l research/dsp_figures.py
    python -W error::DeprecationWarning -c "from research.dsp_figures import A; print('A =', A)"
    ```
    Expected: line count < 70; the import command exits non-zero because `-W error::DeprecationWarning` turns the warning into an error (proves the warning fires); then re-run without `-W error` to confirm `A = (2.0, 3.0)` prints correctly.

    Then run the legacy command:
    ```bash
    python research/dsp_figures.py
    ```
    Expected: DeprecationWarning prints once, then all figures regenerate (delegates to dsplot dispatcher).

    ## Final
    Report back the result of each of the 6 targets. If all pass, the phase ships. If any fail, list which target and what failed; the executor will fix and re-checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved — phase 9 complete" if all 6 targets verify. Otherwise list which target(s) failed and what went wrong.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rIn "from subshader\|import subshader" research/dsplot/ --exclude-dir=figures` returns zero lines (target #1, per D-01)
- `python -m research.dsplot.figures` regenerates all 10 canonical PNGs (target #2)
- `src/subshader/dsp/dsp.ipynb` cell 01 contains DynamicPanel + Vector + dsplot imports; nbformat-valid (target #3 — live animation verified in checkpoint)
- `python -m pytest research/tests/dsplot/` passes including `test_dsplot_example_composition` (target #4)
- `python -m research.dsplot.figures.style_override_demo` produces THREE PNGs with visibly different palettes — baseline, global-override, local-override (target #5, BOTH D-05 modes)
- After demo runs, `dsplot.style.PRIMARY_COLOR` is back to the default value (proves global was restored AND local mode never touched it)
- `wc -l research/dsp_figures.py` < 70 AND `python -W error::DeprecationWarning -c "from research.dsp_figures import A"` exits non-zero (target #6)
- All 6 verification targets confirmed by the human checkpoint
</verification>

<success_criteria>
All 6 CONTEXT.md verification targets met. Phase 9 ships:
- Library isolated from subshader (literal grep clean within the library boundary per D-01)
- All dsp_figures.py outputs reproducible from dsplot at the same filenames DSP.md points to (including vector_xy_reconstruction.png regenerated per D-03)
- Notebook cell 01 demos the locked figure-1 morphing animation with orthogonality beat
- Example test demonstrates the integration pattern in a single small function
- Style override pattern works end-to-end for BOTH D-05 modes — global reassignment (3-PNG demo) + local figure override (demonstrated in demo AND in production via motivator.py's local layout constants from 09-05) + docs snippet + runnable demo
- Old module retired to a small backwards-compat shim with DeprecationWarning
</success_criteria>

<risks>
- **Notebook execution environment unknowns:** %matplotlib widget requires jupyterlab-widgets (JupyterLab) or ipympl (notebook). If neither is installed, the animation fall-back is %matplotlib inline — frame 0 renders but no loop. The user_setup in 09-04 captures this dependency; the checkpoint instructions repeat the fallback so this doesn't block the phase.
- **Caller scan completeness:** The grep in task 3 step 1 looks for common patterns. If a caller uses unusual aliases (e.g. `from research import dsp_figures as df; df.foo()`), the grep may miss it. Acceptable: the DeprecationWarning will catch any runtime caller; a missed callsite means the FIRST import triggers a warning, not a silent break.
- **Style override demo + test pollution:** The demo reassigns `dsplot.style.PRIMARY_COLOR` (and friends) inside `main()` and restores in a try/finally. If a test imports `style_override_demo` and the import path executes the module body, that's fine — only `main()` reassigns. If a test calls `main()` directly without the try/finally completing (e.g. KeyboardInterrupt), other tests may see the wrong palette. Mitigation: the test in test_dsplot_example.py does NOT touch style overrides; the demo is only invoked from the CLI checkpoint. The verification step explicitly re-checks `dsplot.style.PRIMARY_COLOR` after the demo run.
- **D-05 local-override demo clarity:** Mode 2 (local override) is the subtler concept. The demo makes it concrete by defining local `MY_PRIMARY` etc. and passing them explicitly to `_render_one`. The risk is that a reader confuses this with mode 1 (global). The terminal output explicitly labels each PNG with "(mode 1: global)" / "(mode 2: local)" and the snippet docs spell out the difference. Belt-and-braces: the assertion in main() proves `dsplot.style.PRIMARY_COLOR` is unchanged after the local block — that's the load-bearing distinction.
- **Visual subjectivity at the checkpoint:** "Visually distinct palettes" across 3 PNGs is a judgment call. The hex deltas are large enough that distinctness is obvious — but if the user's monitor calibration is unusual, push back to the executor to pick higher-contrast colors. Not expected to be an issue.
- **DSP.md alt-text drift if foundation constants change:** Phase 9 does NOT change A, A_PRIME, B, A_Z, FOUND_LIM — they're copied verbatim from dsp_figures.py. DSP.md alt-text and LaTeX blocks remain valid. Future phases that change these constants MUST update DSP.md in lockstep; the migration map in the deprecation shim and the docstring in foundation_constants.py both flag this.
</risks>

<output>
After completion, create `.planning/phases/09-dsplot-library-architecture/09-06-SUMMARY.md` summarizing: the notebook cell 01 implementation choices (matplotlib backend, frame count, GIF export), the example test shape (teaching artifact, single function), the style override demo behavior for BOTH D-05 modes (global reassignment + local figure override, with the 3-PNG output), the caller-grep result for dsp_figures.py + which aliases the shim ended up exporting, the deprecation shim's final size, and the result of the 6-target verification checkpoint. This SUMMARY also serves as the Phase 9 retrospective hand-off — note any lessons (mixed-type composition working or fallback, twin-axis escape hatch, library/consumer boundary D-01, polymorphic Vector D-02, 3D Axes accommodations D-04, template-with-override pattern D-05).
</output>
