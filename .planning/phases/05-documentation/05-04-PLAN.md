---
phase: 05-documentation
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - research/benchmark.py
  - DSP.md
  - assets/images/dsp/dot_product_geometry.png
  - assets/images/dsp/vector_similarity.png
  - assets/images/dsp/basis_decomposition.png
  - assets/images/dsp/sign_accumulation.png
  - assets/images/dsp/fourier_basis.png
  - assets/images/dsp/stft_windowing.png
  - assets/images/dsp/wavelet_scaling.png
autonomous: true
requirements: [DOCS-02, DOCS-05]

must_haves:
  truths:
    - "benchmark.py has a --foundations-figures flag that generates all 7 DSP foundation figures"
    - "All 7 PLACEHOLDER markers in DSP.md are replaced with live image paths to existing PNG files"
    - "Generated figures are pedagogical visualizations matching the descriptions in DSP.md placeholders"
  artifacts:
    - path: "research/benchmark.py"
      provides: "--foundations-figures flag and generate_foundations_figures() function"
      contains: "--foundations-figures"
    - path: "assets/images/dsp/dot_product_geometry.png"
      provides: "2D vector dot product geometric interpretation figure"
    - path: "assets/images/dsp/vector_similarity.png"
      provides: "Parallel/perpendicular vector pairs figure"
    - path: "assets/images/dsp/basis_decomposition.png"
      provides: "Basis decomposition of 2D vector figure"
    - path: "assets/images/dsp/sign_accumulation.png"
      provides: "Color-coded sign accumulation figure"
    - path: "assets/images/dsp/fourier_basis.png"
      provides: "Fourier basis functions figure"
    - path: "assets/images/dsp/stft_windowing.png"
      provides: "STFT windowing illustration figure"
    - path: "assets/images/dsp/wavelet_scaling.png"
      provides: "Wavelet scaling at different frequencies figure"
    - path: "DSP.md"
      provides: "Updated DSP scaffold with live image paths replacing all PLACEHOLDERs"
  key_links:
    - from: "research/benchmark.py"
      to: "assets/images/dsp/"
      via: "--foundations-figures flag generates all 7 PNGs"
      pattern: "assets/images/dsp"
    - from: "DSP.md"
      to: "assets/images/dsp/"
      via: "Live markdown image references replacing PLACEHOLDER markers"
      pattern: "assets/images/dsp/.*\\.png"
---

<objective>
Add a `--foundations-figures` flag to benchmark.py that generates the 7 missing DSP.md pedagogical figures, then replace DSP.md PLACEHOLDER markers with live image paths.

Purpose: DSP.md has 7 `[PLACEHOLDER:]` markers for foundation figures that block the document from being visually complete. The notebook (wavelet_foundations.ipynb) and its plot_vectors.py helper have working matplotlib figure cells that can be ported. New figures (Fourier basis, STFT windowing, wavelet scaling) need implementation.
Output: 7 PNG files in assets/images/dsp/, updated benchmark.py with --foundations-figures flag, DSP.md with live image references.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

@research/benchmark.py
@research/docs/demo/readmes/wavelet/wavelet_foundations.ipynb
@research/docs/demo/readmes/wavelet/plot_vectors.py
@DSP.md

<interfaces>
<!-- Existing benchmark.py patterns the executor must follow -->

From research/benchmark.py (argparse section, lines 969-985):
```python
parser = argparse.ArgumentParser(...)
parser.add_argument("--comparison-grid", action="store_true", help="Generate 3x3 comparison grid (signals x representations)")
# New flag goes here: --foundations-figures
```

From research/benchmark.py (dispatch section, lines 1021-1022):
```python
if args.comparison_grid:
    modes.append(("Comparison Grid", lambda sp=args.stub_pywt: generate_comparison_grid(stub_pywt=sp)))
# New dispatch goes here
```

From research/benchmark.py (function pattern, line 650):
```python
def generate_comparison_grid(stub_pywt: bool = False):
    """..."""
    os.makedirs(BENCHMARKS_DIR, exist_ok=True)
    # ... figure generation ...
    plt.savefig(output_path, dpi=200, bbox_inches='tight')
```

From research/docs/demo/readmes/wavelet/plot_vectors.py:
```python
def plot_vectors(vectors, filename):
    """Renders 2D vector grid with quiver arrows, axis lines, gridspec layout."""
    # Uses: np.array vectors, matplotlib quiver, gridspec
    # Saves to: ./figures/{filename}.png at 150 DPI
```

DSP.md PLACEHOLDER markers (7 total, lines 65, 79, 101, 115, 157, 179, 229):
- Line 65: "2D vector dot product geometric interpretation..."
- Line 79: "Parallel same/opposite and perpendicular vector pairs..."
- Line 101: "Basis decomposition of a 2D vector into x and y components..."
- Line 115: "Color-coded element products showing sign accumulation..."
- Line 157: "Fourier basis functions..."
- Line 179: "STFT windowing illustration..."
- Line 229: "Wavelet scaling at different frequencies..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --foundations-figures flag to benchmark.py with 7 figure generators</name>
  <files>research/benchmark.py</files>
  <read_first>
    - research/benchmark.py (FULL file — understand the generate_comparison_grid() pattern at line 650, argparse at line 969, dispatch at line 1021, imports at top, BENCHMARKS_DIR constant usage, matplotlib Agg backend setup)
    - research/docs/demo/readmes/wavelet/plot_vectors.py (FULL — the plot_vectors() function uses quiver arrows, gridspec, steelblue color, dpi=150; port this style into benchmark.py)
    - research/docs/demo/readmes/wavelet/wavelet_foundations.ipynb (cells with vector plotting code — cell 75bbabaa uses plot_vectors for parallel/perpendicular, cell 23558581 and c1d6a492 have standalone vector plots with quiver)
    - research/utilities/constants.py (BENCHMARKS_DIR path constant — need a new DSP_FIGURES_DIR constant or inline path)
    - DSP.md (lines 65, 79, 101, 115, 157, 179, 229 — read exact PLACEHOLDER descriptions so figures match expectations)
  </read_first>
  <action>
    Add a `generate_foundations_figures()` function and `--foundations-figures` argparse flag to benchmark.py. Follow the exact same pattern as `generate_comparison_grid()`.

    **Step 1: Add DSP_FIGURES_DIR path constant** near the top of benchmark.py where BENCHMARKS_DIR is defined:
    ```python
    DSP_FIGURES_DIR = os.path.join(os.path.dirname(__file__), "..", "assets", "images", "dsp")
    ```
    Or if BENCHMARKS_DIR uses a constants import pattern, follow that same pattern. The output directory is `assets/images/dsp/`.

    **Step 2: Create `generate_foundations_figures()` function** (place it after the comparison grid section, before the argparse section). This function generates all 7 figures:

    **Figure 1: dot_product_geometry.png** (DSP.md line 65)
    Port from notebook cell 75bbabaa pattern. Show 3 pairs of 2D vectors demonstrating dot product outcomes:
    - Pair 1: parallel same direction (e.g., [2,1] and [1,0.5]) — label "Positive (similar)"
    - Pair 2: parallel opposite direction (e.g., [2,1] and [-2,-1]) — label "Negative (opposite)"
    - Pair 3: perpendicular (e.g., [1,0] and [0,1]) — label "Zero (unrelated)"
    Use 1x3 subplot layout. Each subplot: quiver arrows from origin, axis crosshairs, minimal ticks, steelblue color for vectors. Show dot product value as annotation on each subplot.
    Save to `DSP_FIGURES_DIR / "dot_product_geometry.png"` at 150 DPI.

    **Figure 2: vector_similarity.png** (DSP.md line 79)
    Expand on figure 1 with 6 panels (2x3 grid):
    - Row 1: parallel same, parallel opposite, perpendicular (repeat of fig 1 concepts)
    - Row 2: three oblique angle examples showing intermediate similarity
    Each panel shows two vectors with quiver, annotated with the angle between them and the dot product value. Use the plot_vectors.py style (steelblue, quiver, gridspec).
    Save to `DSP_FIGURES_DIR / "vector_similarity.png"` at 150 DPI.

    **Figure 3: basis_decomposition.png** (DSP.md line 101)
    Show a 2D vector decomposed into x and y basis components:
    - Left panel: original vector v = (3, 2) as a single arrow
    - Right panel: same vector with dashed projections onto x-axis and y-axis, showing the x-component (3,0) and y-component (0,2) as separate colored arrows, with the original vector as the diagonal
    Use quiver arrows, different colors for each component (e.g., red for x, blue for y, black for original).
    Save to `DSP_FIGURES_DIR / "basis_decomposition.png"` at 150 DPI.

    **Figure 4: sign_accumulation.png** (DSP.md line 115)
    Show element-wise multiplication of two discrete vectors and how signs accumulate:
    - Top: two vectors a = [2, -1, 3, -2, 1] and b = [1, -2, -1, 1, 3] displayed as bar charts side by side
    - Middle: element products [2, 2, -3, -2, 3] with bars colored green (positive) or red (negative)
    - Bottom: running sum or final sum annotation showing net result
    Use 3-row subplot layout, bar charts, green/red coloring.
    Save to `DSP_FIGURES_DIR / "sign_accumulation.png"` at 150 DPI.

    **Figure 5: fourier_basis.png** (DSP.md line 157)
    Show Fourier basis functions — this is NOT from the notebook, implement fresh:
    - Plot 4-5 sine waves at different frequencies (e.g., 1 Hz, 2 Hz, 4 Hz, 8 Hz) stacked vertically
    - X-axis: time (0 to 1 second)
    - Each subplot: single sinusoid labeled with its frequency
    - Optional: one subplot showing composite signal (sum of all)
    Use clean line plots with labeled frequencies.
    Save to `DSP_FIGURES_DIR / "fourier_basis.png"` at 150 DPI.

    **Figure 6: stft_windowing.png** (DSP.md line 179)
    Show STFT windowing concept — implement fresh:
    - Top: a composite signal (chirp or frequency-varying signal)
    - Below: 4-5 overlapping rectangular or Hann windows shown along the time axis
    - Highlight one window showing "this chunk gets FFT'd"
    - Key visual: all windows are the same width (fixed resolution tradeoff)
    Use filled alpha regions for windows, line plot for signal.
    Save to `DSP_FIGURES_DIR / "stft_windowing.png"` at 150 DPI.

    **Figure 7: wavelet_scaling.png** (DSP.md line 229)
    Show wavelet scaling at different frequencies — implement fresh:
    - Plot 3-4 Morlet-like wavelets at different scales vertically:
      - Low frequency: wide, slow oscillation
      - Medium frequency: medium width
      - High frequency: narrow, fast oscillation
    - Each subplot labeled with approximate frequency
    - Key visual: width changes with frequency (unlike STFT where window is fixed)
    Use `np.exp(-t**2 / (2*sigma**2)) * np.cos(2*np.pi*f*t)` for Morlet-like shape with varying f and sigma.
    Save to `DSP_FIGURES_DIR / "wavelet_scaling.png"` at 150 DPI.

    **Step 3: Wire into argparse** (after line 978 where --comparison-grid is):
    ```python
    parser.add_argument("--foundations-figures", action="store_true",
                        help="Generate DSP.md foundation figures (pedagogical visualizations)")
    ```

    **Step 4: Wire into dispatch** (after line 1022 where comparison_grid dispatch is):
    ```python
    if args.foundations_figures:
        modes.append(("Foundations Figures", generate_foundations_figures))
    ```

    **Step 5: Update `any_flag`** (line 988-989) to include `args.foundations_figures`:
    ```python
    any_flag = (args.timing or args.figures or any_figure_individual or args.seaborn
                or args.unit_tests or args.stub or args.comparison_grid or args.foundations_figures)
    ```

    **Style consistency:**
    - Use matplotlib Agg backend (already set at top of benchmark.py)
    - All figures saved with `plt.savefig(path, dpi=150, bbox_inches='tight')`
    - Use `os.makedirs(DSP_FIGURES_DIR, exist_ok=True)` at start of function
    - Print progress like comparison_grid does (e.g., "Generating dot_product_geometry.png...")
    - plt.close(fig) after each savefig to prevent memory leaks
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && source venv/bin/activate && python research/benchmark.py --foundations-figures 2>&1 | tail -30 && echo "---" && ls -la assets/images/dsp/*.png 2>&1 && echo "---" && test -f assets/images/dsp/dot_product_geometry.png && test -f assets/images/dsp/vector_similarity.png && test -f assets/images/dsp/basis_decomposition.png && test -f assets/images/dsp/sign_accumulation.png && test -f assets/images/dsp/fourier_basis.png && test -f assets/images/dsp/stft_windowing.png && test -f assets/images/dsp/wavelet_scaling.png && echo "PASS: all 7 figures exist"</automated>
  </verify>
  <acceptance_criteria>
    - research/benchmark.py contains `--foundations-figures` in argparse section
    - research/benchmark.py contains `def generate_foundations_figures`
    - research/benchmark.py contains `foundations_figures` in the any_flag check
    - research/benchmark.py contains `assets/images/dsp` or `DSP_FIGURES_DIR` path
    - Running `python research/benchmark.py --foundations-figures` exits 0
    - assets/images/dsp/dot_product_geometry.png exists and is > 1KB
    - assets/images/dsp/vector_similarity.png exists and is > 1KB
    - assets/images/dsp/basis_decomposition.png exists and is > 1KB
    - assets/images/dsp/sign_accumulation.png exists and is > 1KB
    - assets/images/dsp/fourier_basis.png exists and is > 1KB
    - assets/images/dsp/stft_windowing.png exists and is > 1KB
    - assets/images/dsp/wavelet_scaling.png exists and is > 1KB
  </acceptance_criteria>
  <done>Running `python research/benchmark.py --foundations-figures` generates all 7 DSP foundation figures into assets/images/dsp/ as valid PNG files</done>
</task>

<task type="auto">
  <name>Task 2: Replace DSP.md PLACEHOLDER markers with live image paths</name>
  <files>DSP.md</files>
  <read_first>
    - DSP.md (FULL — locate all 7 PLACEHOLDER lines at lines 65, 79, 101, 115, 157, 179, 229; verify surrounding context to write appropriate alt text)
    - assets/images/dsp/ (confirm all 7 PNGs exist from Task 1 before replacing any placeholder)
  </read_first>
  <action>
    Replace each of the 7 `[PLACEHOLDER: figure — "..."]` lines in DSP.md with markdown image references pointing to the generated PNGs. Each replacement is a single line change.

    **Line 65** — replace:
    `[PLACEHOLDER: figure — "2D vector dot product geometric interpretation — parallel same direction, parallel opposite direction, perpendicular vectors"]`
    with:
    `![Dot Product Geometric Interpretation](assets/images/dsp/dot_product_geometry.png)`

    **Line 79** — replace:
    `[PLACEHOLDER: figure — "Parallel same/opposite and perpendicular vector pairs — visual comparison of similarity outcomes"]`
    with:
    `![Vector Similarity — Parallel, Opposite, Perpendicular, and Oblique Pairs](assets/images/dsp/vector_similarity.png)`

    **Line 101** — replace:
    `[PLACEHOLDER: figure — "Basis decomposition of a 2D vector into x and y components — projection visualization"]`
    with:
    `![Basis Decomposition of a 2D Vector](assets/images/dsp/basis_decomposition.png)`

    **Line 115** — replace:
    `[PLACEHOLDER: figure — "Color-coded element products showing sign accumulation: positive pairs (green), negative pairs (red), net sum result"]`
    with:
    `![Sign Accumulation in Dot Product](assets/images/dsp/sign_accumulation.png)`

    **Line 157** — replace:
    `[PLACEHOLDER: figure — "Fourier basis functions — several sine waves at different frequencies overlaid on a composite signal"]`
    with:
    `![Fourier Basis Functions](assets/images/dsp/fourier_basis.png)`

    **Line 179** — replace:
    `[PLACEHOLDER: figure — "STFT windowing illustration — signal chopped into overlapping windows, FFT applied to each"]`
    with:
    `![STFT Windowing](assets/images/dsp/stft_windowing.png)`

    **Line 229** — replace:
    `[PLACEHOLDER: figure — "Wavelet scaling at different frequencies — mother wavelet stretched for low freq (wide, slow), compressed for high freq (narrow, fast)"]`
    with:
    `![Wavelet Scaling at Different Frequencies](assets/images/dsp/wavelet_scaling.png)`

    Do NOT modify any other content in DSP.md. Only replace these 7 exact lines.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && echo "PLACEHOLDERs remaining:" && grep -c "PLACEHOLDER:" DSP.md && echo "Live image refs:" && grep -c "assets/images/dsp/" DSP.md && echo "---" && grep "assets/images/dsp/" DSP.md && echo "---" && test "$(grep -c 'PLACEHOLDER:' DSP.md)" = "1" && echo "PASS: only legend PLACEHOLDER remains"</automated>
  </verify>
  <acceptance_criteria>
    - DSP.md contains exactly 1 occurrence of "PLACEHOLDER:" (the legend line at line 6 only)
    - DSP.md contains `![Dot Product Geometric Interpretation](assets/images/dsp/dot_product_geometry.png)`
    - DSP.md contains `![Vector Similarity](assets/images/dsp/vector_similarity.png)` (or similar alt text)
    - DSP.md contains `![Basis Decomposition](assets/images/dsp/basis_decomposition.png)` (or similar alt text)
    - DSP.md contains `![Sign Accumulation](assets/images/dsp/sign_accumulation.png)` (or similar alt text)
    - DSP.md contains `![Fourier Basis Functions](assets/images/dsp/fourier_basis.png)`
    - DSP.md contains `![STFT Windowing](assets/images/dsp/stft_windowing.png)`
    - DSP.md contains `![Wavelet Scaling](assets/images/dsp/wavelet_scaling.png)` (or similar alt text)
    - Every image path in DSP.md under assets/images/dsp/ points to a file that exists on disk
    - No other content in DSP.md was changed (section headers, WRITE markers, code examples all preserved)
  </acceptance_criteria>
  <done>All 7 PLACEHOLDER markers in DSP.md are replaced with live markdown image references to existing PNG files in assets/images/dsp/</done>
</task>

</tasks>

<verification>
- `python research/benchmark.py --foundations-figures` runs without error and generates 7 PNGs
- All 7 PNGs exist in assets/images/dsp/ and are valid images (> 1KB each)
- DSP.md has exactly 1 PLACEHOLDER remaining (the legend line)
- DSP.md has 7 live `![...](assets/images/dsp/...)` image references
- No broken image paths in DSP.md (every referenced file exists)
- benchmark.py argparse includes `--foundations-figures` flag
- Existing benchmark.py functionality unaffected (--comparison-grid, --figures still work)
</verification>

<success_criteria>
- All 7 DSP foundation figures generated and saved to assets/images/dsp/
- benchmark.py has a working --foundations-figures flag following the established pattern
- DSP.md is visually complete with all image references pointing to real files
- Figure generation is repeatable via benchmark.py flag (single source of truth for all figures)
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-04-SUMMARY.md`
</output>
