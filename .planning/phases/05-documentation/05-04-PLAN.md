---
phase: 05-documentation
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/subshader/dsp/DSP.md
  - research/figures.py
  - research/test_suite.py
  - research/utilities/constants.py
  - assets/images/dsp/dot_product_geometry.png
  - assets/images/dsp/vector_similarity.png
  - assets/images/dsp/basis_decomposition.png
  - assets/images/dsp/sign_accumulation.png
  - assets/images/dsp/fourier_basis.png
  - assets/images/dsp/stft_windowing.png
  - assets/images/dsp/wavelet_scaling.png
autonomous: false
requirements: [DOCS-02, DOCS-05, DOCS-06]

must_haves:
  truths:
    - "src/subshader/dsp/DSP.md contains zero `[PLACEHOLDER:` substrings outside the legend block — the D-04 workshop pass replaces every PLACEHOLDER with a one-line pedagogical justification before any section-by-section authoring begins"
    - "src/subshader/dsp/DSP.md contains zero `wavelet.py` references — all stale source paths refreshed per D-20 to current `cwt.py` / `pywavelet.py` / `stft.py` / `wavelet_kernel.py` / `gaussian.py`"
    - "src/subshader/dsp/DSP.md contains zero `audio_input.py` references — D-24 path refresh applied during workshop pass"
    - "Sections §§1–6 of src/subshader/dsp/DSP.md (per D-16: inner product → CWT implementation per existing scaffold) contain user-authored prose, not `[WRITE: ...]` skeleton markers — Claude scaffolds (D-09: headers + bullets, NOT draft prose), user writes final text per the D-05 per-section prose-first → figure-to-match loop"
    - "src/subshader/dsp/DSP.md uses 'properties' (not 'features' or 'patterns') anywhere before §7 — terminology ladder per D-21"
    - "src/subshader/dsp/DSP.md §7 Future section consolidates wavelet-foundations §§7–10 plus the cross-domain Future Applications gesture (financial time series, heartbeat, brain signals) per D-17 and 05-06"
    - "src/subshader/dsp/DSP.md §3 (or wherever overlap is framed) explains overlap from the DSP-correctness angle (edge effects, cone of influence, reliable interior coefficients) per D-25 — overlap mechanics (`hop_size`, `file_pos` advancement) are NOT duplicated here; AUDIO.md owns the audio-I/O angle"
    - "src/subshader/dsp/DSP.md prose depth and voice match the archived foundations-notebook reference (`research/archive/docs/demo/readmes/wavelet/wavelet_foundations.ipynb`) per D-18 — accurate, practical explanations, not surface skim"
    - "Every `assets/images/dsp/*.png` referenced from DSP.md exists on disk"
    - "Foundation figures generated by `research/figures.py` helpers (per D-08: shared visual language, no parallel helper system; per D-19: wired into `research/test_suite.py` CLI as a `--foundations-figures` or `--figures` extension); regeneration is repeatable via the documented CLI path"
    - "Code examples shown in §6 are extracted verbatim from current source files per D-20 (cwt.py / pywavelet.py / stft.py / wavelet_kernel.py / config.py) — no illustrative stubs"
    - "Each of the 7 foundation figures in DSP.md is justified by a one-line pedagogical purpose tied to its surrounding section per D-06 — verifiable by confirming each `![...](assets/images/dsp/*.png)` markdown image reference is preceded within ≤3 lines by a one-line caption or pedagogical justification (in italic-caption form, surrounding prose, or a `[FIGURE: <purpose>]` justification line that has not yet been replaced by the image)"
  artifacts:
    - path: "src/subshader/dsp/DSP.md"
      provides: "User-authored DSP module README — wavelet foundations + CWT implementation + post-processing + consolidated Future section"
      contains: "## 1. Motivation"
    - path: "research/figures.py"
      provides: "Foundation-figure generator helpers extending ReadmeFigures, sharing style.py + comparison.py primitives"
      contains: "def generate_foundations_figures"
    - path: "research/test_suite.py"
      provides: "CLI wiring so foundation figures regenerate via the documented research-toolkit dispatcher"
      contains: "foundations"
    - path: "research/utilities/constants.py"
      provides: "IMAGES_DSP_DIR constant pointing at assets/images/dsp/"
      contains: "IMAGES_DSP_DIR"
    - path: "assets/images/dsp/"
      provides: "Seven foundation figures rendered with shared style.py constants"
  key_links:
    - from: "src/subshader/dsp/DSP.md"
      to: "assets/images/dsp/*.png"
      via: "Markdown image references replacing the workshop-pass justifications"
      pattern: "assets/images/dsp/.*\\.png"
    - from: "research/figures.py"
      to: "research/utilities/style.py"
      via: "Foundation figures consume style.py constants — same primitives ReadmeFigures uses"
      pattern: "from utilities.style import|from utilities import style"
    - from: "research/test_suite.py"
      to: "research/figures.py"
      via: "CLI dispatcher imports the foundation-figure entry point"
      pattern: "generate_foundations_figures|generate_all_figures"
---

<objective>
Author `src/subshader/dsp/DSP.md` end-to-end as a collaborative loop: Claude scaffolds, drafts skeleton text, generates figures inline against shared helpers, and verifies technical accuracy; the user owns final prose and approves each section before the next one is touched.

Purpose: The 359-line scaffold has 7 `[PLACEHOLDER:]` figure markers, several `[WRITE: ...]` prose markers, stale `wavelet.py` references in §6, and an unconsolidated §7 Future section. This plan workshops the storyline first (D-04), then walks §§1–6 section-by-section (D-05) with figure generation alongside, then folds the §7 Future material (D-17 + 05-06) in near the end. Every section task ends in a user-review checkpoint — the executor never chains drafts together autonomously, because voice and pedagogy belong to the user (CLAUDE.md "Documentation voice" rule).

Output: A user-authored DSP.md with no remaining placeholder/skeleton markers, current source paths, seven generated foundation figures shipped via the existing `research/figures.py` helper landscape (no parallel helper system per D-08/D-19), and a consolidated Future section.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md
@.planning/phases/05-documentation/05-DISCUSSION-LOG.md
@.planning/phases/05-documentation/05-02-SUMMARY.md

@src/subshader/dsp/DSP.md
@src/subshader/dsp/cwt.py
@src/subshader/dsp/pywavelet.py
@src/subshader/dsp/stft.py
@src/subshader/dsp/wavelet_kernel.py
@src/subshader/dsp/gaussian.py
@src/subshader/dsp/dsp.py
@src/subshader/config.py
@research/figures.py
@research/comparison.py
@research/test_suite.py
@research/utilities/style.py
@research/utilities/constants.py

<collaborative_loop_framing>
The 16-task structure (8 `auto` + 8 `checkpoint:human-verify`) is intentional per D-04 / D-05 + the CLAUDE.md voice constraint, NOT a candidate for plan-splitting. Each `checkpoint:human-verify` task acts as a context-refresh boundary: the executor of the next `auto` task only needs to load the prior section's final post-checkpoint state plus this plan, not the cumulative draft history of every earlier section. This keeps each authoring loop within a single fresh-context window while preserving the per-section workshop rhythm the user requested.
</collaborative_loop_framing>

<voice_constraint>
Per CLAUDE.md: "Documentation voice: User authors final prose. Claude scaffolds and suggests, doesn't write final copy."

The executor's role on every section task is:
- Draft a SKELETON the user will workshop — bullet points, section spine, candidate phrasing the user can rewrite
- Generate FIGURES inline against shared helpers
- Verify TECHNICAL ACCURACY of the user's edits (formulas, source-path references, code-example correctness)

The executor's role is NOT:
- Writing publishable prose for the user to ship as-is
- Deciding which analogies, examples, or section orderings stay
- Cutting or adding sections without the user opting in

Each section task ends with a `checkpoint:human-verify` — the user reads what the executor produced, rewrites the prose in their own voice, then signals approval before the next section task runs.
</voice_constraint>

<cross_doc_scope>
Per D-02 / D-25: this plan's DSP.md owns the CWT-correctness angle on overlap (edge effects, cone of influence, reliable interior coefficients). Mechanics of `hop_size` advancement and `file_pos` belong in AUDIO.md (separate plan). Do not duplicate audio-I/O mechanics here; reframe overlap from the DSP angle and trust AUDIO.md to own its own.

Per project memory `feedback_dsp_doc_focus.md` and D-17: §§1–6 stay 100% audio-focused. Cross-domain content (financial time series, heartbeat, brain signals) appears ONLY as a paragraph-level gesture in §7 Future — pedagogical lessons from cross-domain discussions can sharpen audio explanations without bringing the cross-domain examples themselves into §§1–6.
</cross_doc_scope>

<terminology_ladder>
Per D-21 and the archived `discussion_summary.md`: use `properties` (not `features` or `patterns`) anywhere before §7. The §7 Future section is where the terminology shifts to `features` — there is an explicit NOTE block in the scaffold marking that threshold. Preserve it.
</terminology_ladder>

<gate_idioms>
Several tasks below use grep gates that need to skip the legend block at the top of DSP.md (lines 1–9: H1 title + blockquote legend + first `---` rule). The cleanest legend-skipping idiom is:

```bash
awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c "<pattern>"
```

This slices from the §1 heading to end-of-file, dropping the H1 + legend block + first horizontal rule. Use this idiom (rather than `grep -v "^6:"` line-anchored exemptions) anywhere a sweep needs to ignore the marker-convention legend without false positives or false negatives as line numbers shift.

For per-section range gates (e.g., "DRAFT blocks within §3 only"), use:

```bash
awk '/^## 3\./,/^## 4\./' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT"
```

This isolates §3 content (between `## 3.` heading and the next `## 4.` heading). Apply the same idiom for §4, §5, §6, §7 by adjusting the section heading anchors.
</gate_idioms>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workshop pass — lock §§1–6 storyline, replace 7 PLACEHOLDER markers with one-line pedagogical justifications, refresh stale source paths</name>
  <read_first>
    - src/subshader/dsp/DSP.md (full 359 lines — note the 7 PLACEHOLDER lines at 65, 79, 101, 115, 157, 179, 229; the 3 stale `wavelet.py` references at 263, 266, 307; the benchmarks-path image references at 235 and 299; and the §7 Future block at lines 321–339)
    - .planning/phases/05-documentation/05-CONTEXT.md (D-04, D-06, D-08, D-16, D-17, D-20, D-21, D-24, D-25, D-30, D-31)
    - .planning/phases/05-documentation/05-DISCUSSION-LOG.md (Q1.1, Q1.4, Q4.1, Q4.2 — confirms workshop-first authoring rhythm)
    - src/subshader/dsp/cwt.py (current CWT class hierarchy + cwt() pipeline — verify the §6.2 / §6.5 source paths and method names)
    - src/subshader/dsp/pywavelet.py (verify reference-backend naming used in §6.5)
    - src/subshader/dsp/wavelet_kernel.py (verify L1 normalization location for §6.3 source citation)
    - src/subshader/config.py (verify WaveletConfig / CWTConfig parameter names in the §6.1 table)
  </read_first>
  <action>
    Edit `src/subshader/dsp/DSP.md` directly. This is a SCAFFOLD-level workshop pass — no section prose authored yet. Goal: lock the storyline + figure inventory + accurate source paths so §§1–6 authoring (Tasks 2–7) starts from a clean spine.

    Three concrete edits:

    **Edit A — Replace each of the 7 `[PLACEHOLDER:]` markers with a one-line pedagogical justification (D-04 + D-06).** The justification states what the figure must teach for the surrounding prose to land. Keep the bracketed format so the next-step (figure generation) can grep them. Use this template: `[FIGURE: <one-line pedagogical purpose> — generated by research/figures.py foundation helpers]`.

    Concrete replacements (executor drafts these against the surrounding §-context; the user reviews the wording at the checkpoint):

    | Line | Section | Current PLACEHOLDER | Drafted FIGURE justification |
    |------|---------|---------------------|------------------------------|
    | 65 | §2.3 Dot Product | "2D vector dot product geometric interpretation — parallel same direction, parallel opposite direction, perpendicular vectors" | `[FIGURE: dot_product_geometry — show that the dot product's sign and magnitude come from the angle between two vectors; parallel-same → positive, parallel-opposite → negative, perpendicular → zero]` |
    | 79 | §2.4.1 Geometric Interpretation | "Parallel same/opposite and perpendicular vector pairs — visual comparison of similarity outcomes" | `[FIGURE: vector_similarity — extend the dot-product intuition to a continuum of angles, motivating projection in §2.4.2]` |
    | 101 | §2.4.3 Basis Decomposition | "Basis decomposition of a 2D vector into x and y components — projection visualization" | `[FIGURE: basis_decomposition — show a 2D vector as the sum of x- and y-component projections, motivating the move from 2D to N-dimensional inner products]` |
    | 115 | §2.5 Sign Accumulation | "Color-coded element products showing sign accumulation: positive pairs (green), negative pairs (red), net sum result" | `[FIGURE: sign_accumulation — show element-wise products as signed contributions and the running sum that produces the final similarity score]` |
    | 157 | §3.3 The Fourier Transform | "Fourier basis functions — several sine waves at different frequencies overlaid on a composite signal" | `[FIGURE: fourier_basis — show the family of sine-wave templates the FFT compares against, motivating the §3.4 stationarity limitation]` |
    | 179 | §4.1 Windowed Compromise | "STFT windowing illustration — signal chopped into overlapping windows, FFT applied to each" | `[FIGURE: stft_windowing — show fixed-width windows tiling a non-stationary signal, motivating the §4.2 resolution tradeoff]` |
    | 229 | §5.4 Convolution Implementation | "Wavelet scaling at different frequencies — mother wavelet stretched for low freq (wide, slow), compressed for high freq (narrow, fast)" | `[FIGURE: wavelet_scaling — show the mother wavelet at multiple scales to make the adaptive-resolution claim of §5.1 concrete]` |

    The user may rewrite any justification at the checkpoint. The executor's draft is a starting point.

    **Edit B — Refresh stale source paths.** Replace every occurrence of `src/subshader/dsp/wavelet.py` (currently at lines 263, 266, 307) with the post-Phase-8 layout. Per D-20:
    - The §6.2 pipeline excerpt (lines 263–273) currently quotes `Wavelet.cwt()` from `wavelet.py`. The current implementation lives in `src/subshader/dsp/cwt.py` (CWT base class) — refresh both the prose lead-in and the source comment. Read `cwt.py` and replace the code block with the actual current method body so the example stays runnable per success-criterion #4.
    - The §6.5 class-hierarchy block (lines 305–315) currently shows the old `Wavelet → PyWavelet / AntsWavelet → NumPyWavelet / CuPyWavelet` tree. Per Phase-8 D-13, the tree is now `CWT (in cwt.py) → CpuCWT, GpuCWT` plus `PywaveletCWT (in pywavelet.py)` and `STFT (in stft.py)` as sibling backends under `dsp.py`'s `DSP` ABC. Replace the diagram with the current tree. Read `dsp.py`, `cwt.py`, `pywavelet.py`, `stft.py` to confirm class names and write the diagram against actual code.
    - The §6.1 parameter table (lines 245–253) references `WaveletConfig` — confirm against `src/subshader/config.py`. If the dataclass is now `CWTConfig` (per Phase-8 D-15 alias), update the table caption accordingly. Do NOT delete the table; just align the name with the current dataclass.
    - Search the whole file for any lingering `audio_input.py` reference — none expected after a clean read, but if found, update to `src/subshader/audio/reader.py` per D-24 + D-25.

    **Edit C — Refresh broken/relocated benchmark image paths.** Lines 235 and 299 reference `assets/images/benchmarks/timing_bar_chart.png` and `assets/images/benchmarks/numpy_vs_cupy_diff.png`. The `assets/images/benchmarks/` directory does not exist in the current asset layout. Look up the actual current locations using `ls assets/images/` and the asset-lifecycle rule from STATE.md ("reference/ for committed input files, generated/ for test-suite outputs, figures/ for committed README figures"). Replace each path with the correct current location. If the timing bar chart's canonical home is unsettled, leave a one-line `[FIGURE: timing_bar_chart — needs current path resolved during §6 authoring]` marker rather than guess; the user will resolve it during Task 6 (§6 authoring). This `timing_bar_chart` marker is the ONLY contingent FIGURE marker the §§1–6 plan permits — the seven foundation figures are non-contingent.

    **Out of scope for this task:** Do NOT author prose under any `[WRITE: ...]` marker. Do NOT generate any PNG figures. Do NOT touch §7 Future. This task locks the spine only — sections fill in over Tasks 2–8.

    **Commit:** Single commit message: `docs(05-04): workshop pass — lock DSP.md storyline, refresh stale paths`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c "\[PLACEHOLDER:" | (read n && [ "$n" -eq 0 ] && echo "PASS: PLACEHOLDER count outside legend = 0") && awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c "\[FIGURE:" | (read n && [ "$n" -ge 7 ] && [ "$n" -le 8 ] && echo "PASS: FIGURE markers = $n (7 baseline + up to 1 contingent timing_bar_chart)") && (! grep -n "wavelet\.py" src/subshader/dsp/DSP.md && echo "PASS: no wavelet.py refs") && (! grep -n "audio_input" src/subshader/dsp/DSP.md && echo "PASS: no audio_input refs")</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c '\[PLACEHOLDER:'` returns 0 (every PLACEHOLDER outside the legend block has been replaced; the legend bullet itself is allowed to retain the literal marker convention)
    - `awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c '\[FIGURE:'` returns between 7 and 8 inclusive (one per replaced PLACEHOLDER, plus at most one contingent `timing_bar_chart` marker if the §6 timing chart path was not resolvable in this pass)
    - `grep -n 'wavelet\.py' src/subshader/dsp/DSP.md` returns no matches
    - `grep -n 'audio_input' src/subshader/dsp/DSP.md` returns no matches
    - `grep -n 'assets/images/benchmarks' src/subshader/dsp/DSP.md` returns no matches (the benchmarks/ directory does not exist; references must be refreshed or marked)
    - `grep -n 'src/subshader/dsp/cwt\.py' src/subshader/dsp/DSP.md` returns at least one match (the §6 source citation now points at the current file)
    - The §6.5 hierarchy diagram lists at least one of `CpuCWT`, `GpuCWT`, `PywaveletCWT`, or `STFT` (current class names, not the archived `NumPyWavelet`/`CuPyWavelet`)
    - The `[WRITE:` marker count is unchanged from before this task (workshop pass MUST NOT author section prose)
    - Section heading order `## 1. Motivation`, `## 2. Foundations: Inner Product`, `## 3. Fourier Transform`, `## 4. Short-Time Fourier Transform (STFT)`, `## 5. Wavelet Transform`, `## 6. Implementation Deep Dive`, `## 7. Future` is preserved verbatim
  </acceptance_criteria>
  <done>DSP.md has a locked spine: 7 figure justifications in place, source paths refreshed to post-Phase-8 layout, benchmark image paths resolved or explicitly flagged. No section prose authored yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.1: User reviews workshop-pass edits</name>
  <what-built>
    Task 1 edited the DSP.md scaffold in place:
    - 7 `[PLACEHOLDER:]` markers replaced with `[FIGURE: <pedagogical justification>]` lines
    - §6 source paths refreshed from `wavelet.py` to current `cwt.py` / `pywavelet.py` / `stft.py`
    - §6.5 class hierarchy diagram aligned with the post-Phase-8 `CWT → CpuCWT, GpuCWT` tree
    - Stale `assets/images/benchmarks/*.png` paths refreshed against current asset layout
    - No section prose authored yet — `[WRITE: ...]` markers untouched
  </what-built>
  <how-to-verify>
    1. `git diff src/subshader/dsp/DSP.md` — confirm the diff is scaffold-edits only, no `[WRITE: ...]` content invented
    2. Read the 7 new `[FIGURE: ...]` lines in context — does each justification match what the surrounding section is trying to teach? Rewrite any that miss the mark
    3. Confirm §6.5's refreshed class hierarchy matches your mental model of the current `dsp/` module — fix any class-name mismatch
    4. Confirm refreshed benchmark image paths point at files that actually exist on disk (or are flagged for resolution during §6 authoring)
    5. Confirm the storyline still reads end-to-end without any section feeling orphaned by the edits
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to §1 Motivation skeleton draft, or describe specific scaffold-level changes needed before section authoring begins.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Draft skeleton for §1 Motivation + §2 Foundations: Inner Product</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-1 state — read the locked §§1–2 spine including all `[WRITE: ...]` markers in §1 and §2.1–2.7)
    - .planning/phases/05-documentation/05-CONTEXT.md (D-05 prose-first/figure-to-match, D-11 candidate analogies, D-21 terminology ladder)
    - research/archive/docs/demo/readmes/wavelet/wavelet_foundations_outline.md (original §§1–2 outline — pedagogical voice reference)
    - research/archive/docs/demo/discussion_summary.md (terminology ladder authority)
  </read_first>
  <action>
    Draft skeleton text for the user to workshop under each `[WRITE: ...]` marker in §1 Motivation and §2 Foundations: Inner Product (subsections 2.1 through 2.7). Skeleton text means: 2–4 bullet points or 2–3 sentence-stubs per `[WRITE: ...]` marker that the user can rewrite into their voice. NOT publishable final prose.

    Keep `[WRITE: ...]` markers in place — the executor adds drafted skeleton AFTER each marker, separated by a `<!-- DRAFT -->` HTML comment so the user can grep and remove drafts that don't survive review:

    ```markdown
    [WRITE: "Why time-frequency analysis matters — what frequencies are present and when they occur"]

    <!-- DRAFT (Claude scaffold — user rewrites in own voice or deletes) -->
    - Audio is a sequence of pressure samples; the human perceptual interesting structure (notes, transients, timbre) lives in the time-frequency relationship, not the raw waveform
    - "What frequencies, and when" — both axes matter; one without the other tells half the story
    - Concrete example to potentially use: a single drum hit and a sustained tone have wildly different time-frequency signatures even if their average spectra match
    <!-- /DRAFT -->
    ```

    Apply this pattern to every `[WRITE: ...]` marker in §1 and §2. The user will review, rewrite, and remove the `<!-- DRAFT -->` blocks at the checkpoint.

    **Hard constraints:**
    - DO NOT replace `[WRITE: ...]` markers — the user removes them after authoring final prose
    - DO NOT touch the existing `candidate analogy:` lines — those are user-decision artifacts
    - DO NOT introduce 'features' or 'patterns' terminology — D-21 says 'properties' before §7
    - DO NOT generate figures in this task — figures land in Tasks 3 and 4 (§2.3–2.5 figures) and Task 5 (§3 figure)
    - Keep all skeleton drafts audio-focused — no cross-domain examples in §§1–2 per project memory `feedback_dsp_doc_focus.md`

    **Voice anchor:** match the foundations-notebook voice (per D-18 — see `research/archive/docs/demo/readmes/wavelet/wavelet_foundations.ipynb` if needed for reference). The user's prose voice is informal-precise: explain the math, show why it matters, avoid jargon front-loading.

    **Commit:** `docs(05-04): draft skeletons for §1 Motivation + §2 Foundations`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && awk '/^## 1\./,/^## 3\./' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT" | (read n && [ "$n" -ge 8 ] && echo "PASS: DRAFT blocks in §§1-2 = $n (>=8)") && (! grep -nE "(features|patterns)" src/subshader/dsp/DSP.md | grep -v "^.*7\." | grep -v "^.*Future" && echo "PASS: no features/patterns leaked before §7")</automated>
  </verify>
  <acceptance_criteria>
    - At least 8 `<!-- DRAFT` blocks within §§1–2 (the slice between `## 1.` and `## 3.` headings), matching the count of `[WRITE: ...]` markers in those sections
    - Each `<!-- DRAFT` block ends with `<!-- /DRAFT -->`
    - All `[WRITE: ...]` markers in §1 and §2 still present (the executor does NOT remove them — that is the user's job after authoring final prose)
    - All `candidate analogy:` lines preserved verbatim
    - `grep -nE '\bfeatures\b|\bpatterns\b' src/subshader/dsp/DSP.md` returns matches ONLY at line numbers >= the §7 Future heading line
    - No new image references or figure files added — this task is prose-skeleton only
    - Diff scope is contained to §§1–2; §§3–7 untouched
  </acceptance_criteria>
  <done>§§1–2 have skeleton drafts under every `[WRITE: ...]` marker, marked with HTML comments so the user can grep them while authoring final prose. The user has a clear starting point for §1–2 prose work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.1: User authors §§1–2 final prose against the skeleton drafts</name>
  <what-built>
    Task 2 added skeleton drafts as `<!-- DRAFT ... /DRAFT -->` HTML comment blocks under every `[WRITE: ...]` marker in §§1–2. The drafts are starting points, not final copy.
  </what-built>
  <how-to-verify>
    1. Read each `<!-- DRAFT -->` block — does it capture the right beat for the surrounding `[WRITE: ...]` intent?
    2. Rewrite each `[WRITE: "..."]` line into your own prose voice. Delete the `<!-- DRAFT -->` block once your prose ships, or keep its bullets as a checklist while you write
    3. Confirm the §§1–2 reading flow lands the §2.7 inner-vs-outer-product bridge into §3 cleanly
    4. Workshop word choice / phrasing / pedagogical framing as the locked authoring rhythm specifies (D-04, D-05)
    5. When §§1–2 reads in your voice, signal approval. Until then, this checkpoint stays open — no figure generation or §3 work begins
  </how-to-verify>
  <resume-signal>Type "approved" once §§1–2 prose is in your voice and `[WRITE: ...]` markers are removed for those sections, or describe what should change about the executor's skeleton approach before continuing.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Generate figures for §§2.3–2.5 (dot_product_geometry, vector_similarity, basis_decomposition, sign_accumulation) via research/figures.py helpers</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-2 state — read the user-authored §§2.3–2.5 prose so figures emphasize what the prose actually emphasizes per D-05)
    - research/figures.py (full file — note the `ReadmeFigures` class at line 61 and `generate_all_figures()` at line 546; foundation helpers extend this module per D-19)
    - research/utilities/style.py (style constants — colors, fonts, DPI, dark theme)
    - research/comparison.py (shared layout primitives that foundation figures must reuse where applicable per D-08)
    - research/utilities/constants.py (existing `IMAGES_REFERENCE_DIR` / `IMAGES_GENERATED_DIR` pattern — add a sibling `IMAGES_DSP_DIR` constant)
    - research/archive/docs/demo/readmes/wavelet/plot_vectors.py (archived quiver-plot helper — port styling pattern into research/figures.py; do NOT call from archive)
  </read_first>
  <action>
    Generate the four §2 foundation figures and wire them into DSP.md. Implementation lives in `research/figures.py` (extending the existing helper landscape per D-08 and D-19), NOT in a separate parallel module.

    **Step 1 — Add path constant.** In `research/utilities/constants.py`, add `IMAGES_DSP_DIR = "assets/images/dsp"` next to the existing `IMAGES_REFERENCE_DIR` / `IMAGES_GENERATED_DIR` constants. Create `assets/images/dsp/` on disk if it does not exist.

    **Step 2 — Add foundation-figure helpers to `research/figures.py`.** Add a `generate_foundations_figures()` top-level function (peer of `generate_all_figures` and `generate_method_comparison`) that calls four sub-helpers — one per figure — and saves each to `IMAGES_DSP_DIR`. The four helpers:
    - `_plot_dot_product_geometry(out_path)` — three quiver subplots: parallel-same, parallel-opposite, perpendicular. Each subplot annotates the dot product value and angle. Reuse `research/utilities/style.py` constants for colors / DPI / fonts. Port styling from archived `plot_vectors.py`.
    - `_plot_vector_similarity(out_path)` — 2×3 grid extending the dot-product idea with oblique angles. Same quiver-arrow pattern, same style constants.
    - `_plot_basis_decomposition(out_path)` — 1×2: a 2D vector on the left, the same vector decomposed into x and y projection arrows on the right.
    - `_plot_sign_accumulation(out_path)` — 3-row stack: two discrete vectors as bar charts, element-wise products color-coded by sign, running sum or final sum annotation.

    All four helpers MUST consume `research/utilities/style.py` constants for visual aesthetics — no hardcoded colors / DPI / fonts. This is the "shared visual language" rule per D-08.

    **Step 3 — Wire CLI entry point.** Add a foundation-figures path to `research/test_suite.py`. Two viable approaches; the executor picks one and notes the choice in the commit message:
    - (a) Extend the existing `--figures` flag so `generate_all_figures(stub=...)` calls `generate_foundations_figures()` as well — preferred for "single source of truth for all documentation figures"
    - (b) Add a sibling `--foundations-figures` flag in the mutually-exclusive group — preferred if foundation figures should be regenerated independently

    Either way, running the test_suite path must produce the four PNGs into `assets/images/dsp/`.

    **Step 4 — Wire image references into DSP.md.** Replace the four `[FIGURE: ...]` markers in §§2.3–2.5 (lines previously 65, 79, 101, 115 — line numbers may have shifted after Task 2 prose authoring) with markdown image references:
    ```markdown
    ![Dot Product Geometric Interpretation](assets/images/dsp/dot_product_geometry.png)
    ```
    Use alt text the user can edit at the checkpoint.

    **Hard constraints:**
    - NO parallel helper module — extend `research/figures.py` per D-19
    - NO hardcoded styling — every visual constant comes from `research/utilities/style.py` per D-08
    - NO calls into `research/archive/` — port the styling pattern, do not import the archived file
    - Generated PNGs must be > 1KB each (sanity-check rendering succeeded)
    - This task does NOT touch §3, §4, §5 figures — those are Tasks 5, 6, 7 respectively, after their corresponding prose passes

    **Commit:** `docs(05-04): §2 foundation figures + research/figures.py wiring`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && grep -c "IMAGES_DSP_DIR" research/utilities/constants.py | (read n && [ "$n" -ge 1 ] && echo "PASS: constant added") && grep -c "generate_foundations_figures\|_plot_dot_product_geometry\|_plot_vector_similarity\|_plot_basis_decomposition\|_plot_sign_accumulation" research/figures.py | (read n && [ "$n" -ge 5 ] && echo "PASS: 4 helpers + entry point in figures.py ($n)") && test -f assets/images/dsp/dot_product_geometry.png && test -f assets/images/dsp/vector_similarity.png && test -f assets/images/dsp/basis_decomposition.png && test -f assets/images/dsp/sign_accumulation.png && echo "PASS: 4 PNGs exist" && for f in assets/images/dsp/dot_product_geometry.png assets/images/dsp/vector_similarity.png assets/images/dsp/basis_decomposition.png assets/images/dsp/sign_accumulation.png; do [ "$(stat -c%s "$f")" -gt 1024 ] || { echo "FAIL: $f under 1KB"; exit 1; }; done && echo "PASS: all 4 PNGs > 1KB" && grep -c "assets/images/dsp/dot_product_geometry.png\|assets/images/dsp/vector_similarity.png\|assets/images/dsp/basis_decomposition.png\|assets/images/dsp/sign_accumulation.png" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 4 ] && echo "PASS: 4 image refs in DSP.md")</automated>
  </verify>
  <acceptance_criteria>
    - `research/utilities/constants.py` defines `IMAGES_DSP_DIR`
    - `research/figures.py` defines `generate_foundations_figures` and the four `_plot_*` helpers
    - `research/figures.py` imports from `research/utilities/style` (no hardcoded colors / DPI / fonts in foundation helpers)
    - `assets/images/dsp/{dot_product_geometry,vector_similarity,basis_decomposition,sign_accumulation}.png` all exist on disk and are > 1KB each
    - `src/subshader/dsp/DSP.md` has four image references pointing at the PNGs above
    - Running `python research/test_suite.py --figures` (or `--foundations-figures` if that flag was chosen in Step 3) regenerates the four PNGs without error
    - `grep -nE 'plt\.rcParams|matplotlib\.rcParams' research/figures.py` shows usage routed through style.py — no foundation helper sets rcParams independently
  </acceptance_criteria>
  <done>Four §2 foundation figures generated via shared helpers in research/figures.py; DSP.md has live image references; test_suite.py CLI regenerates them on demand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.1: User reviews §2 figures + figure-helper integration</name>
  <what-built>
    - 4 PNGs in `assets/images/dsp/`
    - `generate_foundations_figures()` + 4 sub-helpers in `research/figures.py` consuming `research/utilities/style.py`
    - `IMAGES_DSP_DIR` in `research/utilities/constants.py`
    - CLI entry-point wiring in `research/test_suite.py`
    - 4 image references inserted into DSP.md §§2.3–2.5
  </what-built>
  <how-to-verify>
    1. Open each of the 4 PNGs — does the figure land the pedagogical point in its `[FIGURE: ...]` justification (now replaced by the markdown image reference) from Task 1?
    2. Are all 4 figures visually consistent? Same color palette, same font, same axis-tick style, same DPI? (D-08 shared visual language rule)
    3. Re-run `python research/test_suite.py --figures` and confirm the PNGs regenerate identically (or with only style-driven differences)
    4. Read §§2.3–2.5 with the figures in place — does prose + figure together teach what the section is supposed to teach? Iterate on prose or request a figure tweak if not
    5. Spot-check that no foundation helper uses hardcoded matplotlib rcParams — every visual constant should route through `style.py`
  </how-to-verify>
  <resume-signal>Type "approved" once §2 figures land their pedagogical purpose and integrate cleanly with surrounding prose, or describe figure-level changes needed (color, layout, annotation, etc.) before continuing.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Draft skeleton for §3 Fourier Transform + §4 STFT, generate fourier_basis.png and stft_windowing.png</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-3 state — read the user-authored §§1–2 prose to inherit voice / framing for §§3–4 skeleton drafts)
    - research/figures.py (post-Task-3 state — `generate_foundations_figures` and helpers added in Task 3)
    - research/utilities/style.py (style constants)
    - research/utilities/dsp_helpers.py (chirp / signal-construction helpers — useful for the STFT-windowing illustration)
  </read_first>
  <action>
    Combine prose-skeleton authoring with figure generation for §§3–4. The single task scope is justified because §3 and §4 form one storyline beat (Fourier-then-STFT motivates the §5 wavelet move) and each section gets exactly one figure.

    **Step 1 — Skeleton drafts.** Apply the same `<!-- DRAFT ... /DRAFT -->` HTML-comment pattern from Task 2 to every `[WRITE: ...]` marker in §3.1, §3.2, §3.3, §3.4, §4.1, and §4.2. Each draft is bullet-level scaffolding the user rewrites; do not author publishable prose.

    Stay audio-focused. The §4 resolution-tradeoff discussion is the most likely place to drift into general DSP language — keep examples tied to audio (e.g., 100 Hz vs 200 Hz frequency-difference perception).

    **Step 2 — Add `_plot_fourier_basis(out_path)` and `_plot_stft_windowing(out_path)` to `research/figures.py`.** Wire them into `generate_foundations_figures()` from Task 3. Style constants from `research/utilities/style.py` apply.

    - `_plot_fourier_basis`: 4–5 sine waves at logarithmically-spaced frequencies stacked vertically, optionally with one row showing their composite. Axis time 0–1 s. Each subplot labeled with frequency.
    - `_plot_stft_windowing`: top row a non-stationary signal (e.g., a chirp from `research/utilities/dsp_helpers.py`); below it 4–5 fixed-width windows tiling the time axis with alpha-fill, one window highlighted.

    **Step 3 — Wire image references into DSP.md.** Replace the §3.3 and §4.1 `[FIGURE: ...]` markers (or `[PLACEHOLDER: ...]` if the workshop pass left any in place) with markdown image references to the new PNGs.

    **Step 4 — Regenerate via test_suite.py.** Run the wired CLI command from Task 3 to confirm the new PNGs land in `assets/images/dsp/`.

    **Hard constraints:**
    - NO publishable prose — drafts only
    - Audio-focused examples only
    - Reuse style.py constants
    - Do NOT touch §5 yet — that is Task 5

    **Commit:** `docs(05-04): §§3-4 skeleton + Fourier/STFT figures`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test -f assets/images/dsp/fourier_basis.png && test -f assets/images/dsp/stft_windowing.png && [ "$(stat -c%s assets/images/dsp/fourier_basis.png)" -gt 1024 ] && [ "$(stat -c%s assets/images/dsp/stft_windowing.png)" -gt 1024 ] && echo "PASS: §§3-4 PNGs exist and >1KB" && awk '/^## 3\./,/^## 5\./' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT" | (read n && [ "$n" -ge 6 ] && echo "PASS: §§3-4 DRAFT blocks = $n (>=6, one per WRITE marker in §§3-4)") && grep -c "_plot_fourier_basis\|_plot_stft_windowing" research/figures.py | (read n && [ "$n" -ge 2 ] && echo "PASS: both helpers in figures.py") && grep -c "assets/images/dsp/fourier_basis.png\|assets/images/dsp/stft_windowing.png" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 2 ] && echo "PASS: 2 image refs in DSP.md")</automated>
  </verify>
  <acceptance_criteria>
    - `assets/images/dsp/fourier_basis.png` and `assets/images/dsp/stft_windowing.png` exist and are > 1KB
    - `research/figures.py` defines `_plot_fourier_basis` and `_plot_stft_windowing`; both are invoked by `generate_foundations_figures`
    - `src/subshader/dsp/DSP.md` references both new PNGs at the §3.3 and §4.1 figure positions
    - `awk '/^## 3\./,/^## 5\./' src/subshader/dsp/DSP.md | grep -c '<!-- DRAFT'` returns at least 6 (one per `[WRITE: ...]` marker in §§3–4) — gate is per-section range, not cumulative, so prior-section DRAFT-block deletions during user authoring (Task 2.1 checkpoint) do not invalidate this task's work
    - All §§3–4 `[WRITE: ...]` markers still present (the user removes them during prose authoring)
    - Foundation helpers continue to consume `research/utilities/style.py` constants — no hardcoded styling
  </acceptance_criteria>
  <done>§§3–4 have skeleton drafts under every `[WRITE: ...]` marker; both Fourier-basis and STFT-windowing figures are generated and referenced from DSP.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.1: User authors §§3–4 final prose + reviews figures</name>
  <what-built>
    - Skeleton drafts under every `[WRITE: ...]` marker in §§3–4
    - 2 PNGs (`fourier_basis.png`, `stft_windowing.png`) and corresponding helpers in `research/figures.py`
    - Image references inserted into §3.3 and §4.1
  </what-built>
  <how-to-verify>
    1. Rewrite each `[WRITE: "..."]` in §§3–4 into your own prose voice
    2. Delete `<!-- DRAFT -->` blocks as your prose ships
    3. Confirm Fourier-basis and STFT-windowing figures land their pedagogical purposes — §3.3 must motivate the §3.4 stationarity limitation; §4.1 must motivate the §4.2 resolution tradeoff
    4. Confirm voice continuity from §§1–2 (same authorial register, same explanation depth)
    5. Confirm §4.2 resolution-tradeoff discussion stays audio-anchored (100 Hz vs 200 Hz, etc.) — no drift to general-DSP language
  </how-to-verify>
  <resume-signal>Type "approved" once §§3–4 reads in your voice and figures land their points, or describe needed changes before §5 work begins.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Draft skeleton for §5 Wavelet Transform + generate wavelet_scaling.png</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-4 state — §§1–4 should be in user voice; §5 still has `[WRITE: ...]` markers and one `[FIGURE: ...]` marker)
    - src/subshader/dsp/wavelet_kernel.py (current kernel construction — informs the wavelet_scaling figure parameters)
    - src/subshader/dsp/gaussian.py (Gaussian-modulated sinusoid construction — Morlet-like wavelet shape)
    - research/figures.py (post-Task-4 state)
    - research/utilities/style.py (style constants)
  </read_first>
  <action>
    **Step 1 — Skeleton drafts for §§5.1, 5.2, 5.3, 5.4.** Same `<!-- DRAFT ... /DRAFT -->` pattern. Each subsection's draft must:
    - §5.1 — frame the adaptive-resolution claim as the answer to §4.2's tradeoff
    - §5.2 — connect the wavelet template to the §2 inner-product framework (kernel = template = mother wavelet at scale)
    - §5.3 — anchor adaptive-resolution to audio (logarithmic chromatic scale, perceptual frequency ratios) — keep audio-focused per project memory
    - §5.4 — bridge convolution to the §6 implementation deep dive

    **Step 2 — Add `_plot_wavelet_scaling(out_path)` to `research/figures.py`.** Show 3–4 Morlet-like wavelets (`np.exp(-t**2 / (2*sigma**2)) * np.cos(2*np.pi*f*t)`) at logarithmically-spaced frequencies / scales stacked vertically. Each subplot labeled with frequency. Highlight that envelope width changes with scale — the visual move that justifies §5.1's "adaptive resolution" claim.

    Use style.py constants. If `gaussian.py` has a Morlet construction helper that already produces these shapes, prefer importing it over reimplementing — this is in spirit of D-20 ("examples extracted from current source files").

    **Step 3 — Wire `_plot_wavelet_scaling` into `generate_foundations_figures()` and replace the §5.4 figure marker in DSP.md** with a markdown image reference.

    **Hard constraints:**
    - Drafts, not final prose
    - Audio-focused
    - Reuse style.py constants
    - Prefer importing real Morlet construction from `src/subshader/dsp/gaussian.py` over reimplementing the math

    **Commit:** `docs(05-04): §5 skeleton + wavelet_scaling figure`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && test -f assets/images/dsp/wavelet_scaling.png && [ "$(stat -c%s assets/images/dsp/wavelet_scaling.png)" -gt 1024 ] && echo "PASS: wavelet_scaling.png exists and >1KB" && grep -c "_plot_wavelet_scaling" research/figures.py | (read n && [ "$n" -ge 1 ] && echo "PASS: helper in figures.py") && grep -c "assets/images/dsp/wavelet_scaling.png" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 1 ] && echo "PASS: image ref in DSP.md") && awk '/^## 5\./,/^## 6\./' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT" | (read n && [ "$n" -ge 4 ] && echo "PASS: §5 DRAFT blocks = $n (>=4, one per WRITE marker in §5)")</automated>
  </verify>
  <acceptance_criteria>
    - `assets/images/dsp/wavelet_scaling.png` exists and is > 1KB
    - `research/figures.py` defines `_plot_wavelet_scaling` and includes it in `generate_foundations_figures`
    - `src/subshader/dsp/DSP.md` references the PNG at the §5.4 figure position
    - `awk '/^## 5\./,/^## 6\./' src/subshader/dsp/DSP.md | grep -c '<!-- DRAFT'` returns at least 4 (one per `[WRITE: ...]` marker in §5) — per-section gate, independent of whether earlier sections still hold their DRAFT blocks after user authoring
    - All §5 `[WRITE: ...]` markers still present
    - Foundation helper continues to consume style.py constants
  </acceptance_criteria>
  <done>§5 has skeleton drafts; wavelet-scaling figure generated and wired into DSP.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.1: User authors §5 final prose + reviews wavelet figure</name>
  <what-built>
    Skeleton drafts under every `[WRITE: ...]` in §5; `wavelet_scaling.png` generated and referenced from §5.4.
  </what-built>
  <how-to-verify>
    1. Rewrite each `[WRITE: "..."]` in §5 into your own voice
    2. Delete `<!-- DRAFT -->` blocks as prose ships
    3. Confirm the wavelet-scaling figure makes the adaptive-resolution claim concrete enough that §5.1 lands without further explanation
    4. Confirm §5.4 sets up the §6 implementation deep dive cleanly (convolution → kernel → impulse response → scalogram)
    5. Confirm §5.3 stays audio-anchored (chromatic scale, perceptual frequency ratios) per the audio-focus rule
  </how-to-verify>
  <resume-signal>Type "approved" once §5 reads in your voice and the wavelet figure lands, or describe needed changes before §6 implementation deep dive.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Draft skeleton for §6 Implementation Deep Dive — extract code from current source files, verify accuracy</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-5 state — §§1–5 in user voice; §6 has `[WRITE: ...]` markers and the parameter table)
    - src/subshader/dsp/cwt.py (full file — verify the §6.2 pipeline excerpt against actual `cwt()` method body; confirm the post-Phase-8 method names)
    - src/subshader/dsp/pywavelet.py (verify §6.5 backend reference)
    - src/subshader/dsp/stft.py (verify §6.5 backend reference)
    - src/subshader/dsp/wavelet_kernel.py (verify §6.3 L1 normalization location)
    - src/subshader/dsp/dsp.py (DSP ABC — verify §6.5 hierarchy diagram against the actual base class + `pre/transform/post` interface)
    - src/subshader/config.py (verify §6.1 parameter table aligns with the current `WaveletConfig` / `CWTConfig` field names + defaults)
  </read_first>
  <action>
    §6 is the most accuracy-sensitive section because it cites code. The executor's primary job here is verification, not prose authoring.

    **Step 1 — Replace the §6.2 code block with the actual current `cwt()` method body.** Read `src/subshader/dsp/cwt.py`, find the `cwt()` method on the `CWT` base class, and paste its body verbatim into the §6.2 code block (replacing the stale 2026-03-era body that referenced `wavelet.py`). Update the source comment to `# Source: src/subshader/dsp/cwt.py — CWT.cwt()`. The body MUST be drawn from the current `cwt.py` AST, not paraphrased — the §6.2 verify step grep-confirms at least one current signature line appears verbatim.

    **Step 2 — Verify the §6.1 parameter table against `src/subshader/config.py`.** For each parameter row (notes_per_octave, num_octaves, root_note_a0_hz, typical_sampling_freq, num_cycles, num_fwhm_cycles, target_width):
    - Confirm field name matches the current dataclass (might be `WaveletConfig` or `CWTConfig` — STATE.md "WaveletConfig=CWTConfig alias added" implies both names work; pick the canonical one used in current source)
    - Confirm default value matches the dataclass default
    - Update any mismatches; flag any missing parameters or removed parameters in a `<!-- DRAFT -->` block for user resolution

    **Step 3 — Verify the §6.5 class hierarchy.** Already refreshed in Task 1; this is the verification pass. Read `dsp.py` and the three backend files; confirm the diagram is accurate.

    **Step 4 — Add `<!-- DRAFT -->` skeleton drafts under each `[WRITE: ...]` marker in §6.1 / §6.2 / §6.3 / §6.4 / §6.5.** Drafts pull from the actual source code — for example, the §6.3 post-processing draft should describe the actual `discard_unreliable_coefs` / `extract_hop_center` / `downsample` methods as they exist in `cwt.py` today, citing line ranges so the user can cross-reference while authoring.

    **Step 5 — Resolve the §6 timing-bar-chart `[FIGURE: timing_bar_chart ...]` marker** (left in place by Task 1 if the path was unsettled). Either:
    - (a) Replace with a markdown image reference to the chart's actual current location on disk (look up via `ls assets/images/` and the asset-lifecycle rule from STATE.md), OR
    - (b) Generate the chart fresh via the timing helpers and save to a documented location, then insert the image reference, OR
    - (c) If the §6 narrative no longer needs the timing chart at all (user choice during Task 6.1 checkpoint can also resolve), remove the marker entirely.

    Whichever path is taken, no `[FIGURE: timing_bar_chart` substring may remain in DSP.md when this task ends — the §6 verify step grep-confirms zero remaining contingent markers, satisfying the Task 8 acceptance criterion that demands "Zero `[FIGURE:` markers anywhere".

    **Hard constraints:**
    - Code blocks MUST be runnable (DOCS-05 + success-criterion #4 — "every code example in every README is accurate and runnable")
    - DO NOT invent illustrative stubs (D-20 + DOCS-05)
    - Audio-focused
    - §6.4 GPU section keeps its existing structure but verifies against current `GpuCWT` class (not archived `CuPyWavelet`)

    **Commit:** `docs(05-04): §6 implementation deep dive — verified source citations + drafts`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && grep -c "src/subshader/dsp/cwt\.py" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 1 ] && echo "PASS: §6 cites cwt.py") && (! grep -n "wavelet\.py" src/subshader/dsp/DSP.md && echo "PASS: no wavelet.py refs anywhere") && awk '/^## 6\./,/^## 7\./' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT" | (read n && [ "$n" -ge 4 ] && echo "PASS: §6 DRAFT blocks = $n (>=4, one per WRITE marker in §6)") && grep -c "class CWT\|CpuCWT\|GpuCWT\|PywaveletCWT\|class STFT" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 2 ] && echo "PASS: §6.5 mentions current class names ($n)") && (! grep -n "assets/images/benchmarks" src/subshader/dsp/DSP.md && echo "PASS: no benchmark/ refs") && (! grep -n "\[FIGURE: timing_bar_chart" src/subshader/dsp/DSP.md && echo "PASS: timing_bar_chart marker resolved") && python -c "import ast; ast.parse(open('src/subshader/dsp/cwt.py').read())" && echo "PASS: cwt.py parses cleanly" && awk '/^## 6\./,/^## 7\./' src/subshader/dsp/DSP.md | grep -cE "class_specific_cwt\(|discard_unreliable_coefs\(|normalize_by_scale\(|extract_hop_center\(" | (read n && [ "$n" -ge 1 ] && echo "PASS: §6 cites at least one verbatim cwt.py signature line ($n)")</automated>
  </verify>
  <acceptance_criteria>
    - §6.2 code block matches current `cwt()` method body in `src/subshader/dsp/cwt.py` (executor verifies by re-reading the source and the DSP.md block side-by-side)
    - At least one signature substring from `cwt.py` (one of: `class_specific_cwt(`, `discard_unreliable_coefs(`, `normalize_by_scale(`, `extract_hop_center(`) appears verbatim within §6 of DSP.md — guards against silent paraphrasing of the source
    - `src/subshader/dsp/cwt.py` parses cleanly under `ast.parse()` — sanity check the source itself is current and not in a broken state when §6.2 quotes from it
    - §6.1 parameter table values match defaults in current `WaveletConfig` / `CWTConfig` dataclass
    - §6.5 hierarchy diagram lists at least two of: `CWT`, `CpuCWT`, `GpuCWT`, `PywaveletCWT`, `STFT`
    - Zero `wavelet.py` references anywhere in DSP.md
    - Zero `assets/images/benchmarks` references anywhere in DSP.md (timing-bar-chart path resolved or chart regenerated to current location)
    - Zero `[FIGURE: timing_bar_chart` substrings remain in DSP.md — the §6 timing chart marker has been resolved (replaced with an image reference) or removed entirely
    - `awk '/^## 6\./,/^## 7\./' src/subshader/dsp/DSP.md | grep -c '<!-- DRAFT'` returns at least 4 (one per `[WRITE: ...]` marker in §6.1–§6.5) — per-section range gate
    - All `[WRITE: ...]` markers in §6 still present
    - All §6 code examples are runnable (no illustrative stubs)
  </acceptance_criteria>
  <done>§6 cites current source files accurately; parameter table aligns with current config; class hierarchy reflects post-Phase-8 layout; skeleton drafts in place for user prose authoring; the contingent timing_bar_chart marker is resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.1: User authors §6 final prose + verifies code citations</name>
  <what-built>
    - Verified §6.2 code block, §6.1 parameter table, §6.5 hierarchy
    - Skeleton drafts under §6's `[WRITE: ...]` markers
    - Resolved §6 timing-chart image path
  </what-built>
  <how-to-verify>
    1. Read §6.2's current code block side-by-side with `src/subshader/dsp/cwt.py` — every method shown must exist in the source as named
    2. Read §6.1 parameter table side-by-side with `src/subshader/config.py` — every row must match the current dataclass
    3. Rewrite each `[WRITE: ...]` in §6 into your own voice
    4. Confirm §6.5 hierarchy reflects what you actually have in the codebase (CWT, CpuCWT, GpuCWT, etc.)
    5. Spot-check that the timing bar chart image referenced in §6 actually exists on disk
  </how-to-verify>
  <resume-signal>Type "approved" once §6 is in your voice and code citations verify against current source, or describe needed changes before the §7 Future consolidation.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Consolidate §7 Future per D-17 + fold in 05-06 Future Applications gesture</name>
  <read_first>
    - src/subshader/dsp/DSP.md (post-Task-6 state — §§1–6 in user voice; §7 still has the original 3-block scaffold "Feature Hierarchy / ML Integration / Applications")
    - .planning/phases/05-documentation/05-CONTEXT.md (D-17 — sections 7–10 consolidate; 05-06 carves out the Future Applications blurb specifically; D-21 — terminology threshold from 'properties' to 'features' lives at §7)
    - .planning/ROADMAP.md (Phase 5 plan list — confirms 05-06 "Future Applications blurb (financial time series, heartbeat, brain signals)" folds into DSP §7 per D-31)
    - research/archive/docs/demo/readmes/wavelet/wavelet_foundations_outline.md (original §§7–10 reference)
  </read_first>
  <action>
    Consolidate §7 Future and fold in the 05-06 Future Applications blurb. Plan 05-06 does NOT have its own PLAN.md file (per the planning context); it is tracked in ROADMAP and folds in here.

    **Step 1 — Confirm §7 already has the consolidated heading + the terminology-threshold NOTE block** ("features" is the correct term from here forward) from the existing scaffold. Both are intentional and stay.

    **Step 2 — Add `<!-- DRAFT -->` skeleton drafts under each of the 3 `[WRITE: ...]` markers** ("Feature Hierarchy", "ML Integration", "Applications"). Drafts must be paragraph-level (a couple of sentences each), not section-level treatments — the entire §7 is a forward-pointer per D-17, not a full chapter.

    **Step 3 — Add the 05-06 Future Applications gesture as a new sub-block at the END of §7.** Per project memory `feedback_dsp_doc_focus.md`: this is a paragraph-level gesture, not a deep treatment. Suggested heading and skeleton:

    ```markdown
    **Beyond Audio (Plan 05-06)**

    <!-- DRAFT (Claude scaffold — user rewrites in own voice) -->
    The CWT's strength on non-stationary signals isn't unique to audio. The same machinery applies anywhere short-lived events live inside longer time series — financial markets (volatility clustering and regime shifts at multiple time scales), cardiology (heartbeat morphology and arrhythmia detection), neuroscience (brain-wave activity across alpha/beta/gamma bands). The audio examples in this document are concrete; the underlying tool generalizes.
    <!-- /DRAFT -->
    ```

    The user rewrites this paragraph at the checkpoint. Keep it audio-anchored — the gesture refers OUT to other domains; it does not bring those domains' material INTO the document.

    **Step 4 — Verify the §7 NOTE block is preserved** ("features" is the correct term from here forward) — this is the locked terminology-threshold marker per D-21.

    **Hard constraints:**
    - §7 stays a forward pointer — do not expand any sub-block to a full chapter
    - Cross-domain content stays paragraph-level only per project memory
    - 05-06 folds in here — there is no separate 05-06-PLAN.md in this run

    **Commit:** `docs(05-04): §7 Future consolidation + 05-06 cross-domain gesture`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && grep -nE "^## 7\. Future" src/subshader/dsp/DSP.md && echo "PASS: §7 heading present" && grep -nE "Beyond Audio|05-06|financial|heartbeat|brain" src/subshader/dsp/DSP.md | head -5 && grep -c "Beyond Audio\|05-06" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 1 ] && echo "PASS: 05-06 gesture folded in") && grep -c "features.*correct term\|features\" is the correct term" src/subshader/dsp/DSP.md | (read n && [ "$n" -ge 1 ] && echo "PASS: terminology threshold NOTE preserved") && awk '/^## 7\./,EOF' src/subshader/dsp/DSP.md | grep -c "<!-- DRAFT" | (read n && [ "$n" -ge 4 ] && echo "PASS: §7 DRAFT blocks = $n (>=4, three [WRITE:] markers + Beyond Audio gesture)")</automated>
  </verify>
  <acceptance_criteria>
    - `^## 7\. Future` heading present (preserved from scaffold)
    - The §7 terminology-threshold NOTE block ("features" is the correct term from here forward) is preserved verbatim
    - A new "Beyond Audio" / "Future Applications" sub-block exists at the end of §7 referencing financial time series, heartbeat, and brain signals as a paragraph-level gesture
    - `awk '/^## 7\./,EOF' src/subshader/dsp/DSP.md | grep -c '<!-- DRAFT'` returns at least 4 (one per existing `[WRITE: ...]` marker in §7 plus one for the 05-06 gesture) — per-section range gate
    - The original 3 sub-headings ("Feature Hierarchy", "ML Integration", "Applications") still appear (not collapsed into a single block)
    - §§1–6 untouched in this task's diff
  </acceptance_criteria>
  <done>§7 has skeleton drafts for the 3 forward-pointer blocks plus the 05-06 cross-domain gesture; terminology threshold preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.1: User authors §7 Future final prose</name>
  <what-built>
    Skeleton drafts under §7's `[WRITE: ...]` markers + a new "Beyond Audio" sub-block folding in 05-06.
  </what-built>
  <how-to-verify>
    1. Rewrite each `[WRITE: ...]` in §7 into your own voice
    2. Rewrite the "Beyond Audio" cross-domain gesture in your voice — keep it paragraph-level, do not expand into examples per the audio-focus rule
    3. Confirm the terminology threshold NOTE still reads correctly in context
    4. Read §7 end-to-end with §6 — does §7 land as a forward pointer (not a full chapter) per D-17?
  </how-to-verify>
  <resume-signal>Type "approved" once §7 is in your voice and the 05-06 cross-domain gesture lands as a brief reference, or describe needed changes before the final read-through.</resume-signal>
</task>

<task type="auto">
  <name>Task 8: Final read-through + acceptance check — DSP.md ships</name>
  <read_first>
    - src/subshader/dsp/DSP.md (full file — final state after Tasks 1–7)
    - .planning/phases/05-documentation/05-CONTEXT.md (re-verify D-04, D-16, D-17, D-20, D-21, D-25, D-30 against final state)
    - .planning/ROADMAP.md (success-criteria #2 and #4 for Phase 5)
  </read_first>
  <action>
    Final acceptance pass. The user has authored final prose during the per-section checkpoints; this task verifies the document as a shipping artifact.

    **Step 1 — Marker sweep.** Confirm zero remaining markers that signal incomplete authoring (legend block at the top of the file is exempted by structural slicing — `awk '/^## 1\./,EOF'` skips the H1 + blockquote legend + first horizontal rule):
    - `[WRITE:` count in body = 0 (legend block is structurally exempted; line numbers are NOT used)
    - `[PLACEHOLDER:` count in body = 0 (legend block structurally exempted)
    - `<!-- DRAFT` count anywhere = 0 (every draft has been resolved during checkpoints)
    - `[FIGURE:` count anywhere = 0 (every figure justification was replaced by an actual markdown image reference during Tasks 3–6, including the contingent timing_bar_chart marker resolved in Task 6)

    If any of these counts are non-zero outside the legend, raise it as a blocking issue at this task's checkpoint — do not silently delete the user's pending work.

    **Step 2 — Path sweep.** Confirm:
    - Zero `wavelet.py` references
    - Zero `audio_input.py` references
    - Zero `assets/images/benchmarks/` references
    - Every `assets/images/dsp/*.png` referenced from DSP.md exists on disk
    - Every code example in §6 cites a current source file (`cwt.py` / `pywavelet.py` / `stft.py` / `wavelet_kernel.py` / `gaussian.py` / `dsp.py` / `config.py`)

    **Step 3 — Terminology ladder check.** `grep -nE '\bfeatures\b|\bpatterns\b' src/subshader/dsp/DSP.md` — every match line number must be at or below the §7 Future heading line. Any earlier match is a D-21 violation.

    **Step 4 — Figure regeneration test.** Run `python research/test_suite.py --figures` (or whichever flag from Task 3). Confirm exit code 0 and all 7 PNGs in `assets/images/dsp/` regenerate.

    **Step 5 — Cross-doc rule check.** Confirm no "see AUDIO.md §X" or "see RENDERER.md §Y" cross-references in DSP.md per D-01. Each doc owns its angle on shared concepts; reframing is OK, hyperlinks across docs that force reader hops are not.

    **Step 6 — Update ROADMAP.md.** Mark `05-04-PLAN.md` as `[x]` in the Phase 5 plan list. Do NOT touch 05-06 entry — that plan ships separately, even though its content folded into this DSP.md authoring run.

    **Commit:** `docs(05-04): final read-through — DSP.md ships`.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c "\[WRITE:" | (read n && [ "$n" -eq 0 ] && echo "PASS: no [WRITE: in body (legend structurally exempted)") && awk '/^## 1\./,EOF' src/subshader/dsp/DSP.md | grep -c "\[PLACEHOLDER:" | (read n && [ "$n" -eq 0 ] && echo "PASS: no [PLACEHOLDER: in body") && (! grep -n "<!-- DRAFT" src/subshader/dsp/DSP.md && echo "PASS: no DRAFT blocks remain") && (! grep -n "\[FIGURE:" src/subshader/dsp/DSP.md && echo "PASS: no [FIGURE: markers remain") && (! grep -n "wavelet\.py\|audio_input\|assets/images/benchmarks" src/subshader/dsp/DSP.md && echo "PASS: no stale paths") && for f in $(grep -oE "assets/images/dsp/[^)\"' ]+\.png" src/subshader/dsp/DSP.md | sort -u); do test -f "$f" || { echo "FAIL: missing $f"; exit 1; }; done && echo "PASS: every image ref resolves" && python research/test_suite.py --figures 2>&1 | tail -5 && echo "---" && grep -nE "\bfeatures\b|\bpatterns\b" src/subshader/dsp/DSP.md | awk -F: '{print $1}' | while read ln; do future_ln=$(grep -n "^## 7\. Future" src/subshader/dsp/DSP.md | head -1 | awk -F: '{print $1}'); [ "$ln" -ge "$future_ln" ] || { echo "FAIL: features/patterns at line $ln (before §7 at $future_ln)"; exit 1; }; done && echo "PASS: terminology ladder ok"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `[WRITE:` markers in the document body (legend block is structurally exempted via `awk '/^## 1\./,EOF'` — the marker convention bullet is allowed to retain the literal string)
    - Zero `[PLACEHOLDER:` markers in the document body (same legend exemption)
    - Zero `<!-- DRAFT` blocks anywhere
    - Zero `[FIGURE:` markers anywhere (including the contingent `[FIGURE: timing_bar_chart` resolved in Task 6)
    - Zero `wavelet.py` references
    - Zero `audio_input.py` references
    - Zero `assets/images/benchmarks/` references
    - Every `assets/images/dsp/*.png` referenced from DSP.md exists on disk
    - Every line containing `\bfeatures\b` or `\bpatterns\b` in DSP.md is at or below the `## 7. Future` heading line
    - `python research/test_suite.py --figures` exits 0 and regenerates all 7 PNGs
    - `.planning/ROADMAP.md` Phase 5 plan list marks `05-04-PLAN.md` as `[x]`
    - DSP.md has no cross-doc hyperlinks ("see AUDIO.md §X" pattern)
  </acceptance_criteria>
  <done>DSP.md ships: every marker resolved, every path current, every figure regenerable, terminology ladder honored, cross-doc rules respected, ROADMAP updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8.1: Final user sign-off — DSP.md publishes</name>
  <what-built>
    DSP.md as a complete, user-authored, publishable artifact.
  </what-built>
  <how-to-verify>
    1. Read DSP.md end-to-end — does it read in your voice from §1 to §7?
    2. Open every figure in `assets/images/dsp/` — visual consistency across all 7?
    3. Run any §6 code example you can run locally — do they execute against current source?
    4. Confirm no cross-doc hyperlinks force a reader to hop docs (D-01)
    5. Confirm the storyline lands — a reader without DSP background can follow §1 → §7 and understand what SubShader does
  </how-to-verify>
  <resume-signal>Type "approved" to ship DSP.md and close Plan 05-04, or list residual edits needed before publish.</resume-signal>
</task>

</tasks>

<verification>
- DSP.md has zero remaining authoring markers (`[WRITE:`, `[PLACEHOLDER:`, `<!-- DRAFT`, `[FIGURE:`) outside the legend block (structurally exempted via `awk '/^## 1\./,EOF'`)
- DSP.md has zero stale paths (`wavelet.py`, `audio_input.py`, `assets/images/benchmarks/`)
- Every `assets/images/dsp/*.png` referenced from DSP.md exists on disk
- Foundation figures regenerate via `python research/test_suite.py --figures` (or `--foundations-figures` if that flag was chosen)
- All foundation helpers live in `research/figures.py` and consume `research/utilities/style.py` constants — no parallel helper system per D-08 / D-19
- Code examples in §6 are extracted verbatim from current source files per D-20
- Terminology ladder honored: `\bfeatures\b` and `\bpatterns\b` appear only at or below the §7 Future heading per D-21
- §7 consolidates wavelet-foundations §§7–10 plus the 05-06 cross-domain gesture per D-17
- Cross-doc rule (D-01): zero hyperlink cross-references to AUDIO.md or RENDERER.md
- ROADMAP marks 05-04 as `[x]`
</verification>

<success_criteria>
- DSP.md ships as a user-authored README in user's voice (DOCS-02 + DOCS-06)
- Every code example is accurate and runnable against current source files (DOCS-05 + Phase 5 success-criterion #4)
- Foundation figures share visual language with the existing comparison-grid + per-signal figures via shared `research/figures.py` + `research/utilities/style.py` infrastructure (D-08)
- Document is internally consistent: locked spine from Task 1 holds through Task 8, terminology ladder honored, cross-doc rules respected
- The collaborative authoring loop completed end-to-end without the executor authoring final prose for the user
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-04-SUMMARY.md` documenting:
- Storyline-edit decisions made during the workshop pass (which figure justifications shifted, which paths needed manual resolution)
- Per-section authoring iterations (which sections needed multiple checkpoints, which voice / framing decisions emerged)
- Foundation-figure helper integration choices (CLI flag chosen, helper-function organization in `research/figures.py`)
- Any §7 cross-domain gesture wording iterations (so the 05-06 followup task can pick up the loose thread)
- Any residual `assets/images/dsp/*.png` style decisions worth preserving for future doc figures
</output>
