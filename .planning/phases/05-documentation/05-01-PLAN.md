---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - research/benchmark.py
  - research/utilities/constants.py
  - assets/images/benchmarks/comparison_grid.png
autonomous: false
requirements: [DOCS-05]

must_haves:
  truths:
    - "Comparison grid figure exists showing 3 signals x 3 representations in a single image"
    - "benchmark.py has a --comparison-grid flag that generates the grid figure"
  artifacts:
    - path: "assets/images/benchmarks/comparison_grid.png"
      provides: "3x3 comparison grid figure for README.md Performance section"
    - path: "research/benchmark.py"
      provides: "--comparison-grid flag"
      contains: "--comparison-grid"
  key_links:
    - from: "research/benchmark.py"
      to: "assets/images/benchmarks/comparison_grid.png"
      via: "--comparison-grid flag generates the file"
      pattern: "comparison.grid"
---

<objective>
Build the comparison grid figure for Phase 5 documentation: a 3x3 grid (signals x representations) generated via benchmark.py.

Purpose: The comparison grid is the visual centerpiece of the README Performance section. All figure generation must live in benchmark.py per user decision.
Output: comparison_grid.png, updated benchmark.py with --comparison-grid flag.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

@research/benchmark.py
@research/utilities/constants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --comparison-grid flag to benchmark.py</name>
  <files>research/benchmark.py, research/utilities/constants.py</files>
  <read_first>
    - research/benchmark.py (full file — understand existing figure generation patterns, argparse setup, render_spectrogram_row, create_figure_scaffold, render_top_row utilities)
    - research/utilities/constants.py (understand BENCHMARKS_DIR and other path constants)
    - research/utilities/__init__.py (understand exports)
    - assets/images/benchmarks/stubs/ (check what PyWavelet stubs exist)
  </read_first>
  <action>
    Add a new `--comparison-grid` argparse flag to benchmark.py that generates a single 3x3 matplotlib figure:

    **Grid layout:**
    - Columns: chirp signal (10 seconds), polyphonic signal (~16 seconds), musical signal (~8 bars)
    - Rows: STFT spectrogram, PyWavelet CWT, SubShader CWT
    - Use the existing signal loading and DSP pipeline already in benchmark.py (AudioInput, PyWavelet, NumPyWavelet or CuPyWavelet, scipy STFT)

    **Style per CONTEXT.md decisions:**
    - Minimal/no axis labels (remove or shrink to bare minimum)
    - Squeeze subplots close together using plt.subplots_adjust(wspace=0.02, hspace=0.05) or similar tight spacing
    - Add light grid lines on each subplot
    - Add row labels on the left (STFT, PyWavelet, SubShader) and column labels on top (Chirp, Polyphonic, Musical)
    - Use decorators: thin border around each subplot

    **Implementation pattern:**
    - Follow the existing `--figures` mode pattern in benchmark.py for signal loading and DSP execution
    - Create a new function `generate_comparison_grid()` that:
      1. Loads all three audio signals using existing AudioInput pattern
      2. Computes STFT (using scipy.signal.stft with STFT_NPERSEG), PyWavelet CWT (using PyWavelet class), and SubShader CWT (using NumPyWavelet or CuPyWavelet) for each signal
      3. Creates a single plt.figure with 3x3 grid of subplots via fig, axes = plt.subplots(3, 3, figsize=(18, 10))
      4. Renders each cell using pcolormesh or imshow (match existing render pattern)
      5. Saves to BENCHMARKS_DIR / "comparison_grid.png" at 200 DPI
    - Wire the flag into the argparse section and run_modes dispatch

    **Audio signals (from existing constants):**
    - Chirp: use existing chirp generation (CHIRP_F0=100, CHIRP_F1=10000) but limit to 10 seconds
    - Polyphonic: AUDIO_POLYPHONIC constant
    - Musical: AUDIO_MUSICAL constant

    **Note:** Layout is NOT locked per CONTEXT.md — this is a working draft that user will workshop. Focus on getting all 9 cells rendering correctly; aesthetic tuning happens at checkpoint.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python research/benchmark.py --comparison-grid 2>&1 | tail -20 && test -f assets/images/benchmarks/comparison_grid.png && echo "PASS: comparison_grid.png exists"</automated>
  </verify>
  <acceptance_criteria>
    - research/benchmark.py contains `--comparison-grid` in argparse section
    - research/benchmark.py contains `def generate_comparison_grid`
    - assets/images/benchmarks/comparison_grid.png exists after running `python research/benchmark.py --comparison-grid`
    - The figure contains 9 subplots (3 rows x 3 columns)
    - Row labels include "STFT", "PyWavelet", "SubShader"
    - Column labels include "Chirp", "Polyphonic", "Musical"
  </acceptance_criteria>
  <done>Running `python research/benchmark.py --comparison-grid` produces a 3x3 comparison grid figure at assets/images/benchmarks/comparison_grid.png with all 9 cells rendering real DSP output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approves comparison grid layout</name>
  <files>assets/images/benchmarks/comparison_grid.png</files>
  <action>
    Present the generated comparison_grid.png to the user for layout review. This is the workshop step from CONTEXT.md where exact layout is decided.
  </action>
  <what-built>3x3 comparison grid figure showing chirp/polyphonic/musical signals processed by STFT/PyWavelet/SubShader CWT. Layout not locked — this is the workshop step from CONTEXT.md.</what-built>
  <how-to-verify>
    1. Open assets/images/benchmarks/comparison_grid.png
    2. Verify all 9 cells have real rendered data (no blank/stub cells)
    3. Check: columns are chirp, polyphonic, musical
    4. Check: rows are STFT, PyWavelet, SubShader
    5. Evaluate spacing, labels, grid lines
    6. Decide: linear chirp vs bouncing chirp for Column 1
    7. Note any layout changes needed (spacing, sizing, colors, labels)
  </how-to-verify>
  <verify>
    <automated>test -f /home/eddie-water/dev/python/sub-shader/assets/images/benchmarks/comparison_grid.png && echo "PASS: file exists for review"</automated>
  </verify>
  <acceptance_criteria>
    - User has reviewed assets/images/benchmarks/comparison_grid.png
    - User has provided approval or specific layout feedback
  </acceptance_criteria>
  <done>User has approved comparison grid layout or provided actionable feedback for iteration</done>
  <resume-signal>Type "approved" or describe layout changes needed</resume-signal>
</task>

</tasks>

<verification>
- `python research/benchmark.py --comparison-grid` runs without error
- assets/images/benchmarks/comparison_grid.png exists and is a valid PNG
- benchmark.py argparse includes `--comparison-grid` flag
</verification>

<success_criteria>
- Comparison grid figure generated with all 9 cells rendering real DSP data
- User has approved or provided feedback on grid layout
- Figure generation is repeatable via benchmark.py flag
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-01-SUMMARY.md`
</output>
