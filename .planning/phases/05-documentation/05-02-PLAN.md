---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DSP.md
autonomous: true
requirements: [DOCS-02, DOCS-05, DOCS-06]

must_haves:
  truths:
    - "DSP.md exists at project root with scaffold covering wavelet foundations sections 1-6"
    - "Every section has placeholder markers distinguishing done content from sections needing writing"
    - "Code examples in DSP.md are extracted from actual source files and are accurate"
    - "No paragraph-length prose written by Claude — only scaffold structure and placeholders"
  artifacts:
    - path: "DSP.md"
      provides: "DSP module documentation scaffold"
      contains: "## 1. Motivation"
  key_links:
    - from: "DSP.md"
      to: "src/subshader/dsp/wavelet.py"
      via: "Code examples extracted from wavelet.py"
      pattern: "class_specific_cwt"
    - from: "DSP.md"
      to: "research/docs/demo/readmes/wavelet/wavelet_foundations_outline.md"
      via: "Section structure derived from outline sections 1-6"
      pattern: "Inner Product"
---

<objective>
Create the DSP.md scaffold at project root — the pedagogical wavelet foundations document covering sections 1-6 of the wavelet foundations outline, plus a consolidated Future section for sections 7-10.

Purpose: DSP.md is the deepest technical document in the project. The scaffold gives the user exact structure, section guidance, image placeholders, candidate analogies, and code examples so they can author prose in their own voice.
Output: DSP.md scaffold file at project root.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

@research/docs/demo/readmes/wavelet/wavelet_foundations_outline.md
@research/docs/demo/readmes/wavelet/wavelet_foundations.ipynb
@research/docs/demo/discussion_summary.md
@research/docs/demo/readmes/wavelet/dsp_submodule_readme_claude.md
@src/subshader/dsp/wavelet.py
@src/subshader/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DSP.md scaffold from foundations outline</name>
  <files>DSP.md</files>
  <read_first>
    - research/docs/demo/readmes/wavelet/wavelet_foundations_outline.md (FULL — this is the structural source for sections 1-6; every header, sub-header, and bullet point maps to DSP.md sections)
    - research/docs/demo/discussion_summary.md (terminology ladder rules: "properties" not "features" before section 7; concrete before abstract; candidate analogies)
    - research/docs/demo/readmes/wavelet/dsp_submodule_readme_claude.md (workflow instructions for DSP README)
    - src/subshader/dsp/wavelet.py (CWT pipeline: cwt() method, class_specific_cwt, normalize_by_scale, compute_mag, discard_unreliable_coefs, extract_hop_center, downsample — for accurate code examples in Section 6)
    - src/subshader/config.py (WaveletConfig dataclass — for configuration section)
  </read_first>
  <action>
    Create DSP.md at project root as a scaffold document. This is NOT prose — it is structure + placeholders + candidate analogies + code examples per DOCS-06 decision.

    **Scaffold format (per CONTEXT.md):**
    - `[DONE: keep this text]` — for content from outline that is ready as-is
    - `[WRITE: "specific topic to cover here"]` — blank sections needing user prose
    - `[PLACEHOLDER: figure — "description"]` — images to be generated
    - `candidate analogy:` prefix for suggested analogies user can accept/reject/rewrite

    **Section structure (from wavelet_foundations_outline.md sections 1-6 + consolidated Future):**

    ```
    # DSP: Wavelet Transform Foundations

    ## 1. Motivation
    [WRITE: "Why time-frequency analysis — what frequencies are present and when they occur"]
    [WRITE: "Why Fourier Transform has limitations for this task"]
    [WRITE: "Why Wavelet Transform is better suited — signal decomposition as the common foundation"]
    candidate analogy: "unmixing paint" — figuring out how much of each color contributed

    ## 2. Foundations: Inner Product

    ### 2.1 Signal Decomposition
    [WRITE: "End goal is decomposing a composite signal into fundamental components"]
    [WRITE: "Two motivations: (1) define fundamental components, (2) measure how much of each"]
    candidate analogy: paint mixing — already in outline, user voice established here

    ### 2.2 Inner Product
    [WRITE: "Similarity score between function f and reference function g"]
    [Include inner product notation: ⟨f, g⟩]
    [WRITE: "Bridge to audio: function=signal, reference=properties we measure"]
    TERMINOLOGY NOTE: Use "properties" here, NOT "features" or "patterns" (per discussion_summary.md)

    ### 2.3 Dot Product
    [WRITE: "Simplest form of inner product with geometric visualization"]
    [PLACEHOLDER: figure — "2D vector dot product geometric interpretation, generated via benchmark.py --foundations-figures"]

    ### 2.4 Projection
    [WRITE: "Casting a shadow — how much of one vector lies in the direction of another"]
    [PLACEHOLDER: figure — "Vector projection visualization"]
    candidate analogy: "casting a shadow" for projection

    ### 2.5 Basis Decomposition
    [WRITE: "Decomposing a vector into basis components"]
    [PLACEHOLDER: figure — "Basis decomposition of 2D vector"]

    ### 2.6 Continuous Inner Product
    [WRITE: "Bridge from discrete dot product to continuous inner product integral"]

    ### 2.7 Signal Decomposition with Inner Product
    [WRITE: "Connecting continuous inner product to signal analysis"]

    ## 3. Fourier Transform
    [WRITE: "Frequency templates — sinusoidal basis functions"]
    [WRITE: "What FT measures and what it loses (no time information)"]
    [PLACEHOLDER: figure — "Fourier basis functions"]

    ## 4. Short-Time Fourier Transform (STFT)
    [WRITE: "Windowed compromise — fixed time-frequency resolution tradeoff"]
    [WRITE: "Why fixed resolution is a problem for audio with both transients and sustained tones"]
    [PLACEHOLDER: figure — "STFT windowing illustration"]
    candidate analogy: "measuring with one ruler size for everything"

    ## 5. Wavelet Transform
    [WRITE: "Adaptive resolution — different rulers for different frequencies"]
    [WRITE: "Mother wavelet concept, scaling and translation"]
    [WRITE: "Why this is better for audio: fine time resolution at high freq, fine freq resolution at low freq"]
    [PLACEHOLDER: figure — "Wavelet scaling at different frequencies"]
    candidate analogy: "measuring with different rulers at different frequencies"

    ## 6. Implementation Deep Dive

    ### 6.1 Wavelet Construction
    [WRITE: "How SubShader builds wavelet kernels — chromatic scale, Morlet wavelet"]
    [Reference WaveletConfig parameters: num_octaves, voices_per_octave, sigma]

    ### 6.2 CWT Pipeline
    [Code example — the full pipeline from wavelet.py cwt() method:]
    ```python
    # Source: src/subshader/dsp/wavelet.py Wavelet.cwt
    cwt_coefs = self.class_specific_cwt(input_data)
    cwt_coefs = self.normalize_by_scale(cwt_coefs)
    mag_coefs = self.compute_mag(cwt_coefs)
    reliable_coefs = self.discard_unreliable_coefs(mag_coefs)
    hop_center_coefs = self.extract_hop_center(reliable_coefs)
    downsampled_coefs = self.downsample(hop_center_coefs, self.output_n)
    ```
    [WRITE: "Walk through each step — what it does and why"]

    ### 6.3 Post-Processing Pipeline
    [WRITE: "L1 kernel normalization at construction — why bias is structural"]
    [WRITE: "Cone of influence — edge contamination and why we discard it"]
    [WRITE: "Hop center extraction — trimming overlap wings"]
    [WRITE: "Downsampling to output resolution"]

    ### 6.4 GPU Acceleration
    [WRITE: "CuPy implementation — same algorithm, GPU execution"]
    [WRITE: "Why GPU matters for real-time: FFT-based convolution is parallelizable"]
    [PLACEHOLDER: figure — "numpy_vs_cupy_diff.png" — NOTE: this image exists at assets/images/benchmarks/numpy_vs_cupy_diff.png, moved here from top-level README per decision]
    ![NumPy vs CuPy Coefficient Difference](assets/images/benchmarks/numpy_vs_cupy_diff.png)

    ### 6.5 Configuration
    [List actual WaveletConfig parameters with their defaults from config.py:]
    - num_octaves (default from config)
    - voices_per_octave (default from config)
    - sigma (default from config)
    - base_freq (default from config)
    [WRITE: "What each parameter controls and when to change it"]

    ## 7. Future
    [WRITE: "Consolidated forward pointer for sections 7-10 from foundations outline"]
    [WRITE: "Feature hierarchy (low/mid/high-level signal features)"]
    [WRITE: "ML integration possibilities"]
    [WRITE: "Applications beyond audio"]
    NOTE: Full treatment of these topics is deferred — this section preserves their info as a roadmap
    ```

    **Critical rules:**
    - Do NOT write paragraph-length prose. Every content section is a `[WRITE: ...]` placeholder.
    - Candidate analogies are labeled `candidate analogy:` as one-liners, never embedded in prose.
    - Code examples MUST be extracted from actual source (wavelet.py, config.py) — verify method names and parameter names match.
    - Use "properties" not "features/patterns" before Section 7 (per discussion_summary.md terminology ladder).
    - Image placeholders use `[PLACEHOLDER: figure — "description"]` format for figures not yet generated.
    - For numpy_vs_cupy_diff.png which already exists, use actual markdown image syntax.
    - Include the terminology note about "properties" vs "features" inline where relevant so the user sees it while writing.
  </action>
  <verify>
    <automated>test -f /home/eddie-water/dev/python/sub-shader/DSP.md && grep -c "WRITE:" /home/eddie-water/dev/python/sub-shader/DSP.md && grep -c "PLACEHOLDER:" /home/eddie-water/dev/python/sub-shader/DSP.md && grep -c "candidate analogy:" /home/eddie-water/dev/python/sub-shader/DSP.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - DSP.md exists at project root
    - DSP.md contains `## 1. Motivation`
    - DSP.md contains `## 2. Foundations: Inner Product`
    - DSP.md contains `### 2.1 Signal Decomposition` through `### 2.7`
    - DSP.md contains `## 3. Fourier Transform`
    - DSP.md contains `## 4. Short-Time Fourier Transform`
    - DSP.md contains `## 5. Wavelet Transform`
    - DSP.md contains `## 6. Implementation Deep Dive`
    - DSP.md contains `## 7. Future`
    - DSP.md contains at least 15 `[WRITE:` placeholders
    - DSP.md contains at least 4 `[PLACEHOLDER:` image markers
    - DSP.md contains at least 3 `candidate analogy:` lines
    - DSP.md contains the CWT pipeline code example with `class_specific_cwt`, `normalize_by_scale`, `compute_mag`, `discard_unreliable_coefs`, `extract_hop_center`, `downsample`
    - DSP.md contains `numpy_vs_cupy_diff.png` image reference
    - DSP.md does NOT contain any paragraph of 3+ sentences written as prose (scaffold only)
    - DSP.md does NOT use the word "features" or "patterns" before the `## 7. Future` section
  </acceptance_criteria>
  <done>DSP.md scaffold exists at project root with sections 1-7, all placeholder markers, candidate analogies, accurate code examples from source, and no Claude-written prose</done>
</task>

<task type="auto">
  <name>Task 2: Extract and verify code examples for DSP.md</name>
  <files>DSP.md</files>
  <read_first>
    - DSP.md (just created in Task 1 — verify code examples are present)
    - src/subshader/dsp/wavelet.py (verify every method name and parameter in DSP.md code examples matches actual source)
    - src/subshader/config.py (verify WaveletConfig parameter names and defaults match what DSP.md lists)
  </read_first>
  <action>
    Cross-check every code example and parameter reference in DSP.md against actual source files:

    1. **CWT pipeline code block** — verify the 6 method calls in order match wavelet.py Wavelet.cwt():
       - `class_specific_cwt(input_data)` — confirm parameter name is `input_data`
       - `normalize_by_scale(cwt_coefs)` — confirm it takes CWT output
       - `compute_mag(cwt_coefs)` — confirm method name
       - `discard_unreliable_coefs(mag_coefs)` — confirm method name
       - `extract_hop_center(reliable_coefs)` — confirm method name
       - `downsample(hop_center_coefs, self.output_n)` — confirm second parameter

    2. **WaveletConfig parameters** — read config.py WaveletConfig dataclass, extract:
       - Every field name and its default value
       - Update DSP.md Section 6.5 with actual field names and defaults
       - If DSP.md has wrong field names, fix them

    3. **Class hierarchy** — verify wavelet.py exports match what DSP.md references:
       - Wavelet (base), PyWavelet, AntsWavelet, NumPyWavelet, CuPyWavelet, CuWavelet
       - If DSP.md references a class that doesn't exist, fix it

    If any mismatch found, update DSP.md to match actual source. This is the DOCS-05 requirement — no inaccurate code examples.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader && python -c "
from subshader.dsp.wavelet import Wavelet
# Verify methods referenced in DSP.md exist
methods = ['cwt', 'class_specific_cwt', 'normalize_by_scale', 'compute_mag', 'discard_unreliable_coefs', 'extract_hop_center', 'downsample']
for m in methods:
    assert hasattr(Wavelet, m), f'Missing method: {m}'
print('All DSP.md method references verified against source')
from subshader.config import WaveletConfig
print(f'WaveletConfig fields: {[f.name for f in WaveletConfig.__dataclass_fields__.values()]}')
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every method name in DSP.md code examples exists on the Wavelet class in wavelet.py
    - Every WaveletConfig field name in DSP.md Section 6.5 matches a real field in config.py
    - No illustrative or placeholder method names remain (no `# simplified` or `# illustrative` comments)
  </acceptance_criteria>
  <done>All code examples and parameter references in DSP.md are verified accurate against src/subshader/dsp/wavelet.py and src/subshader/config.py</done>
</task>

</tasks>

<verification>
- DSP.md exists at project root
- DSP.md has sections 1-7 matching wavelet foundations outline structure
- All code examples verified against actual source
- No Claude-written prose paragraphs — scaffold only
</verification>

<success_criteria>
- DSP.md scaffold covers wavelet foundations sections 1-6 + consolidated Future section
- Every placeholder is tagged with [WRITE:], [PLACEHOLDER:], or [DONE:]
- Candidate analogies from discussion_summary.md are present as labeled suggestions
- Terminology ladder respected (no "features/patterns" before Section 7)
- All code examples match actual source — DOCS-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
