---
phase: 07-visual-style-system-and-frequency-range-configuration
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - research/figures.py
  - research/comparison.py
  - research/test_suite.py
  - research/timing.py
autonomous: true
requirements: [STY-06, STY-07, RTK2-07, RTK2-08, RTK2-09]

must_haves:
  truths:
    - "figures.py uses style.py constants — no hardcoded visual values remain"
    - "Comparison grid column titles have visible top margin and are centered over spectrogram columns"
    - "comparison.py exists with generate_comparison_grid() and extensible method config list"
    - "test_suite.py imports from comparison.py for --comparison-grid and --comparison flags"
    - "research/timing.py is a thin profiler that reports timing from @timed attributes"
  artifacts:
    - path: "research/comparison.py"
      provides: "Method-vs-method comparison grid and timing table"
      contains: "COMPARISON_METHODS"
      exports: ["generate_comparison_grid", "generate_timing_bar_chart"]
    - path: "research/figures.py"
      provides: "Per-signal figure generation (ReadmeFigures)"
      contains: "from utilities import style"
  key_links:
    - from: "research/test_suite.py"
      to: "research/comparison.py"
      via: "import generate_comparison_grid"
      pattern: "from comparison import"
    - from: "research/figures.py"
      to: "research/utilities/style.py"
      via: "style constant references"
      pattern: "style\\."
    - from: "research/comparison.py"
      to: "research/utilities/dsp_helpers.py"
      via: "compute function imports"
      pattern: "from utilities.*import.*compute_stft_frame"
---

<objective>
Migrate all figure code to use style.py, extract comparison.py with extensible method list, and fix comparison grid header spacing.

Purpose: This is the integration plan — connecting the style system (Plan 01) to all consumers, extracting the comparison concern from figures.py into comparison.py (D-17, D-24, D-25), and fixing the grid header visual issues (D-06, D-07).

Output: figures.py using style.py constants; comparison.py with COMPARISON_METHODS config list and generate_comparison_grid(); fixed grid headers; test_suite.py updated imports.
</objective>

<execution_context>
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/workflows/execute-plan.md
@/home/eddie-water/dev/python/sub-shader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-CONTEXT.md
@.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-RESEARCH.md
@.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-01-SUMMARY.md
@.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-02-SUMMARY.md
@.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract comparison.py from figures.py with extensible method list and header fix</name>
  <files>research/comparison.py, research/figures.py, research/test_suite.py</files>
  <read_first>
    - research/figures.py (full file — need to identify generate_comparison_grid() and generate_timing_bar_chart() to extract)
    - research/utilities/style.py (created in Plan 01 — constants to reference)
    - research/utilities/dsp_helpers.py (compute_stft_frame and other DSP functions used by comparison)
    - research/test_suite.py (after Plan 03 — imports to update)
    - research/utilities/plotting.py (after Plan 01 — primitives available)
  </read_first>
  <action>
**1. Create `research/comparison.py`** (per D-17, D-24, D-25):

Extract `generate_comparison_grid()` and `generate_timing_bar_chart()` from `research/figures.py` into a new `research/comparison.py` module.

At the top of comparison.py, define the extensible method config list (per D-24):
```python
"""
Method-vs-method comparison figures and timing tables.

To add a new comparison method, append an entry to COMPARISON_METHODS:
    {"name": "my_method", "fn": my_compute_function, "label": "Display Label"}
"""
```

The COMPARISON_METHODS list structure (per D-24, D-25):
```python
COMPARISON_METHODS = [
    {"name": "stft",      "fn": compute_stft_frame,    "label": "STFT"},
    {"name": "pywavelet", "fn": run_pywavelet,          "label": "PyWavelet"},
    {"name": "numpy_cwt", "fn": run_numpy_cwt,          "label": "SubShader (NumPy)"},
    {"name": "gpu_cwt",   "fn": run_gpu_cwt,            "label": "SubShader (GPU)"},
]
```

Note: The current `generate_comparison_grid()` doesn't use a method list pattern — it has inline code for each method. The extraction should:
1. Move the function as-is first
2. Then refactor to iterate over COMPARISON_METHODS where feasible
3. The compute functions (`compute_stft_frame`, etc.) come from `utilities.dsp_helpers`
4. For GPU CWT: use deferred import inside the function (matching the Phase 05.2 pattern — `CuWavelet import deferred inside generate_comparison_grid()`)

**2. Fix comparison grid header** (per D-06, D-07):

In the extracted `generate_comparison_grid()` in comparison.py, fix the column title rendering. Currently at line 975 of figures.py:
```python
axes[0][col_idx + 1].set_title(col["label"], fontsize=LABEL_FONT_SIZE, fontweight="bold", pad=8)
```

Change `pad=8` to `pad=style.GRID_TITLE_PAD` (which is 20 in style.py). This gives visible breathing room. The `set_title` `loc="center"` (default) centers over the axes bounding box which includes the spectrogram.

Also update the `subplots_adjust` call to use style constants:
```python
plt.subplots_adjust(left=style.GRID_MARGIN, right=1 - style.GRID_MARGIN,
                    top=1 - style.GRID_MARGIN, bottom=style.GRID_MARGIN)
```

**3. Replace all hardcoded visual values** in the extracted comparison.py with `style.*` references:
- `GRID_CMAP = "inferno"` → `style.GRID_CMAP`
- `GRID_BG_COLOR = "#1A1A1A"` → `style.BG_COLOR`
- `GRID_WAVEFORM_COLOR = "#ffffcf"` → `style.GRID_WAVEFORM_COLOR`
- `LABEL_FONT_SIZE = 24` → `style.LABEL_FONT_SIZE`
- `LABEL_CHAR_WIDTH = 0.028` → `style.LABEL_CHAR_WIDTH`
- `LABEL_PAD = 0.14` → `style.LABEL_PAD`
- `figsize=(24, 16)` → `figsize=(style.GRID_FIGSIZE_W, style.GRID_FIGSIZE_H)`
- `hspace=0.08` → `hspace=style.GRID_HSPACE`
- `wspace=0.04` → `wspace=style.GRID_WSPACE`

Import pattern: `from utilities import style` (CWD-relative, matching research/ convention).

**4. Remove `generate_comparison_grid()` and `generate_timing_bar_chart()` from `research/figures.py`**. Keep `ReadmeFigures` class and per-signal figure methods in figures.py.

**5. Update `research/test_suite.py`** imports:
Change:
```python
from figures import ReadmeFigures, generate_comparison_grid, generate_timing_bar_chart
```
To:
```python
from figures import ReadmeFigures
from comparison import generate_comparison_grid, generate_timing_bar_chart
```
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader/research && python -c "from comparison import generate_comparison_grid, generate_timing_bar_chart, COMPARISON_METHODS; print(f'OK: {len(COMPARISON_METHODS)} methods')" && ! grep -q "def generate_comparison_grid" figures.py && echo "OK: removed from figures.py" && grep -q "from comparison import" test_suite.py && echo "OK: test_suite imports updated" && grep -q "style\." comparison.py && echo "OK: style refs present"</automated>
  </verify>
  <acceptance_criteria>
    - research/comparison.py exists and contains `def generate_comparison_grid`
    - research/comparison.py contains `def generate_timing_bar_chart`
    - research/comparison.py contains `COMPARISON_METHODS` as a list
    - research/comparison.py contains `from utilities import style` or `from utilities.style import`
    - research/comparison.py contains `style.GRID_TITLE_PAD` (header fix)
    - research/comparison.py does NOT contain `pad=8` (old header padding)
    - research/comparison.py contains `style.GRID_CMAP` (not hardcoded "inferno")
    - research/comparison.py contains `style.BG_COLOR` (not hardcoded "#1A1A1A")
    - research/figures.py does NOT contain `def generate_comparison_grid`
    - research/figures.py does NOT contain `def generate_timing_bar_chart`
    - research/test_suite.py contains `from comparison import generate_comparison_grid`
    - COMPARISON_METHODS has at least 3 entries (STFT, PyWavelet, SubShader NumPy)
  </acceptance_criteria>
  <done>comparison.py extracted with extensible method list, header fix applied, figures.py trimmed, test_suite.py imports updated</done>
</task>

<task type="auto">
  <name>Task 2: Migrate figures.py to use style.py constants throughout</name>
  <files>research/figures.py</files>
  <read_first>
    - research/figures.py (after Task 1 removed generate_comparison_grid — need to update remaining ReadmeFigures code)
    - research/utilities/style.py (constants to use)
    - research/utilities/plotting.py (after Plan 01 — primitives now use style.py, no style parameter)
  </read_first>
  <action>
Update all remaining code in `research/figures.py` to use `style.*` constants (per D-02):

**1. Add style import** at top of file:
```python
from utilities import style
```

**2. Remove the seaborn import block** (lines 10-14 of current figures.py):
```python
# DELETE:
try:
    import seaborn  # noqa: F401
    SEABORN_AVAILABLE = True
except ImportError:
    SEABORN_AVAILABLE = False
```

**3. Update ReadmeFigures class:**

The `__init__` method currently takes `backends=("matplotlib",)` parameter. Since we now have one canonical style (D-04), remove the `backends` parameter. The class should always use matplotlib (the only backend). Remove any backend iteration logic in `run_all()` and individual figure methods.

**4. Replace hardcoded values in per-signal figure methods** (chirp_signal_comparison, polyphonic_signal_comparison, musical_signal_comparison):

These methods call `create_figure_scaffold()` and `render_top_row()` from `utilities.plotting`. Since Plan 01 already updated those functions to use style.py directly (no style parameter), the figure methods no longer need to pass style dicts. Remove any `style=` keyword arguments from these calls.

**5. Replace any remaining hardcoded style values** in figures.py:
- Any `fontsize=24` → `fontsize=style.TITLE_FONT_SIZE` or `fontsize=style.LABEL_FONT_SIZE` as appropriate
- Any `"#1A1A1A"` → `style.BG_COLOR`
- Any `dpi=150` → `dpi=style.DEFAULT_DPI`
- Any `figsize=(20, ...)` → `figsize=(style.FIGURE_WIDTH, ...)`

Search the file for any literal color strings, font sizes, or layout values and replace with style.* references. The goal per D-02 is "no hardcoded visual values anywhere in figure code."

**6. Remove `BENCHMARKS_SEABORN_DIR` usage** from figures.py if present — seaborn output directory is no longer needed.
  </action>
  <verify>
    <automated>cd /home/eddie-water/dev/python/sub-shader/research && python -c "from figures import ReadmeFigures; print('OK: figures imports')" && ! grep -q "SEABORN_AVAILABLE" figures.py && echo "OK: no seaborn" && ! grep -q "DEFAULT_STYLE\|SEABORN_STYLE\|get_active_style" figures.py && echo "OK: no style dicts" && grep -c "style\." figures.py</automated>
  </verify>
  <acceptance_criteria>
    - figures.py contains `from utilities import style` or `from utilities.style import`
    - figures.py does NOT contain `import seaborn` or `SEABORN_AVAILABLE`
    - figures.py does NOT contain `DEFAULT_STYLE` or `SEABORN_STYLE` or `get_active_style`
    - figures.py does NOT contain `backends` parameter in ReadmeFigures.__init__
    - figures.py contains at least 3 references to `style.` (using style constants)
    - `python -c "from figures import ReadmeFigures"` succeeds (from research/ CWD)
    - figures.py does NOT contain `"#1A1A1A"` as a literal (replaced with style.BG_COLOR)
  </acceptance_criteria>
  <done>figures.py fully migrated to style.py constants, seaborn remnants removed, backend parameter eliminated</done>
</task>

</tasks>

<verification>
- `cd research && python -c "from comparison import generate_comparison_grid, COMPARISON_METHODS; print('comparison OK')"` succeeds
- `cd research && python -c "from figures import ReadmeFigures; print('figures OK')"` succeeds
- `cd research && python test_suite.py --help` shows all expected flags
- `grep -c "style\." research/comparison.py research/figures.py` shows multiple style references in both
- `grep "pad=8" research/comparison.py` returns nothing (header fix applied)
- `grep -r "SEABORN\|seaborn" research/figures.py research/test_suite.py` returns nothing
</verification>

<success_criteria>
- comparison.py exists with extensible COMPARISON_METHODS list
- Comparison grid header has increased title padding (D-06, D-07)
- figures.py uses style.py constants throughout — no hardcoded visual values
- test_suite.py correctly imports from both figures.py and comparison.py
- No seaborn references remain in figures.py or test_suite.py
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-style-system-and-frequency-range-configuration/07-04-SUMMARY.md`
</output>
