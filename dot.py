import numpy as np
import matplotlib.pyplot as plt

f_s = 100

t = 0 to 100 at f_s steps

signal = sin(2*pi*f*t)

basis_functions = []

for in range(0, 10):
    my_basis_functions.append(sin(2*pi*i*t))

self_dot_product = np.dot(signal, signal) 
dot_products = np.dot(signal, basis_functions) - the signal dot'd with each of the basis functions

plot the signal left and signal dot signal right

then each basis function in left column, and dot product in right column side by side

